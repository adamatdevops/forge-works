# Changelog

All notable changes to ForgeWorks are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added

- **Twenty-fourth vendored skill: `obra/test-driven-development` — 4th obra-vendor adoption + EXTENDS obra cluster from 3 to 4 skills (§AF + §AM + §AU + §AV at shared SHA `f2cbfbef...`, SECOND-LARGEST cluster in framework after ToB 7) + 13TH consecutive Tier 1 estimate-confirm (N=16 staleness datapoints) + RESOLVES AB-036 candidate-resolution event #2 (testing-anti-patterns → test-driven-development consolidation successor adopted) + 5-step AB-036 doctrine lifecycle now empirically validated end-to-end through TWO complete candidate-resolution events + CLOSES TWO UN-adopted-target gates on §AM (L179 + L287)** — adopted via the AGENTS.md §4.4.3 7-step process. Vendored at `.skills/obra/test-driven-development/` (3-file bundle: SKILL.md 2420t + `testing-anti-patterns.md` 1933t companion file [consolidated content from former standalone skill] + LICENSE byte-identical to §AF/§AM/§AU) from `github.com/obra/superpowers` @ SHA `f2cbfbefebbfef77321e4c9abc9e949826bea9d7` — **SAME SHA as §AF + §AM + §AU** (obra cluster extension to 4 skills). **AB-036 candidate-resolution event #2 RESOLVED:** at §AU adoption, event #2 was filed for `testing-anti-patterns` CONSOLIDATED INTO `test-driven-development` on 2025-12-18 commit `718ec45d3358`; §AV adopts the consolidation successor. **The 5-step AB-036 doctrine lifecycle is now empirically validated end-to-end through TWO complete events:** §AM resolves root-cause-tracing → systematic-debugging; §AV resolves testing-anti-patterns → test-driven-development. **TWO UN-adopted-target gate closures on §AM via §AV adoption:** §AM SKILL.md L179 + L287 references to `superpowers:test-driven-development` were UN-adopted; both gates now CLOSED. Canonical `§AM → §AV → §AF` chain replaces UN-adopted-advisory. **Obra cluster (4-skill EXTENSION — SECOND-LARGEST in framework after ToB 7):** §AF + §AM + §AU + §AV at shared SHA `f2cbfbef...`. **§AF + §AM + §AU gate entries BACKPATCHED**; §AM additionally backpatched to close TWO UN-adopted-target gates. **6-of-6 vendor clusters now span 6 distinct cluster sizes** (5 / 7 / **4 NEW** / 2 / 2 / 2). Size-class progression 2 → 3 → 4 → 5 → 7 spans the framework's working range. **Same-vendor 4-skill canonical chain inside obra cluster:** §AU receive-feedback → §AM debug → §AV TDD-fix → §AF verify-done. **SIXTH adopted-skill-to-adopted-skill internal reference cluster** — §AV ↔ §AM bidirectional canonical chain. SKILL.md body **2396 tokens** post-frontmatter-strip (body sha256 `0fa4bc2d40be30ec...`). Worst-case archive **4353 tokens**. Score sum 30. Author Jesse Vincent (obra), community. **Vendor concentration: 7 ToB (LARGEST at 7/24 = 29.2%) + 5 hamelsmu + 4 obra (TIED with Anthropic) + 4 Anthropic + 2 CodeRabbit + 2 OpenAI = 24 total**. Adoption budget: **24 / 30** (80% of soft cap; 6 slots remaining). **Loader smoke test:** 24 skills, 0 errors. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AV row NEWLY CREATED at adoption time; AB-036 candidate-resolution event #2 RESOLVED in eval-list at testing-anti-patterns row; budget ledger 23/30→24/30 with obra-cluster-4-skill-extension milestone; Counts 23→24.
- **Twenty-third vendored skill: `obra/receiving-code-review` — 3rd obra-vendor adoption + EXTENDS obra cluster from 2 to 3 skills (§AF + §AM + §AU at shared SHA `f2cbfbef...`, THIRD-LARGEST cluster in framework after hamelsmu 5 + ToB 7) + TWELFTH consecutive Tier 1 estimate-confirm at adoption review (eval-list pre-adoption Tier-1 estimate HELD; N=15 staleness datapoints) + SECOND AB-036 candidate-resolution event filed in this same commit (`testing-anti-patterns` was CONSOLIDATED INTO `test-driven-development` on 2025-12-18 — same consolidation day as root-cause-tracing → systematic-debugging that triggered AB-036 originally) + AB-036 doctrine VALIDATED for the second time (proactive consolidation-check predicted the right event class on a NEW skill at the SAME consolidation date) + 6-of-6 vendor clusters extended (obra grows to 3-skill, demonstrating cluster scales across sizes 2/3/5/7)** — adopted via the AGENTS.md §4.4.3 7-step process with new §4.4.3 step 2 Tier-staleness audit + proactive AB-036 follow-up audit. Vendored at `.skills/obra/receiving-code-review/` (single-file SKILL.md + LICENSE byte-identical to §AF/§AM at shared repo+SHA) from `github.com/obra/superpowers` @ SHA `f2cbfbefebbfef77321e4c9abc9e949826bea9d7` — **SAME SHA as §AF + §AM** (obra cluster extension to 3 skills; cluster co-adoption verified). Audited 2026-05-20: ✅ Tier-staleness audit PASSED — (i) NO file-write directives (grep clean); (ii) NO `allowed-tools` frontmatter; (iii) NO `commands/` dir; (iv) Only L205 references `gh api .../comments/{id}/replies` as USER-RECOMMENDATION (not agent-execution); (v) NO `Task`/cross-agent surface. **Tier 1 read-only** — pure methodology emitting inline-transcript content (Response Pattern, Forbidden Responses, Source-Specific Handling, YAGNI Check, Implementation Order, Push-Back guidance, Common Mistakes table, Real Examples). **AB-036 candidate-resolution event #2 (NEW — validates AB-036 doctrine in practice for the SECOND time):** `testing-anti-patterns` (eval-list line 182, ✅ score 4) was CONSOLIDATED INTO `test-driven-development` on 2025-12-18 (commit `718ec45d3358` — "Integrate testing-anti-patterns into test-driven-development"). Standalone path NO LONGER EXISTS at obra/superpowers `main` OR §AF/§AM/§AU's SHA `f2cbfbef...`. The proactive AB-036 follow-up check (per AB-036 doctrine codified at §AM adoption) PREDICTED THIS EXACT EVENT — flagged testing-anti-patterns as a consolidation-check candidate, and the check found the predicted consolidation. **Strongest validation of the AB-036 doctrine in practice** — the proactive check identifies consolidation events on NEW skills at the SAME consolidation date as the original AB-036 trigger (2025-12-18). **AB-036 doctrine refinement (NEW):** the 2025-12-18 obra/superpowers commit consolidated MULTIPLE skills simultaneously; future quarterly reviews should grep eval-list for ALL obra candidates whose paths predate 2025-12-18 + grep upstream for consolidation events on that date or later. **`test-driven-development` (the testing-anti-patterns consolidation target)** is itself an UN-adopted sister of §AM (per §AM SKILL.md L179 + L287) and now also the testing-anti-patterns consolidation successor. Recommended for next-session adoption as §AV — path is `skills/test-driven-development/` containing SKILL.md 9867 bytes + `testing-anti-patterns.md` 8251 bytes as a consolidated component file (SAME pattern as systematic-debugging containing root-cause-tracing.md). **Obra cluster EXTENSION (FIRST 3-skill obra cluster):** §AF + §AM + §AU all at shared SHA `f2cbfbef...` — obra cluster grows 2→3 skills (NEW SIZE CLASS: 3-skill cluster, distinct from prior sizes 2 / 5 / 7). **6-of-6 vendor clusters now span 6 distinct cluster sizes** (5 / 7 / 3 / 2 / 2 / 2) — empirical validation extends to a new size class. **§AF + §AM gate entries BACKPATCHED** per §4.5.3 sister-reference status maintenance rule. **§AF + §AM Ordering basis backpatch:** obra cluster sequence §AF `1bbde5c` (2026-05-16) → §AM (2026-05-20 earlier today) → §AU (2026-05-20 later today). Measured runtime context cost via tiktoken cl100k_base: SKILL.md body **1469 tokens / 6029 bytes** post-frontmatter-strip per §2D (full-file 1513t / 6314b; body sha256 `6385a196f86e3fe6...`). **Worst-case archive 1513 tokens** — well under §3B 5K-preferred cap; SECOND-smallest Tier-1 single-file bundle after §F ask-questions (884t). Score sum 30 (passes §4.4.4 ≥28 threshold). Author Jesse Vincent (obra), community per AGENTS.md §4.4.1 known-community list. **Framework lifts:** (1) **AB-036 doctrine validated for SECOND time** — proactive consolidation-check predicted right event class on new skill. (2) **AB-036 doctrine refinement** — multi-skill same-date consolidations are a known pattern (obra 2025-12-18 commit consolidated 2 skills simultaneously); flagged for 2026-Q3 quarterly codification. (3) **Obra cluster extension to 3-skill** — new cluster size class (3-skill) joins the framework's 6 vendor clusters (sizes 2/3/5/7 now represented). (4) **PR-review-receiving methodology layer** — §AU complements §AR/§AT (execution layer) for the full receive-and-act-on-feedback workflow. **Direct project fit for forge-works:** §AU pairs naturally with §AR coderabbitai/autofix + §AT openai/gh-address-comments + §H differential-review in the PR-review-receiving workflow. **Vendor concentration AFTER this adoption: 7 ToB (LARGEST at 7/23 = 30.4%) + 5 hamelsmu (21.7%) + 4 Anthropic + 3 obra (NEW SECOND-place tier; grows from 2 to 3) + 2 CodeRabbit + 2 OpenAI = 23 total** — obra grows 2→3, becoming SECOND-LARGEST vendor after ToB (overtaking hamelsmu's 5/23 = 21.7%... wait actually hamelsmu at 5 still beats obra at 3; let me recompute: ToB 7 > hamelsmu 5 > Anthropic 4 > obra 3 > CodeRabbit 2 = OpenAI 2). ToB stays LARGEST at 30.4% (down from 31.8%) via §AU's obra-diversification effect; §J R3-B-2 ToB cluster gate remains INACTIVE under strict `>50%` rule. Adoption budget post-landing: **23 / 30** (per §4.4.4 soft cap — 77% of the way; 7 slots remaining). **Loader smoke test:** scanner returns 23 skills, 0 errors. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AU row updated at adoption time (was at line 183 in original pool); AB-036 candidate-resolution event #2 filed inline at line 182 (`testing-anti-patterns` Notes column); budget ledger 22/30→23/30 with obra-cluster-extension milestone + 6-of-6 vendor clusters across 6 size classes note; Counts 22→23; §AF + §AM rows sister-reference status backpatched.
- **Twenty-second vendored skill: `openai/gh-address-comments` — 2nd OpenAI-vendor adoption + FIRST OpenAI cluster (§T + §AT at shared SHA `c25113bf...`, SIXTH cluster overall) + ELEVENTH consecutive Tier 2 estimate-confirm (eval-list pre-adoption Tier-2 HELD; N=14 staleness datapoints: 11 corrections + 3 deferrals) + NEW doctrinal observation: OpenAI's `agents/` dir contains plugin-marketplace UI metadata, NOT Claude sub-agent definitions + 6-of-6 vendor clusters now empirically validated (100% cluster-co-adoption coverage)** — adopted via the AGENTS.md §4.4.3 7-step process. Vendored at `.skills/openai/gh-address-comments/` (5-file bundle: SKILL.md 282t [TINIEST in framework, 224t body] + agents/openai.yaml [62t OpenAI plugin-marketplace UI metadata, NOT a Claude sub-agent definition] + 2 assets/_.{svg,png} [937t branding only] + scripts/fetch_comments.py [1549t `gh api graphql` wrapper; R2-2 clean: NO model-client invocations] + LICENSE.txt 2270t Apache 2.0) from `github.com/openai/skills` @ SHA `c25113bf4c64c8dba6bfe61acf06051d79aa43f6` — **SAME SHA as §T security-threat-model** (cluster co-adoption verified). Audited 2026-05-20: ✅ Tier-staleness audit (new §4.4.3 step 2) PASSED — Step 3 directs file-EDIT on existing source files; NO `allowed-tools` frontmatter; gh-CLI shell-execute subsumes network (same as §H pattern); NO `Task`/cross-agent surface. **Tier 2 shell-execute+repo-write** — matches §H/§AQ/§AS pattern. **Read-only-on-GitHub (NOT external-write — distinguishes from §AR):** §AT READS PR comments via `gh api graphql` + EDITS local files; user handles commit/push per AGENTS.md §3.3. **Distinct from §AR coderabbitai/autofix:** §AT is GENERIC (any commenter); §AR is CodeRabbit-specific. Hybrid pairing pattern: §AT for human-reviewer comments + §AR for CodeRabbit-bot comments in same PR. **NEW doctrinal observation — `agents/` directory disambiguation:** OpenAI's `agents/` dir contains plugin-marketplace UI metadata — NOT Claude sub-agent definitions per §2E condition (b). DO NOT CONFUSE with Claude's `agents/_.md`sub-agent definitions (§D, §AP). Future audits MUST inspect file content. Flagged for §4.4.5 quarterly codification. **OpenAI cluster (FIRST):** §T + §AT share repo + SHA`c25113bf...`. **6-of-6 vendor cluster coverage** — all 6 multi-skill vendors form clusters where SHAs align (hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2 / Anthropic 2 / OpenAI 2 = 100% coverage). Strongest evidence yet for 2026-Q3 normative elevation. **§T gate entry BACKPATCHED** per §4.5.3 sister-reference status maintenance rule. **User-clarification step required** per SKILL.md L17-18. SKILL.md body **224 tokens / 991 bytes** post-frontmatter-strip; body sha256 `760c50276d5ad1ec...`. **Worst-case archive 5100 tokens** — slightly over §3B 5K-preferred cap (driven by LICENSE + script). Score sum 28. **Framework lifts:** (1) FIRST OpenAI cluster — completes 6-of-6 vendor coverage. (2) `agents/`directory disambiguation doctrine — flagged for codification. (3) GENERIC PR-comment-handling skill — complements §AR's CodeRabbit-specific autofix. **Vendor concentration: 7 ToB (LARGEST at 7/22 = 31.8%) + 5 hamelsmu + 4 Anthropic + 2 obra + 2 CodeRabbit + 2 OpenAI = 22 total** — OpenAI grows 1→2; ToB drops 33.3%→31.8%. Adoption budget: **22 / 30** (73% of soft cap; 8 slots remaining). **Loader smoke test:** 22 skills loaded, 0 errors. **Decision Record additions (in`research/agents/evaluation_list.md`, gitignored):\*\* §AT row updated; budget ledger 21/30→22/30 with OpenAI-cluster milestone + 6-of-6 cluster coverage note; Counts 21→22; §T row sister-reference status backpatched.
- **Twenty-first vendored skill: `anthropics/webapp-testing` — 4th Anthropic-first-party adoption (after §D + §A + §AQ) + FIRST Anthropic cluster (§AQ + §AS at shared SHA `b9e19e6f...`, FIFTH cluster overall after hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2) + TENTH Tier 1→2 correction at adoption review per new §4.4.3 step 2 audit (N=13 Tier-staleness datapoints: 10 corrections + 3 deferrals from 6 vendors) + FIRST framework adoption with Playwright browser-automation surface + FIRST framework adoption with `shell=True` subprocess script + direct project fit for forge-works Next.js+vitest frontend** — adopted via the AGENTS.md §4.4.3 7-step process with new §4.4.3 step 2 Tier-staleness audit checklist applied. Vendored at `.skills/anthropics/webapp-testing/` (5-file bundle: SKILL.md + 3 examples/\*.py [console_logging.py 254t, element_discovery.py 361t, static_html_automation.py 230t — pure Playwright-API pattern documentation] + 1 scripts/with_server.py [813t — server lifecycle manager with `subprocess.Popen(shell=True)` for compound commands; R2-2 audit clean: NO model-client invocations] + LICENSE.txt byte-identical to §AQ's at the shared SHA) from `github.com/anthropics/skills` @ SHA `b9e19e6f44773509fbdd7001d77ff41a49a486c1` — **SAME SHA as §AQ mcp-builder** (cluster co-adoption; latest at-path commit for `skills/webapp-testing/` happens to equal §AQ's audit SHA). Audited 2026-05-20: ✅ Tier-staleness audit checklist (new §4.4.3 step 2) PASSED — (i) Phase 1 file-write directives ("write native Python Playwright scripts" L9-10); (ii) NO `allowed-tools` frontmatter (host default tool envelope, same pattern as §AM/§AQ); (iii) NO `commands/` dir; (iv) NO network egress in SKILL.md body — Playwright launches LOCAL chromium in headless mode to test LOCAL webapp at `localhost:<port>`; (v) NO `Task`/cross-agent surface. **Tier 2 shell-execute+repo-write** — matches §AQ/§D pattern. **Bundled-script audit:** `scripts/with_server.py` uses `subprocess.Popen(..., shell=True, ...)` at L68-71 — FIRST framework adoption with vendored `shell=True` subprocess script; security relies on user authorization at Phase-B gate (user explicitly approves the `--server` command before Bash tool runs the script). Per §2E "Bundled-script same-model self-invocation" clause: NON-APPLICABLE (no model-client invocations — pure server-lifecycle management). Flagged at §4.4.5 quarterly for codification of the shell=True doctrine. **Playwright browser-automation surface** — FIRST framework adoption with explicit Playwright (`playwright.sync_api`) usage. AB-034 (build-review-interface) was DEFERRED partly over Playwright surface (per AB-034 trigger (c)); **§AS adopts the surface in a different application context** — §AS uses Playwright for TESTING existing webapps (local headless chromium → assertion-based test scripts), while AB-034 would have used Playwright for VERIFYING agent-built HTML annotation interfaces (Playwright as verification step in a file-CREATE-heavy workflow). **§AS does NOT trigger AB-034 rescore** because application contexts differ. AB-034 status remains DEFERRED-CONFIRMED post-§AS adoption. **Anthropic cluster (FIRST):** §AQ + §AS share repo `anthropics/skills` + SHA `b9e19e6f...` — FIRST Anthropic cluster in framework. §D + §A remain solo Anthropic adoptions at distinct SHAs (`f458cee31...` + `6a5bb069...`) and are NOT part of the cluster. **§AQ gate entry BACKPATCHED in this commit** per §4.5.3 sister-reference status maintenance rule (codified at §AK): §AQ was previously solo at the SHA; now has §AS as cluster sibling at shared SHA. **§AQ Ordering basis backpatch (per §4.5.5 later-sibling rule):** Anthropic cluster sequence §AQ (2026-05-20, earlier today) → §AS (2026-05-20, later today). **Cluster-co-adoption pattern empirical validation EXTENDED:** with 5 clusters now (hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2 / Anthropic 2), the §4.4.5 cluster-co-adoption pattern is validated across 5 vendor profiles + 5 cluster sizes + 4 Tier-mix shapes (hamelsmu all-Tier-1 / ToB mixed / obra Tier-1+Tier-1 / CodeRabbit all-Tier-3 / Anthropic Tier-2+Tier-2). VERY STRONG case for 2026-Q3 normative elevation of the cluster-co-adoption rule from provisional to normative. **Direct project fit for forge-works:** frontend is Next.js + vitest (per `src/frontend/`); §AS directly applies to E2E testing the frontend; pairs with `pnpm test:coverage` workflow (existing CI in `ci.yml`). Phase 2 frontend coverage debt is the canonical use case. Measured runtime context cost via tiktoken cl100k_base: SKILL.md body **832 tokens / 3626 bytes** post-frontmatter-strip per §2D (full-file 881t / 3913b; body sha256 `830bd54146bc08d4...`). **Worst-case archive 2539 tokens** — SMALLEST Tier-2 bundle in framework; well under §3B 5K-preferred cap. Score sum 30 (passes §4.4.4 ≥28 threshold; goal=4 align=5 provenance=5 scope=4 maint=4 context=4 replace=4). Author Anthropic, first-party. **Framework lifts:** (1) **FIRST Anthropic cluster** — extends cluster-co-adoption pattern to 5 vendors. (2) **FIRST Playwright surface** — established as Tier-2 shell-execute+repo-write for local-only browser testing. (3) **FIRST `shell=True` subprocess script** — flagged for quarterly review codification. (4) **Local-only scoping doctrine** — §AS explicitly scopes Playwright to `localhost:<port>`; production-URL testing is OUT-OF-SCOPE (would be Tier 3 external-state inspection). **Vendor concentration AFTER this adoption: 7 ToB (LARGEST at 7/21 = 33.3%) + 5 hamelsmu (23.8%) + 4 Anthropic (19.0%) + 2 obra + 2 CodeRabbit + 1 OpenAI = 21 total** — Anthropic grows 3→4; ToB stays LARGEST but drops from 35.0% to 33.3% via §AS's Anthropic-diversification effect; §J R3-B-2 ToB cluster gate remains INACTIVE under strict `>50%` rule. Adoption budget post-landing: **21 / 30** (per §4.4.4 soft cap — 70% of the way to the cap). **Loader smoke test:** scanner returns 21 skills, 0 errors. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AS row updated at adoption time (was at line 90 in original eval-list pool); budget ledger 20/30→21/30 with Anthropic-cluster milestone note; Counts 20→21; §AQ row sister-reference status backpatched.
- **Twentieth vendored skill: `coderabbitai/autofix` — 2nd CodeRabbit-vendor adoption + FIRST Tier-3-only cluster in framework (§V + §AR at shared SHA `a81eb76a...`, FOURTH cluster after hamelsmu 5-skill / ToB 7-skill / obra 2-skill) + SECOND Tier 3 adoption in framework + FIRST framework adoption to apply Tier-3 flavor-(ii) external-write canonical prompt scaffold (pre-authored 2026-05-18 per Codex §V Round-2 Section A Finding R2-A-2) + NEW triple-compound tool-scope `shell-execute+repo-write+external-write` (loader enum extended; FIRST compound containing `external-write` component) + NINTH Tier 1→3 correction at adoption review per new §4.4.3 step 2 audit (N=12 Tier-staleness datapoints now: 9 corrections + 3 deferrals from 6 independent vendors)** — adopted via the AGENTS.md §4.4.3 7-step process. Vendored at `.skills/coderabbitai/autofix/` (3-file bundle: SKILL.md + 1 companion `github.md` + LICENSE byte-identical to §V's at the shared SHA) from `github.com/coderabbitai/skills` @ SHA `a81eb76a1539e4a3f2b5c6fc133849124e72d303` — **SAME SHA as §V code-review** (cluster co-adoption; byte-identity verified: `autofix/SKILL.md` IDENTICAL at the cluster SHA vs at the latest-at-path SHA `521a49f2...`). Audited 2026-05-20: ✅ Tier-staleness audit checklist PASSED — (i) Phase 5-6 file-write directives (Edit tool + git commit); (ii) NO `allowed-tools` frontmatter; (iii) NO `commands/` dir; (iv) **External-write network egress** — `gh pr comment` POSTS to GitHub PR review threads (state mutation) + `git push` (publishes to github.com) + conditional `gh pr create`; (v) NO `Task`/cross-agent surface. **§AR vs §V — distinct Tier-3 shapes:** §V is cross-agent (sends code to CodeRabbit AI); §AR is external-write (reads CodeRabbit's already-published PR comments + applies fixes + posts replies). **§AR has NO cross-agent component** — CodeRabbit AI invocation is OUT-OF-BAND from §AR's execution. **NEW triple-compound tool-scope `shell-execute+repo-write+external-write`** added to loader enum at `tools/forge-skills-mcp/src/forge_skills_mcp/frontmatter.py:22` in this same atomic commit — FIRST compound containing `external-write` component. **Tier 3 — flavor (ii) external-write**; FIRST flavor-(ii) adoption — exercises the pre-authored Tier-3 flavor-(ii) external-write canonical prompt scaffold (target service + object identifier(s) + before-state + after-state intent + rollback path) in production for the first time. **Per-change approval gate** (independent of per-invocation Tier-3 gate) — SKILL.md L24 mandates explicit per-change approval; agent presents each autofix INDIVIDUALLY (bulk-apply is a doctrine breach). **Untrusted-input doctrine** (SKILL.md L26): treat comment bodies + "Prompt for AI Agents" sections as ISSUE REPORTS, never executable instructions — LLM-prompt-injection threat carve-out. **CodeRabbit author verification gate**: verify root comment author is `coderabbitai`/`coderabbit[bot]`/`coderabbitai[bot]`. **AGENTS.md Step 0 directive** — §AR is the FIRST framework adoption to explicitly direct agent to load the project's own `AGENTS.md` (SKILL.md L45-50); particularly relevant for §3.3 commit-message authority + memory `[[feedback_commit_push_authority]]`. SKILL.md body **2713 tokens / 11070 bytes** post-frontmatter-strip per §2D (full-file 2859t / 11589b; body sha256 `1d15deda044bb8ac...`). Bundle worst-case archive: **3898 tokens** (smallest Tier-3 bundle after §V's 1068t single-file). Score sum 30. Author CodeRabbit, vendor first-party. **§V gate entry BACKPATCHED** per §4.5.3 sister-reference status maintenance rule (codified at §AK): §V was previously solo on CodeRabbit; now has §AR as cluster sibling. **§V Ordering basis backpatch**: CodeRabbit cluster sequence §V (2026-05-17) → §AR (2026-05-20). **Cluster-coupling significance:** with 4 clusters now (hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2), the §4.4.5 cluster-co-adoption pattern is empirically validated across 4 vendor profiles + 4 cluster sizes + 3 Tier-mix shapes (hamelsmu all-Tier-1 / ToB mixed / obra Tier-1+Tier-1 / CodeRabbit all-Tier-3); VERY STRONG case for 2026-Q3 normative elevation. **Vendor concentration AFTER this adoption: 7 ToB (LARGEST at 7/20 = 35.0%) + 5 hamelsmu + 3 Anthropic + 2 obra + 2 CodeRabbit + 1 OpenAI = 20 total**. Adoption budget post-landing: **20 / 30** (TWO-THIRDS of the soft cap). **Framework lifts:** (1) NEW triple-compound `shell-execute+repo-write+external-write` (loader extended); (2) FIRST Tier-3-only cluster (establishes clusters can be tier-homogeneous at any tier); (3) FIRST flavor-(ii) external-write Tier-3 in production (validates the pre-authored scaffold); (4) Distinct Tier-3 shapes within same cluster — §V (cross-agent) + §AR (external-write) at SHARED SHA demonstrates cluster siblings can have DISTINCT Tier-3 shapes; (5) FIRST framework adoption to explicitly direct AGENTS.md loading in skill workflow. **Loader smoke test:** scanner returns 20 skills, 0 errors. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AR row updated at adoption time (was line 157 in original pool); budget ledger 19/30→20/30 with CodeRabbit-cluster milestone; Counts 19→20; §V row sister-reference status backpatched.
- **Nineteenth vendored skill: `anthropics/mcp-builder` — 3rd Anthropic-first-party adoption (after §D skill-creator + §A doc-coauthoring) + EIGHTH Tier 1→2 correction at adoption review (N=11 Tier-staleness datapoints now: 8 corrections + 3 deferrals from 6 independent vendors) + FIRST framework adoption with a vendored Python script that DIRECTLY imports the `anthropic` SDK (distinct from §D's `claude --print` CLI subprocess pattern) + direct meta-applicability to the project's `tools/forge-skills-mcp/` loader** — adopted via the AGENTS.md §4.4.3 7-step process with the new §4.4.3 step 2 Tier-staleness audit checklist applied. Vendored at `.skills/anthropics/mcp-builder/` (9-file bundle: SKILL.md + 4 reference/_.md guides [evaluation.md 4938t, mcp_best_practices.md 1593t, node_mcp_server.md 6581t, python_mcp_server.md 5524t] + 4 scripts/_ files [connections.py 1051t, evaluation.py 2865t, example_evaluation.xml 326t, requirements.txt 18t] + LICENSE.txt byte-identical Anthropic Apache 2.0) from `github.com/anthropics/skills` @ SHA `b9e19e6f44773509fbdd7001d77ff41a49a486c1` (latest at-path commit touching `skills/mcp-builder/` as of 2026-04-20). Audited 2026-05-20: ✅ NO encoded content; ✅ no transitive `load skill X` refs (zero direct cross-references to other adopted skills); ✅ no `claude --` CLI subprocess paths in SKILL.md; ✅ Tier-staleness audit checklist (new §4.4.3 step 2) PASSED with full surface enumeration — (i) Phase 2 file-CREATE directives (MCP server source files) → Tier 2 confirmed; (ii) NO `allowed-tools` frontmatter declaration (same pattern as §AM — host uses default tool envelope); (iii) NO `commands/` dir; (iv) documentation-fetch network egress is host-mediated via WebFetch on `modelcontextprotocol.io` (read-only public docs, NOT skill-declared); (v) NO `Task` tool / cross-agent surface declared. **Tier 2 shell-execute+repo-write** — matches §D anthropics/skill-creator's tool-scope pattern. **Bundled-script analysis (NEW framework pattern surfaced):** `scripts/evaluation.py` (12579 bytes / 2865 tokens) DIRECTLY imports `from anthropic import Anthropic` + calls `client.messages.create` with configurable Claude model (default `claude-3-7-sonnet-20250219`) — **FIRST framework adoption with a vendored Python script using the direct `anthropic` SDK import pattern** (distinct from §D anthropics/skill-creator's 3 scripts which use `claude --print` CLI subprocess wrappers). **§2E "Bundled-script same-model self-invocation" clause is NON-APPLICABLE at agent-run time** because SKILL.md does NOT direct the agent to execute the script — Phase 4 "Create Evaluations" directs the agent to AUTHOR a `<evaluation>` XML file (per `reference/evaluation.md`), and the USER subsequently runs the harness against the built MCP server. Same user-utility pattern as §AM's `find-polluter.sh`, §AP's `merge_sarif.py`, §AN's `sarif_helpers.py`. **R2-2 audit documented for provenance:** if SKILL.md WERE to direct agent execution, all 3 §2E cross-agent normative-predicate conditions would FAIL (execution boundary external, child grants not provably subset, grant inheritance opaque) → would be cross-agent Tier 3 → would require intra-skill Tier-3 escalation gate (§D-pattern). **`scripts/connections.py` R2-2 audit clean:** pure Python helper, NO subprocess / NO anthropic / NO SDK tokens. **Doctrinal sharpening (NEW):** §AQ adoption establishes that the §2E "Bundled-script same-model self-invocation" clause applies to BOTH (a) `claude --` subprocess invocations AND (b) direct `anthropic` SDK imports — both are model-client invocation surfaces. NON-APPLICABILITY is driven by SKILL.md not directing agent execution, NOT by absence of model-client tokens. This sharpening will be codified at next §4.4.5 quarterly review when the §2E clause is amended to enumerate both patterns explicitly. Measured runtime context cost via tiktoken cl100k_base: SKILL.md body **1847 tokens / 8735 bytes** post-frontmatter-strip per §2D canonical algorithm (full-file 1922t / 9092b; body sha256 `6eaabfcf59c08178...`). **Worst-case archive 24,818 tokens** — SECOND-largest single-skill bundle in framework after §AO codeql's 27,298; 8 companion files load-on-demand per §3B (the 4 reference guides are referenced via explicit "Load [...]" directives at Phase 1.2-1.3 + Phase 4 — agent loads them WHEN entering the corresponding phase, not at SKILL.md ingest). **Cross-cluster Anthropic vendor (NOT a cluster):** §D + §A + §AQ all share vendor `anthropics` + repo `anthropics/skills` BUT pinned at 3 DIFFERENT SHAs (§D `f458cee31...` 2026-05-09 pre-restructure layout; §A `6a5bb069...` 2026-05-17 post-restructure; §AQ `b9e19e6f...` 2026-04-20 latest at-path for mcp-builder). Per §4.4.5 cluster-co-adoption note: cluster requires SHARED SHA, not just shared repo → **NO Anthropic cluster** in the optimization sense; each Anthropic skill has independent quarterly source-SHA review. **Meta-applicability to forge-works:** §AQ directly applies to authoring + maintaining the `tools/forge-skills-mcp/` loader (the loader IS an MCP server we author). §AQ Phase 4 evaluation-authoring workflow can produce eval XML files against the forge-skills-mcp loader for user-run harness validation. Score sum 30 (passes §4.4.4 ≥28 threshold; goal=5 align=5 provenance=5 scope=4 maint=4 context=3 replace=4). Author Anthropic, first-party. **Framework patterns codified in this adoption:** (1) **§2E clause sharpening flagged for codification** — applies to both `claude --` subprocess + direct `anthropic` SDK imports (will be amended at next §4.4.5 quarterly). (2) **User-utility-vs-agent-directed script distinction** — when SKILL.md does NOT direct agent execution, model-client-token-bearing scripts are user utilities (§AM/§AP/§AN/§AQ pattern); when SKILL.md DOES direct execution, intra-skill Tier-3 escalation gate applies (§D pattern). **AGENTS.md §4.5.1 trigger row** added: positive triggers cover "build an MCP server for X API", "create an MCP server that wraps Y", "write a new MCP server in Python", "design tool schemas for our internal HR API", "scaffold a TypeScript MCP server"; cluster boundary with §D anthropics/skill-creator (create-an-MCP-server vs create-a-skill — distinct artifact classes); 7 negative-trigger classes. **AGENTS.md §4.5.3 §AQ Policy Gates Index** entry added with 5 gates: Phase-A/Phase-B per Tier 2; File-CREATE per §J R3-A-3; documentation-fetch host-mediated network; bundled-script same-model self-invocation NON-APPLICABLE analysis with R2-2 provenance documentation; §2E doctrinal sharpening surfaced. **Vendor concentration AFTER this adoption: 7 ToB (LARGEST at 7/19 = 36.8%) + 5 hamelsmu (26.3%) + 3 Anthropic (15.8%) + 2 obra + 1 OpenAI + 1 CodeRabbit = 19 total** — Anthropic grows from 2/18 (11.1%) to 3/19 (15.8%); ToB remains LARGEST at 36.8%. §J R3-B-2 ToB cluster gate remains INACTIVE under strict `>50%` rule. Adoption budget post-landing: **19 / 30** (per §4.4.4 soft cap). **Loader smoke test:** scanner returns 19 skills, 0 errors. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AQ full record updated at adoption time; budget ledger 18/30→19/30 with Anthropic-grows-to-3 note; Counts block 18→19 active adopted.
- **Three simultaneous vendored skills: `trailofbits/sarif-parsing` (§AN) + `trailofbits/codeql` (§AO) + `trailofbits/semgrep` (§AP) — FIRST multi-skill plugin adoption in the framework + FIFTH/SIXTH/SEVENTH Tier 1→2 corrections at adoption review (N=10 Tier-staleness datapoints now: 7 corrections + 3 deferrals from 5 independent vendors) + AB-037 candidate-MISMATCH resolution doctrine + FIRST 7-skill ToB cluster (LARGEST cluster in framework)** — adopted via the AGENTS.md §4.4.3 7-step process applied 3× simultaneously, landing in ONE atomic commit. **Upstream is the `trailofbits/skills/plugins/static-analysis/` multi-skill plugin**: 3 distinct SKILL.md files (sarif-parsing, codeql, semgrep) + 1 bundled sub-agent (semgrep-scanner) + 1 vendored Python script (merge*sarif.py) + 21 companion files. **AB-037 candidate-resolution event (filed 2026-05-20 in eval-list at candidate-table row line 145):** eval-list candidate `static-analysis` was a PLUGIN-LEVEL aggregator name (sum 31, Tier 1 estimated, ✅); upstream is materially 3 distinct skills. **AB-037 establishes the SECOND candidate-resolution sub-pattern after AB-036** (upstream-consolidation): **candidate-MISMATCH** where eval-list row maps to multiple upstream skills. Resolved by adopting all 3 as separate framework entries §AN/§AO/§AP at the same SHA `a56045e9...` in ONE atomic commit. Score-5 + ✅ verdict transfers across the 3 successors; Tier estimate UPDATED Tier 1→2 for all 3 per new §4.4.3 step 2 audit. Vendored at `.skills/trailofbits/{sarif-parsing,codeql,semgrep}/` (3 separate top-level slugs because the forge-skills loader scans `*/_/SKILL.md`at depth 2 only —`scanner.py:50`). **§AN sarif-parsing** (Tier 2 shell-execute): SKILL.md 3590t + `resources/{jq-queries.md, sarif_helpers.py}`— worst-case archive 7654t;`sarif_helpers.py` is a stdlib-only helper LIBRARY (imported, not invoked) per R2-2 audit clean. **§AO codeql** (Tier 2 shell-execute+repo-write): SKILL.md 3433t + 12 references/_.md (15871t) + 3 workflows/\_.md (8094t) — **worst-case archive 27,298 tokens, LARGEST single-skill bundle in framework**; 15 reference + workflow companion files load-on-demand per §3B. Multiple file CREATE operations (output directory, `codeql.db/`, `codeql-database.yml`, SARIF outputs, data extensions, diagnostics CSVs) → Tier 2 per §J R3-A-3. **§AP semgrep** (Tier 2 shell-execute+repo-write with intra-skill Tier-3 escalation on parallel sub-agent spawns via Task tool per §2E vendored-named carve-out condition (b)): SKILL.md 2198t + 3 references + 1 workflow + `scripts/merge_sarif.py` (R2-2 audit clean: subprocess.run calls for SARIF Multitool, NO model-client invocations) + `agents/semgrep-scanner.md` (vendored-named sub-agent at audited SHA with `tools: Bash(semgrep scan:*), Bash` — constrained Bash with allowlist + bare-Bash fallback for `wait`/`mkdir`) — worst-case archive 11,696t. **FIRST framework adoption to apply §2E condition (b) vendored-named carve-out for a NON-`claude --` sub-agent surface** (§D used the same condition for vendored scripts; §AP uses it for `agents/semgrep-scanner.md` invoked via host's `Task` tool); intra-skill Tier-3 escalation gate at parallel-spawn ACTION (multiple Task calls in one message) parallels §D's `claude --` script escalation. **Tier-staleness audit checklist gap surfaced + backpatched in this commit:** the new §4.4.3 step 2 signature (v) "cross-agent invocation directives" was originally written for PROMPT-LEVEL invocations ("ask GPT-4"); §AP demonstrates the TOOL-LEVEL pattern (`Task` declared in `allowed-tools` frontmatter). Per §AGENT_SKILLS.md §2E "Cross-agent normative predicate" (lines ~406-426): all 3 conditions FAIL the cross-agent classification → §AP qualifies for same-model bounded-subagent carve-out → Tier 2 (not Tier 3). The §AP Notes include the full predicate analysis. **FIRST 7-skill ToB cluster (LARGEST in framework, overtaking hamelsmu 5-skill):** ToB grows 4→7 skills at shared SHA `a56045e9...`; per §4.4.5 cluster-co-adoption note, quarterly source-SHA review for ToB MAY use one shared upstream check covering all 7 skills, each Decision Record independently attested. With THREE clusters now in framework (hamelsmu 5-skill, obra 2-skill, ToB 7-skill — third datapoint), the cluster-co-adoption pattern is empirically validated across 3 different vendor profiles; STRONG case for 2026-Q3 normative elevation per the §AL gate observation + the §AM gate observation. **Static-analysis sub-cluster relationships (within the 3-skill plugin):** §AN is OUTPUT-PROCESSING; §AO + §AP are SCAN-EXECUTION. Canonical pipeline chains: (i) `§AO codeql scan → §AN sarif-parsing` (process codeql output); (ii) `§AP semgrep scan → §AN sarif-parsing` (process semgrep output); (iii) **multi-tool aggregation chain** `§AO + §AP → §AN` (aggregate findings via merge_sarif.py + sarif-tools). FIFTH adopted-skill-to-adopted-skill internal reference cluster in framework (after §AJ→§AH, §AK→§AH, §AL→§AJ, §AM→§AF). **Vendor concentration AFTER this 3-way adoption: 7 ToB (LARGEST at 7/18 = 38.9%) + 5 hamelsmu (27.8%) + 2 Anthropic + 2 obra + 1 OpenAI + 1 CodeRabbit** — ToB regains LARGEST-vendor position (previously held until §AL adoption); §J R3-B-2 ToB cluster gate remains INACTIVE under strict `>50%` rule (7/18 = 38.9% < 50%). Adoption budget post-landing: **18 / 30** (per §4.4.4 soft cap). **Framework lifts in this commit (3 codified at gate level + 1 doctrine gap surfaced):** (1) **AB-037 candidate-MISMATCH doctrine** — second candidate-resolution sub-pattern after AB-036 upstream-consolidation; future §4.4.5 quarterly reviews should grep eval-list for candidates whose names are plugin-level aggregators mapping to multiple upstream skills. (2) **Multi-skill plugin adoption pattern** — vendor at separate top-level slugs (loader scans depth 2 only); preserve provenance via `source-canonical` URL pointing to the plugin path; document the plugin-level bundle structure in each skill's Notes for cross-reference. (3) **§AP first framework adoption to apply §2E condition (b) vendored-named carve-out for non-`claude --` sub-agent surface** — establishes that the carve-out applies to host's `Task` tool sub-agent spawns (not just `claude --` subprocess paths). (4) **Tier-staleness audit checklist (v) gap surfaced** — the prompt-level signature missed tool-level `Task` declarations; backpatch documented inline in §AN Notes (and §4.4.3 step 2 checklist will be updated at next §4.4.5 quarterly review to add "(v-bis) Task tool in `allowed-tools` frontmatter"). **Cluster-co-adoption pattern empirical validation:** with 3 clusters (hamelsmu 5-skill + obra 2-skill + ToB 7-skill), the §4.4.5 cluster-co-adoption note is now STRONGLY evidenced — recommend elevation from provisional to normative at 2026-Q3 quarterly review. **Loader smoke test:** scanner returns 18 skills, 0 errors. **Adoption hardening:** no sliced Codex feedback loop (solo-developer scope; per the §4.4.3 step 6 doctrine the Codex review can be deferred to a follow-up commit when scope is genuinely multi-skill; the §4.4.3 step 2 Tier-staleness audit + the §J R3-A-3 trichotomy verification provide the doctrine-validity gate inline). **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AN/§AO/§AP full records NEWLY CREATED at adoption time (5th/6th/7th eval-list entries created at adoption time after §AJ/§AK/§AL/§AM); budget ledger updated to 18/30 with 7-skill ToB cluster milestone note + ToB-regains-LARGEST note; AB-037 filed in candidate-table row line 145 (`static-analysis` Notes column).
- **Fifteenth vendored skill: `obra/systematic-debugging` — second obra-vendor adoption + FIRST obra cluster co-adoption (§AF + §AM at shared SHA `f2cbfbef...`, SECOND cluster in framework after hamelsmu's 5-skill cluster) + FIRST adoption to surface upstream-consolidation candidate-resolution event (filed as AB-036) + SEVENTH adoption to maintain Tier 1 estimate (first since the inline-output-stays-Tier-1 observation was elevated to normative at 2026-Q2 §4.4.5)** — adopted via the AGENTS.md §4.4.3 7-step process with NEW §4.4.3 step 2 Tier-staleness audit checklist applied. **Upstream-consolidation candidate-resolution event (AB-036, filed in eval-list 2026-05-20):** the eval-list candidate `root-cause-tracing` (line 181, ✅ score 5) was consolidated INTO `systematic-debugging` upstream on 2025-12-18 (commit `5845b527` — "Consolidate debugging techniques into systematic-debugging skill"); the original eval-list path no longer exists at upstream `obra/superpowers` `main`. The new §4.4.3 step 2 audit caught this at adoption time — methodology lives on at `systematic-debugging` with `root-cause-tracing.md` as one component. Adopted the canonical successor; AB-036 establishes the FIRST upstream-consolidation candidate-resolution doctrine. Vendored at `.skills/obra/systematic-debugging/` (12-file bundle: SKILL.md + 3 referenced .md companions [root-cause-tracing.md → SKILL.md L114, defense-in-depth.md → L283, condition-based-waiting.md → L284] + 1 referenced TS example [condition-based-waiting-example.ts] + 1 user-utility script [find-polluter.sh — bash bisection tool, audited per §4.4.3 step 1 R2-2 hardening: NO model-client invocations] + 4 QA pressure-test scaffolds [test-pressure-1/2/3.md, test-academic.md] + 1 meta-doc [CREATION-LOG.md] + LICENSE byte-identical) from `github.com/obra/superpowers` @ SHA `f2cbfbefebbfef77321e4c9abc9e949826bea9d7` — **SAME SHA as §AF; FIRST obra cluster co-adoption**. Audited 2026-05-20: ✅ Tier-staleness audit checklist (new §4.4.3 step 2) PASSES all 5 signatures — (i) NO file-write directives (grep `Save|Write the|Create a file|Output to|writes? to` returned ZERO); (ii) NO `allowed-tools` frontmatter (upstream YAML has only `name` + `description`); (iii) NO network egress; (iv) NO cross-agent invocation directives; (v) Embedded bash snippets at SKILL.md L90-106 are USER-FACING DIAGNOSTIC EXAMPLES (echo/security/codesign Layer-1-through-4 evidence-gathering), NOT agent-execution directives — same pattern as §J's bash recommendations. ✅ no encoded content; ✅ no transitive `load skill X` refs (3 sister cross-references all explicit + handled: `superpowers:test-driven-development` UN-adopted advisory ×2 + `superpowers:verification-before-completion` → §AF adopted ×1); ✅ no `claude -p` / SDK subprocess paths (find-polluter.sh has process spawns but NO model-client invocations); ✅ no bundled subagents (§2E carve-outs all NON-APPLICABLE). **Tier 1 read-only** (the 4-phase methodology produces inline transcript content — process guidance + Iron Law reminders + Red Flags catalog + Rationalizations table — per the inline-transcript-output Tier 1 normative rule codified 2026-05-20 at 2026-Q2 §4.4.5; SEVENTH adoption to maintain Tier 1 estimate after §J/§AG/§AH/§AJ/§AK/§AL). Measured runtime context cost via tiktoken cl100k_base: SKILL.md **body 2303 tokens / 9743 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 2330 tokens / 9884 bytes including upstream YAML frontmatter); body sha256 `24eaf14d2d4aa053efa03a219ab77c4d7d4ccee0a1900124d2940fe47a040cb5`. **Bundle worst-case archive: 9737 tokens** — exceeds §3B 5K-preferred cap but well under hard limits; QA pressure-test files + CREATION-LOG.md (~2772 tokens) are load-on-demand only; 5 core load-bearing files sum to ~6505 tokens. Score sum 31 (passes §4.4.4 ≥28 threshold; matches original eval-list `root-cause-tracing` row's score-5 + Tier-1 estimate cleanly per AB-036 candidate-resolution analysis). Author Jesse Vincent (obra), community per AGENTS.md §4.4.1 known-community list. Fit: 4-phase systematic debugging framework — directly applicable to normalizer DLQ failure analysis (Sprint E5.1c+), Sentry triage workflows, Flink 1.20 → 2.0 version-bump regression hunting, intermittent test failures (find-polluter.sh user-utility for test-pollution bisection), and the project's `/codex-review` feedback-loop methodology when sliced-loop convergence stalls (the 3-fix-failure escalation rule maps onto the "feedback-loop stop signal" doctrine from memory `[[feedback_loop_stop_signal]]`). **Framework patterns codified in this adoption (no §4.5.5 / §4.4.5 framework lifts this round — all §AM-specific gate doctrine; §4.4.3 step 2 Tier-staleness checklist proved out empirically with this adoption, the FIRST candidate to pass cleanly per the new checklist):** (1) **FIRST obra cluster co-adoption** — §AF + §AM at shared SHA `f2cbfbef...`; quarterly source-SHA review optimization per §4.4.5 cluster-co-adoption note (rule was elevated to normative-elevation-candidate at 2026-Q2; obra cluster is the SECOND empirical datapoint after hamelsmu 5-skill cluster — strong case for 2026-Q3 normative elevation). (2) **FOURTH adopted-skill-to-adopted-skill internal reference + FIRST same-repo + same-SHA cross-cluster reference** — §AM → §AF SUCCESSOR chain canonical when §AM Phase 4 Step 3 "Verify Fix" fires; codified at §4.5.3 §AM gate as the Phase 4 verify-completion transition. (3) **Iron-Law gate doctrine** — "NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST" codified at §4.5.3 §AM gate as runtime transcript-emission requirement. (4) **3-fix-failure escalation rule** — codified at §AM gate as hard architectural-discussion gate; maps onto memory `[[feedback_loop_stop_signal]]` doctrine. (5) **Red-flags self-rationalization gate** — 11 rationalization patterns from SKILL.md L215-228 codified at §AM gate as STOP-and-return-to-Phase-1 triggers. (6) **§AF sister-reference status backpatch (per §4.5.3 maintenance rule codified at §AK):** §AF gate entry BACKPATCHED in this commit to acknowledge §AM as cluster sibling at shared SHA. (7) **§AF Ordering basis backpatch (per §4.5.5 later-sibling rule):** obra cluster sequence §AF `1bbde5c` (2026-05-16 audit) → §AM `<TBD>` (2026-05-20 audit). **AGENTS.md §4.5.1 trigger row** added: positive triggers cover "debug this test failure", "why is X broken", "production API is down — systematic-debug this", "find the polluter test", "intermittent test failure — debug", "build/integration failure — root cause", "this exception only fires under load — diagnose"; also fires when agent catches self-rationalization patterns (Red Flags catalog). Negative-trigger list covers 8 adjacent classes (code review unrelated to specific failure, security threat-modeling without active bug, Python tooling config without debugging context, build/config setup absent failure, preventive code-quality without failure trigger, perf-optimization without failure, feature implementation, post-mortem authoring after fix shipped). **AGENTS.md §4.5.3 §AM Policy Gates Index** entry added with FIVE gates: Iron-Law transcript-emission requirement; 3-fix-failure escalation gate; Red-flags self-rationalization gate (11 patterns); §AM → §AF Phase 4 Step 3 transition gate; bundle metering acknowledgment (worst-case 9737 tokens). **Vendor concentration AFTER this adoption: 5 hamelsmu (LARGEST at 5/15 = 33.3%) + 4 Trail of Bits (4/15 = 26.7%) + 2 Anthropic + 2 obra (NEW second-smallest tier alongside Anthropic) + 1 OpenAI + 1 CodeRabbit** — hamelsmu drops from 35.7% to 33.3% via §AM's obra-diversification effect; **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule. Adoption budget post-landing: **15 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm + Python byte-compare from `# Systematic Debugging` heading (no companion-file divergence; LICENSE byte-identical to upstream `obra/superpowers/LICENSE` blob SHA `abf0390320aa14406af7a520b9b0739fdda9bf08`). **§AM Ordering basis backpatches:** per the §4.5.5 later-sibling backpatch rule, the §AM adoption commit ALSO updates §AF's Ordering basis line (the only prior obra adoption) to reflect the now-2-skill obra sequence. **AB-036 candidate-resolution doctrine (NEW framework pattern):** future §4.4.5 quarterly reviews should grep eval-list for candidates whose upstream paths have been consolidated, renamed, or removed; AB-036 establishes the canonical resolution chain (upstream-consolidation → check successor at canonical path → adopt successor with cross-reference to original eval-list candidate). The §AM adoption was hardened through inline §4.4.3 step 2 Tier-staleness audit (no sliced Codex feedback loop this round — single-file methodology with clean cluster pattern); the §4.4.5 step-2 enhancement proved out at first use. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AM full record NEWLY CREATED at line `<TBD>` (FOURTH eval-list entry created at adoption time after §AJ/§AK/§AL); budget ledger updated to 15/30 with obra-cluster milestone note + Anthropic-tied-with-obra-at-2 note; §AM Notes section with R3-A-3 trichotomy compliance + Iron-Law + 3-fix-escalation + Red-Flags gate details + AB-036 candidate-resolution narrative; §AF Ordering basis line BACKPATCHED to 2-skill obra sequence; §AF Notes SISTER-REFERENCE-STATUS BACKPATCHED per the §4.5.3 maintenance rule. AB-036 filed in candidate-table row line 181 (`root-cause-tracing` Notes column).
- **Fourteenth vendored skill: `hamelsmu/evals-skills/generate-synthetic-data` — fifth hamelsmu-vendor adoption + EXTENDS cluster from 4 to 5 skills (first 5-skill cluster co-adoption AND completes the full hamelsmu/evals-skills candidate set) + SIXTH adoption to maintain Tier 1 estimate (EXCEEDS N≥5 elevation threshold) + vendor-concentration milestone (hamelsmu becomes LARGEST vendor for first time)** — adopted via the AGENTS.md §4.4.3 7-step process. Vendored at `.skills/hamelsmu/generate-synthetic-data/` (single SKILL.md, no companions; LICENSE byte-identical to §AG/§AH/§AJ/§AK's verified `evals-skills/LICENSE`) from `github.com/hamelsmu/evals-skills` @ SHA `febdb335bd658a01f756b8b5b3364277a4fa6a4a` — **SAME SHA as §AG/§AH/§AJ/§AK; fifth and FINAL skill in upstream candidate set (cluster COMPLETE)**. Audited 2026-05-19: ✅ no encoded content; **THIRD adopted-skill-to-adopted-skill internal reference in the framework** (after §AJ→§AH at §AJ L131 and §AK→§AH at §AK L5) — §AJ SKILL.md L36 cites `generate-synthetic-data` which is ADOPTED as §AL; codified at §4.5.3 §AJ trace-sourcing rule backpatch as canonical §AJ → §AL PRECURSOR chain (inverse direction to the §AJ → §AH and §AK → §AH SUCCESSOR chains); ✅ no `claude -p` / SDK subprocess paths; ✅ no bundled subagents; ✅ no executable shell/python/node snippets vendored (the dimension/tuple/query templates at L25-39, L41-50, L52-66, L73-87 are INLINE markdown scaffolds, NOT execution directives; Step 6 instructs the USER to run queries through their pipeline, agent does NOT execute). **Tier 1** (MAINTAINED — SIXTH adoption to do so after §J/§AG/§AH/§AJ/§AK; **EXCEEDS the N≥5 elevation threshold** from §AG R1 B-R1-2 for the inline-output-stays-Tier-1 provisional observation — strong evidence for normative codification at next §4.4.5 quarterly review). Measured runtime context cost via tiktoken cl100k*base: SKILL.md **body 1104 tokens / 4985 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 1196 tokens / 5427 bytes including upstream YAML frontmatter); **SMALLEST single-file Tier 1 skill in the cluster** — ~36% smaller than §AG's 1696 body tokens. **§AL is the third adoption with a NEWLY-CREATED Decision Record** (§AJ first, §AK second; §AL third) — §AL was NOT in the original A-AI pre-scored pool (eval-list now spans A-AL = 38 records); all score axes assigned at adoption time. Score sum 29 (passes §4.4.4 ≥28 threshold; maint=3 inherited from cluster siblings; provenance=3 capped at axis max for fifth same-vendor adoption per §4.4.4). Author Hamel Husain, community per AGENTS.md §4.4.1 known-community list. Fit: LLM-pipeline synthetic-test-input generation via dimension-based tuple construction (the primary skill use case) + meta-applicability for stress-testing the project's own `/codex-review` doctrine templates via adversarial Codex prompts. **Two UN-adopted-target gate closures (NEW pattern via §AL adoption):** (a) §AG no-infrastructure path (SKILL.md L156-162 directed users to `generate-synthetic-data`) — gate CLOSED, §AG → §AL canonical chain now available; (b) §AJ 0-trace mode UN-adopted-target gate — gate CLOSED, §AJ → §AL canonical PRECURSOR chain replaces the inline-fallback path (legacy inline path preserved only when user explicitly declines §AL invocation). **Framework patterns codified in §AL gate** (no §4.5.5 / §4.4.5 framework lifts this round — all rules are §AL-specific gate doctrine, but they establish operational patterns and complete the §AJ trace-sourcing rule deterministic semantics): (1) **§AL → §AJ canonical PRECURSOR chain** — third adopted-skill-to-adopted-skill internal reference, INVERSE direction (§AL precedes §AJ; §AH precedes §AK and §AJ → §AH succeeds discover); (2) **§AJ re-entry semantics codified** (per Codex §AL R1 A-R1-1 — the prior "RE-ENTERS §AJ" wording was non-deterministic): trace_count := traces_generated_by*§AL binding; auto-resume without re-confirmation; resume marker transcript line `[§AJ resumed: trace_count=<N>; mode=<resolved-§AJ-mode>; source=§AL-bootstrap]`; low-count branch (N < 10 → §AJ pre-bootstrap with §AL expansion option OR user override); (3) **Dimension-discipline rules** (4 codified from upstream): failure-hypothesis-grounded dimensions; user-confirmed tuples required; two-step generation discipline; realism-filter required; (4) **Anti-pattern enforcement rules** (3 codified): domain-realism guard (legal/medical/structured domains); low-resource-language guard; realism-judgability requirement; (5) **Output-volume mode taxonomy** (parallel to §AJ trace-count + §AK sample-size): bootstrap (10-29) / standard (30-99) / saturation (≥100) per SKILL.md L93 target. **AGENTS.md §4.5.1 trigger row** added: positive-triggers cover "generate synthetic test inputs", "we have no production data — create test queries", "bootstrap an eval dataset", "stress-test our system" patterns + cluster boundary as 5-skill eval-methodology pipeline + canonical pipeline chains for new projects; negative-trigger list expanded to 11 illustrative classes with category-level guard prepended ("ONLY for dimension-based synthetic test-input generation for LLM pipeline EVALUATION"); per Codex §AL R1 A-R1-2 the security red-team / adversarial corpus exclusion is explicit. **AGENTS.md §4.5.3 §AL Policy Gates Index** entry added: NO policy gates beyond standard Tier-1 advisory rules — read-only + transform, NO file CREATE/EDIT/shell/network. **Sister-reference status backpatches** (per the §4.5.3 maintenance rule codified at §AK R1 A-R1-2): §AG gate updated (4 adopted: §AJ/§AH/§AK/§AL; 3 still UN-adopted: build-review-interface, evaluate-rag, meta-skill); §AJ gate updated (2 adopted: §AH/§AL; 1 still UN-adopted: build-review-interface) PLUS the §AJ trace-sourcing 0-trace mode rule fully updated to §AJ → §AL canonical chain. **Vendor concentration AFTER this adoption: 5 hamelsmu (LARGEST at 5/14 = 35.7%) + 4 Trail of Bits (4/14 = 28.6%) + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit** — **first time hamelsmu is the largest vendor**, overtaking ToB which was historically the largest. **hamelsmu cluster is now COMPLETE** — §AL is the 5th and FINAL skill in the upstream `hamelsmu/evals-skills` candidate set; no more hamelsmu cluster adoptions possible from this repo. **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule AND remains vendor-specific because cluster-size rationale (saturation risk from candidate-pool size) is structurally different between ToB (~6 candidates remaining) and hamelsmu (0 candidates remaining = saturation risk zero by construction). Adoption budget post-landing: **14 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm (body sha256 `2200459349ecdcd5bdaaad60950f940c743d155e26d6cdc495445efda6045f5e`; body bytes 4985; full upstream file 5427 bytes / 164 lines; vendored file 9734 bytes with 4749-byte forge-skills frontmatter+source-comment wrapper). **§AL Ordering basis backpatches:** per the §4.5.5 later-sibling backpatch rule, the §AL adoption commit ALSO updates §AG's, §AH's, §AJ's, AND §AK's Ordering basis lines to reflect the now-5-skill sequence on 2026-05-19 (sequence per committer-timestamp on main: §AG `2ce8bde` → §AH `7ec6efa` → §AJ `1bbde5c` → §AK `cb42717` → §AL `<TBD>`). **§AL is the FIRST adoption to backpatch FOUR prior siblings** in one commit. The §AL adoption was hardened through a **2-round sliced Codex feedback loop** producing **2 findings (0H/1M/1L)**, 100% applied; loader 14/14 pass throughout. R1 surfaced 2 findings (A: 1 MED + 1 LOW; B + C SOLID at R1 — first time Sections B + C were SOLID together at R1 for a hamelsmu cluster adoption). R2-A reached **3/3 SOLID overall** — both R1 fixes verified hold; convergence trajectory R1=2 → R2=0. **Cleanest R1 finding count in the cluster** (§AG=3, §AH=6, §AJ=3, §AK=4, §AL=2 — decreasing trend at end). **Shipped at R2 per `[[feedback_loop_stop_signal]]`** — matches §AH/§AK pattern; framework-lift count this round (0 framework lifts; 2 §AL-specific gate refinements) is the lowest since §AG's clean adoption. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AL full record NEWLY CREATED at line 1117 (third eval-list entry created at adoption time after §AJ + §AK); budget ledger updated to 14/30 with vendor-concentration milestone note (hamelsmu becomes LARGEST) + cluster COMPLETE note; §AL Notes section with R1-R2 doctrinal blocks + Ordering basis attestation; §AG/§AH/§AJ/§AK Ordering basis lines BACKPATCHED to 5-skill sequence (FIRST 4-WAY backpatch); §AG/§AJ gate entries SISTER-REFERENCE-STATUS BACKPATCHED per the §4.5.3 maintenance rule. Round-1+2 audit trail at `research/feedback_loops/agents-evaluation_list/20260519T140000Z-AL/` (local-only, gitignored).
- **Thirteenth vendored skill: `hamelsmu/evals-skills/validate-evaluator` — fourth hamelsmu-vendor adoption + extends cluster from 3 to 4 skills (first 4-skill cluster co-adoption in the framework) + FIFTH adoption to maintain its eval-list pre-adoption Tier estimate (hits N≥5 elevation threshold)** — adopted via the AGENTS.md §4.4.3 7-step process; completes the **eval-methodology pipeline `§AJ discover → §AH author → §AK validate (→ §AG audit)`**. Vendored at `.skills/hamelsmu/validate-evaluator/` (single SKILL.md, no companions; LICENSE byte-identical to §AG/§AH/§AJ's verified `evals-skills/LICENSE`) from `github.com/hamelsmu/evals-skills` @ SHA `febdb335bd658a01f756b8b5b3364277a4fa6a4a` — **SAME SHA as §AG/§AH/§AJ; fourth skill in the cluster co-adoption pattern**. Audited 2026-05-19: ✅ no encoded content; **SECOND adopted-skill-to-adopted-skill internal reference in the framework** (after §AJ→§AH at §AJ L131) — SKILL.md L5 cites `write-judge-prompt` which is ADOPTED as §AH; codified at §4.5.3 §AK gate as the canonical §AH → §AK SUCCESSOR chain (inverse direction to the §AJ → §AH PRECURSOR chain); ✅ no `claude -p` / SDK subprocess paths; ✅ no bundled subagents; ✅ no executable shell/python/node snippets vendored (FOUR Python snippets at L44-56 sklearn `train_test_split`, L76-83 sklearn `confusion_matrix`, L151-181 bootstrap CI function, L185-194 `judgy` library usage with `pip install judgy` recommendation at L183 — all INLINE documentation showing formulas + code, NOT runtime execution directives; same Tier 1 pattern as §AJ L104-109 and §AH L109-114). **Tier 1** (MAINTAINED from eval-list authoring's Tier 1 estimate — FIFTH adoption to do so after §J/§AG/§AH/§AJ; **strengthens the inline-output-stays-Tier-1 provisional observation from §AG R1 B-R1-2 from 4 datapoints to 5 datapoints — hits the N≥5 elevation threshold** in the original provisional caveat; may be re-evaluated for normative codification at next §4.4.5 quarterly review). Measured runtime context cost via tiktoken cl100k_base: SKILL.md **body 2170 tokens / 8278 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 2253 tokens / 8640 bytes including upstream YAML frontmatter); **largest single-file Tier 1 skill in the cluster** — ~28% larger than §AG's 1696 body tokens due to 4 Python code blocks + 4 reference tables + Rogan-Gladen bias-correction methodology + bootstrap confidence interval. **§AK is the second adoption with a NEWLY-CREATED Decision Record** (§AJ was first) — §AK was NOT in the original A-AI pre-scored pool (eval-list now spans A-AK = 37 records); all score axes assigned at adoption time. Score sum 29 (passes §4.4.4 ≥28 threshold; maint=3 inherited from cluster siblings; provenance=3 capped at axis max for fourth same-vendor adoption per §4.4.4). Author Hamel Husain, community per AGENTS.md §4.4.1 known-community list. Fit: LLM-judge calibration via TPR/TNR + Rogan-Gladen bias correction + bootstrap confidence intervals (the primary skill use case) + meta-applicability for calibrating the project's own `/codex-review` judge templates against curated finding catalogs — the TPR/TNR methodology provides the discipline our codex-review loop currently lacks. **Framework lifts this round (codified in AGENTS.md):** (1) **§4.5.3 Sister-reference status maintenance rule (NEW FRAMEWORK LIFT per Codex §AK R1 A-R1-2)** — parallel to the §4.5.5 later-sibling Ordering basis backpatch rule, this codifies that when an UN-adopted sister-skill reference cited in an existing adopted skill's gate entry becomes ADOPTED, the adopting-sister's commit MUST same-PR backpatch all affected gate entries to reflect the new adopted-vs-UN-adopted status. The §AG and §AH gate entries are backpatched in this §AK commit to reflect that 2-3 of their previously-UN-adopted sister references are now adopted (error-analysis → §AJ; write-judge-prompt → §AH; validate-evaluator → §AK). Failure to backpatch is a doctrine breach catchable at §4.4.3 step 3 review. (2) **§4.5.3 §AK gate framework patterns** (no §4.5.5 / §4.4.5 lifts this round beyond the sister-reference maintenance rule): (a) **Second adopted-skill-to-adopted-skill internal reference** — codified at §AK gate as canonical §AH → §AK SUCCESSOR chain (inverse of §AJ → §AH PRECURSOR); (b) **§AH → §AK gate-stack inheritance + multiplicity rule** (parallel to §AJ → §AH at §AJ R1 A-R1-2): agent MUST run §AK's full gate stack per validated judge; batching requires per-judge transcript logs; (c) **Fail-open-per-judge partial-failure semantics** (parallel to §AJ→§AH fail-open-per-category): calibration failure on one judge MUST NOT cancel others; (d) **Four metric-discipline rules from upstream SKILL.md L85/L208/L120-124/L212**: TPR/TNR-only metric rule; Cohen's-Kappa scope rule; Dev/test discipline rule (test-set one-shot); Bootstrap-CI mandatory rule; (e) **Sample-size mode taxonomy with per-class minimum** (5 modes: non-applicable / pre-validation / bootstrap / standard / saturation; computed from BOTH total N AND minority-class count `m = min(pass, fail)`; imbalance guard downgrades N≥100 with m<30 to `bootstrap` — refined per Codex §AK R1 A-R1-3 from initial total-count-only thresholds). **AGENTS.md §4.5.1 trigger row** added: positive-triggers cover "validate this judge prompt", "calibrate our LLM-judge", "compute TPR/TNR", "estimate the real pass rate with bias correction" patterns + cluster boundary with §AG/§AH/§AJ as 4-skill eval-methodology pipeline + canonical pipeline chains; negative-trigger list expanded to 15 illustrative classes (code-evaluator testing, new judge authoring, discovering failure modes, auditing eval system, Precision/Recall, Cohen's Kappa misuse, IAA on humans, model-selection-only, production-monitoring, A/B testing, judge-vendor comparisons, prompt regression, federated labeling, single-trace interpretability) with category-level guard prepended ("ONLY for LLM-judge calibration against human-labeled data using TPR/TNR + bias correction"). **AGENTS.md §4.5.3 §AK Policy Gates Index** entry added: NO policy gates beyond standard Tier-1 advisory rules — the skill is read-only + transform with NO file CREATE, NO file EDIT (refinement diffs are presented for user to apply via Edit tool; same pattern as §H), NO shell-execute (Python snippets are documentation; `pip install judgy` is user recommendation), NO skill-declared network egress. **Vendor concentration AFTER this adoption: 4 ToB + 4 hamelsmu (TIED at 4/13 = 30.8% — first time cluster co-adoption equalized two largest vendor groups) + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit** — ToB drops from 33.3% (4/12) to 30.8% (4/13) via the §AK hamelsmu cluster-extension effect; **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule. **hamelsmu-cluster gate observation (NEW)**: §J R3-B-2 ToB cluster gate is vendor-specific; no hamelsmu cluster gate exists. With hamelsmu now tied with ToB at 4/13, the question of generalizing the gate to ANY vendor at >50% concentration arises — flagged for next §4.4.5 quarterly review (Codex §AK R1 B-A2 verdict was "no immediate generic-vendor gate codification required at 30.8% with a finite hamelsmu candidate pool"). Adoption budget post-landing: **13 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm (body sha256 `b2c6e597dbfdd5e142fa8785ae405eb16051b03b3bb416dae6118f736c332a57`; body bytes 8278; full upstream file 8640 bytes / 212 lines; vendored file 13470 bytes with 5192-byte forge-skills frontmatter+source-comment wrapper). **§AK Ordering basis backpatches:** per the §4.5.5 later-sibling backpatch rule, the §AK adoption commit ALSO updates §AG's, §AH's, AND §AJ's Ordering basis lines to reflect the now-4-skill sequence on 2026-05-19 (sequence per committer-timestamp on main: §AG `2ce8bde` → §AH `7ec6efa` → §AJ `1bbde5c` → §AK `<TBD>`). **§AK is the FIRST adoption to backpatch THREE prior siblings** in one commit (§AG was backpatched by §AH then §AJ then §AK = 3 backpatches; §AH by §AJ then §AK = 2 backpatches; §AJ by §AK = 1 backpatch). The §AK adoption was hardened through a **2-round sliced Codex feedback loop** producing **4 findings (0H/2M/2L)**, 100% applied; loader 13/13 pass throughout. R1 surfaced 4 findings (A: 2 MED + 2 LOW; B + C SOLID at R1). R2-A reached **3/3 SOLID overall** — all 4 R1 fixes verified hold; convergence trajectory R1=4 → R2=0. **Shipped at R2 per `[[feedback_loop_stop_signal]]`** — matches §AH's R2/3 SOLID pattern; framework-lift count (1: sister-reference maintenance rule) is well-bounded with concrete patch. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AK full record NEWLY CREATED at line 1080 (second eval-list entry created at adoption time after §AJ); budget ledger updated to 13/30 with vendor-tie milestone note; §AK Notes section with R1-R2 doctrinal blocks + Ordering basis attestation; §AG/§AH/§AJ Ordering basis lines BACKPATCHED to 4-skill sequence; §AG/§AH gate entries SISTER-REFERENCE-STATUS BACKPATCHED per the new §4.5.3 maintenance rule. Round-1+2 audit trail at `research/feedback_loops/agents-evaluation_list/20260519T120000Z-AK/` (local-only, gitignored).
- **Twelfth vendored skill: `hamelsmu/evals-skills/error-analysis` — third hamelsmu-vendor adoption + extends cluster from 2 to 3 skills (first 3-skill cluster co-adoption in the framework) + FOURTH adoption to maintain its eval-list pre-adoption Tier estimate** — adopted via the AGENTS.md §4.4.3 7-step process; completes the **eval-methodology pipeline `§AJ discover → §AG audit → §AH author`**. Vendored at `.skills/hamelsmu/error-analysis/` (single SKILL.md, no companion files; LICENSE byte-identical to §AG/§AH's already-verified `evals-skills/LICENSE`) from `github.com/hamelsmu/evals-skills` @ SHA `febdb335bd658a01f756b8b5b3364277a4fa6a4a` — **SAME SHA as §AG and §AH; third skill in the cluster co-adoption pattern**. Audited 2026-05-19: ✅ no encoded content; ✅ sister-skill forward references at three locations — (a) L36 `generate-synthetic-data` (UN-adopted, AGENT_SKILLS.md §2E UN-adopted-target clause); (b) L102 `build-review-interface` (UN-adopted, same handling); (c) **L131 `write-judge-prompt` — ALREADY ADOPTED as §AH** — **the FIRST adopted-skill-to-adopted-skill internal reference in the framework**, codified at AGENTS.md §4.5.3 §AJ gate entry as the canonical §AJ → §AH pipeline chain; ✅ no `claude -p` / SDK subprocess paths (§2E "Bundled-script same-model self-invocation" non-applicable); ✅ no bundled subagents (§2E same-model bounded-subagent carve-out non-applicable); ✅ no executable shell/python/node snippets vendored (pure markdown prose; the Python snippet at L104-109 `failure_rates = labeled_df[failure_columns].sum() / len(labeled_df); failure_rates.sort_values(ascending=False)` is INLINE documentation showing the computation formula, NOT a runtime execution directive — the agent presents the formula for the user to apply in their preferred environment per L102 "spreadsheet / annotation app / simple script"; same pattern as §AH's JSON schema example at L109-114). **Tier 1** (MAINTAINED from eval-list authoring's Tier 1 estimate — FOURTH adoption to do so after §J + §AG + §AH; strengthens the inline-output-stays-Tier-1 provisional observation from §AG R1 B-R1-2 from 3 to 4 datapoints, still recorded as observational not normative per the limited-sample caveat). Measured runtime context cost via tiktoken cl100k_base: SKILL.md **body 1696 tokens / 7925 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 1763 tokens / 8255 bytes including upstream YAML frontmatter); between §AG (1970 body) and §AH (1524 body) in size — the 7-step process is more verbose than §AH's 4-component pattern but less than §AG's 6-area diagnostic checks. **§AJ is the first adoption with a NEWLY-CREATED Decision Record** — §AJ was NOT in the original A-AI pre-scored pool (eval-list now spans A-AJ = 36 records); all score axes assigned at adoption time. Score sum 29 (passes §4.4.4 ≥28 threshold; maint=3 inherited from cluster siblings — same upstream repo + 77-day-stale single-maintainer signal). Author Hamel Husain, community per AGENTS.md §4.4.1 known-community list (provenance 3, capped at axis max per §4.4.4). Fit: LLM-pipeline failure-mode discovery via trace analysis (the primary skill use case) + meta-applicability for systematically auditing the project's own `/codex-review` finding catalogs over time. **Framework lifts this round (codified in AGENTS.md):** **§4.5.3 §AJ gate entry framework patterns** (no AGENTS.md §4.5 / §4.4.5 / §4.4.3 lifts this round — all surfaced rules are §AJ-specific gates per the conservative interpretation, but they establish operational patterns that may be lifted to framework-level at future quarterly review): (1) **First adopted-skill-to-adopted-skill internal reference handling** — the §AJ → §AH transition rule (L131 → §AH) is the first operational pattern for adopted-skill internal references; codified at §AJ gate entry with explicit gate-stack inheritance and multiplicity rules (per Codex §AJ R1 A-R1-2). (2) **Trace-sourcing escape-hatch with 5-mode trace-count taxonomy** — `non-applicable` (0) / `pre-bootstrap` (1-9) / `bootstrap` (10-29) / `standard` (30-99) / `saturation` (≥100); mode classification transcript line MUST be emitted at every §AJ invocation, absence is HIGH-severity doctrine breach at §4.4.5 first-3-invocations audit (per Codex §AJ R1 A-R1-3 + R2 A-R2-1). (3) **Batched §AJ→§AH hand-off fail-open-per-category partial-failure semantics** — batch invocation evaluated independently per category with `{instrument | defer | reject}` status; gate failure on one category does NOT cancel others (per Codex §AJ R2 A-R2-2). (4) **Category-level guard prepended to negative-trigger enumeration** — §AJ is ONLY for "trace-level OUTPUT-quality failure-mode discovery for LLM responses"; the 14-item enumeration is now illustrative not load-bearing (per Codex §AJ R2 A-R2-3). **AGENTS.md §4.5.1 trigger row** added: positive-triggers cover "do error analysis", "categorize the failure modes I see", "production metrics dropped — read these failure traces and find patterns" patterns + cluster boundary with §AG and §AH (precursor vs audit vs author) + 3 canonical pipeline chains; negative-trigger list expanded to 14 illustrative classes (code-level error analysis, production-incident RCA, generic ML metric questions, brainstorming-without-traces, data-quality audits, labeled-data sourcing, eval-pipeline code review, existing catalog audit, new judge authoring, RAG audits, RLHF dataset construction, classifier training, test authoring from failure modes, perf-only outlier analysis) with category-level guard prepended. **AGENTS.md §4.5.3 §AJ Policy Gates Index** entry added: NO policy gates beyond standard Tier-1 advisory rules — the skill is read-only + transform with NO file CREATE, NO file EDIT, NO shell-execute, NO skill-declared network egress. **Vendor concentration AFTER this adoption: 4 ToB + 3 hamelsmu + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit = 4/12 (33.3%)** — ToB drops from 36.4% (4/11) to 33.3% (4/12) via the §AJ hamelsmu cluster-extension effect; **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule. Adoption budget post-landing: **12 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm (body sha256 `8243ee67f593fd7942f7e2724beabf485bf50b29b2af5ba05e3b812bf961f859`; body bytes 7925; full upstream file 8255 bytes / 164 lines; vendored file 12344 bytes with 4419-byte forge-skills frontmatter+source-comment wrapper). **§AJ Ordering basis backpatches:** per the §4.5.5 later-sibling backpatch rule codified at §AH adoption per Codex §AH R1 C-R1-1, the §AJ adoption commit ALSO updates BOTH §AG's and §AH's Ordering basis lines to reflect the now-3-skill sequence on 2026-05-19 (sequence per committer-timestamp on main: §AG `2ce8bde` → §AH `7ec6efa` → §AJ `<TBD>`). §AJ is the FIRST adoption to exercise the backpatch rule on TWO prior siblings. The §AJ adoption was hardened through a **3-round sliced Codex feedback loop** producing **6 findings (0H/3M/3L)**, 100% applied; loader 12/12 pass throughout. R1 surfaced 3 findings (A: 2 MED + 1 LOW) — Section A `needs_changes`; Sections B + C SOLID at R1. R2 surfaced 3 findings (A: 1 MED + 2 LOW; severity decreased) — Section A `not_solid`; B + C carried SOLID. **R3-A reached SOLID (3/3 SOLID overall)** — all 6 R1+R2 fixes verified hold; convergence trajectory R1=3 → R2=3 (severity-decreased, count-same) → R3-A=0. **Shipped at R3 per `[[feedback_loop_stop_signal]]`** — convergence pattern matches §A (Tier 2 single-artifact ≈ R3 with multiple framework-lift surfaces); §AJ is Tier 1 but the 4 §AJ-specific gate refinements (negative-trigger guard, 5-mode trace taxonomy, §AJ→§AH hand-off rules, partial-failure semantics) added auditing depth comparable to §A's framework lifts. Memory anchor [[feedback_loop_stop_signal]] updated with new "Tier 1 single-file + 4 gate refinements ≈ R3" data point. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AJ full record NEWLY CREATED at line 1044 (first eval-list entry created at adoption time — A-AI were all pre-scored); budget ledger updated to 12/30 at line 224 with vendor concentration drop 36.4%→33.3% + 3-skill hamelsmu-cluster note; §AJ Notes section with R1-R3 doctrinal blocks + Ordering basis attestation referencing §AG `2ce8bde` → §AH `7ec6efa` → §AJ `<TBD>`; §AG AND §AH Ordering basis lines BACKPATCHED in the same commit per the §4.5.5 later-sibling backpatch rule. Round-1+2+3 audit trail at `research/feedback_loops/agents-evaluation_list/20260519T100000Z-AJ/` (local-only, gitignored).
- **Eleventh vendored skill: `hamelsmu/evals-skills/write-judge-prompt` — second hamelsmu-vendor adoption + sister/cluster co-adoption to §AG (eval-quality-methodology cluster) + THIRD adoption to maintain its eval-list pre-adoption Tier estimate** — adopted via the AGENTS.md §4.4.3 7-step process; pairs with §AG as the eval-methodology cluster (§AG audits judge prompts → §AH authors corrected designs). Vendored at `.skills/hamelsmu/write-judge-prompt/` (single SKILL.md, no companion files; LICENSE byte-identical to §AG's already-verified `evals-skills/LICENSE`) from `github.com/hamelsmu/evals-skills` @ SHA `febdb335bd658a01f756b8b5b3364277a4fa6a4a` — **SAME SHA as §AG; first "cluster co-adoption" in the framework where one upstream source attests two adopted skills**. Audited 2026-05-19: ✅ no encoded content; ✅ no transitive `load skill X` refs beyond UN-adopted-target citations (two sister skills `validate-evaluator` at L143 + `error-analysis` at L17 are advisory citations to UN-adopted targets, handled by AGENT_SKILLS.md §2E "Conditional load of an UN-adopted target" clause); ✅ no `claude -p` / SDK subprocess paths (§2E "Bundled-script same-model self-invocation" clause is non-applicable); ✅ no bundled subagents (§2E same-model bounded-subagent carve-out also non-applicable); ✅ no executable shell/python/node snippets vendored (pure markdown prose; the JSON schema example at L109-114 `{"critique": "...", "result": "Pass or Fail"}` is INLINE documentation showing the structured-output format the PRODUCED judge prompt will use at the user's eval-pipeline runtime, NOT a file-write directive emitted by the agent applying §AH). **Tier 1** (MAINTAINED from eval-list authoring's Tier 1 estimate — THIRD adoption to do so after §J + §AG; strengthens the inline-output-stays-Tier-1 provisional observation from §AG R1 B-R1-2 from 2 datapoints to 3 datapoints, still recorded as observational not normative per the limited-sample caveat). Measured runtime context cost via tiktoken cl100k_base: SKILL.md **body 1524 tokens / 7263 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 1621 tokens / 7709 bytes including upstream YAML frontmatter); ~24% smaller than §AG's SKILL.md — sub-skill specialization is more compact than the audit overview. Score sum 29 (passes §4.4.4 ≥28 threshold; maint=3 inherited from §AG R1 B-R1-1 calibration — same upstream repo, same 77-day-stale single-maintainer signal; provenance=3 same-vendor 2nd adoption, axis maxes at 3 per §4.4.4). Author Hamel Husain, community per AGENTS.md §4.4.1 known-community list. Fit: judge-prompt authoring for eval pipelines (the primary skill use case) + meta-use for grading the agent's own `/codex-review` judge prompts for new doctrine surfaces. **Framework lifts this round (codified in AGENTS.md):** (1) **§4.5.5 later-sibling backpatch rule** — when a later same-date adoption lands, the adoption commit MUST co-edit ALL prior same-date Decision Records' Ordering basis lines to reflect the now-existing sibling chain (codified per Codex §AH R1 C-R1-1 — the §AG/§AH first multi-adoption-day pair surfaced this gap; §AG's Ordering basis was originally "solo adoption today" and would have gone stale after §AH landed without an explicit backpatch rule). §AG's Ordering basis backpatched in this adoption commit to: `Ordering basis: 2026-05-19 sequence per committer-timestamp on main: §AG 2ce8bde → §AH <ah-sha>`. (2) **§4.5.5 ordering direction asymmetry codification** — the framework uses TWO ordering directions: (i) chronological oldest→newest for the enumeration registries (§4.5.1 trigger table, §4.5.3 Policy Gates Index, §4.5.6 anti-pattern arithmetic, evaluation_list ledger+Counts); (ii) reverse-chronological newest→oldest for CHANGELOG `### Added` per Keep a Changelog convention (codified per Codex §AH R1 C-R1-2 — §A R1+R2 established the convention but never explicitly codified). §AH bullet placed ABOVE §AG bullet in this CHANGELOG entry per the codified rule. (3) **§4.4.5 cluster-co-adoption note** — when two or more adopted skills share canonical upstream repo + SHA, quarterly source-SHA review MAY be executed as ONE shared upstream check covering the cluster, BUT each skill's Decision Record MUST still be independently attested (codified per Codex §AH R1 C-R1-3, provisional at single-cluster sample of §AG+§AH; re-evaluate for normative elevation at next §4.4.5 quarterly if additional clusters appear or §AG/§AH operationally diverge). **AGENTS.md §4.5.1 trigger row** added: positive-triggers cover "write a judge prompt", "design an LLM-as-judge for X", "author a binary evaluator for Y" patterns + the cluster boundary with §AG (CREATE vs AUDIT); negative-trigger list covers ELEVEN adjacent classes (code-based checks, Likert/numeric/letter, validation/calibration, auditing existing prompts, general prompt-engineering, eval-pipeline code review, human-reviewer rubric design, judge-execution wrappers, red-team/adversarial, IAA/labeling protocol, model-selection-only) — expanded per Codex §AH R1 A-R1-1 from the initial 6-item list because §J R2-C-1 negative-trigger doctrine requires explicit guarding of plausible adjacent task classes. **AGENTS.md §4.5.3 §AH Policy Gates Index** entry added: NO policy gates beyond standard Tier-1 advisory rules (§3.2) — the skill is read-only + transform with NO file CREATE, NO file EDIT, NO shell-execute, NO skill-declared network egress. Two NEW falsifiability rules per Codex §AH R1: (i) **code-based-check escape hatch transcript log** (A-R1-2): agent MUST emit `[§AH escape-hatch eval: <alternatives> | <ruled-out reasons> | verdict=<invoke §AH | stay code-based>]` BEFORE invoking the 4-component workflow — absence at §4.4.5 first-3-invocations audit is a HIGH-severity doctrine breach; (ii) **Likert non-applicability rule** (A-R1-3): if user explicitly requires non-binary output, §AH is non-applicable — state mismatch + counter-propose multiple binary judges per SKILL.md L142 + emit `[§AH non-applicable: user-requested non-binary evaluator format <format>]` transcript note. **Vendor concentration AFTER this adoption: 4 ToB + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit + 2 hamelsmu = 4/11 (36.4%)** — ToB drops from 40% (4/10) to 36.4% (4/11) via the §AH hamelsmu cluster-co-adoption effect; **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule. Adoption budget post-landing: **11 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm (body sha256 `12685e98f3a73d353160f84a9b1ef8ea240b8b09ced4a0f87d030ecaa53351b7`; body bytes 7263; full upstream file 7709 bytes / 144 lines; vendored file 11661 bytes with 4398-byte forge-skills frontmatter+source-comment wrapper). The §AH adoption was hardened through a **2-round sliced Codex feedback loop** producing **6 findings (0H/3M/3L)**, 100% applied; loader 11/11 pass throughout. R1 surfaced 6 findings — A: 2 MED (negative-trigger expansion to 11 classes; escape-hatch transcript log) + 1 LOW (Likert non-applicability); B: SOLID; C: 1 MED (later-sibling backpatch + §AG backpatch) + 2 LOW (ordering direction asymmetry; cluster-co-adoption §4.4.5 note). **R2 reached 3/3 SOLID** — cleanest convergence trajectory in the framework's history (finding-count R1=6 → R2=0; all 6 R1 fixes verified hold). **Shipped at R2 per `[[feedback_loop_stop_signal]]`** — Tier 1 single-file ≈ R2 holds for §AH, matching §AG's pattern despite §AH surfacing 3 framework lifts. Memory anchor [[feedback_loop_stop_signal]] updated with the new "framework-lift adoptions can still converge at R2 if all lifts are well-bounded MED-or-lower" data point. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AH full record at line 978; budget ledger updated to 11/30 at line 224 with vendor concentration drop 40%→36.4% + hamelsmu-cluster note; §AH Notes section with R1-R2 doctrinal blocks + Ordering basis attestation referencing §AG `2ce8bde` → §AH `<ah-sha>`; §AG Ordering basis line BACKPATCHED in the same commit per the new §4.5.5 later-sibling backpatch rule. Round-1+2 audit trail at `research/feedback_loops/agents-evaluation_list/20260519T080000Z-AH/` (local-only, gitignored).
- **Tenth vendored skill: `hamelsmu/evals-skills/eval-audit` — first hamelsmu-vendor adoption + first eval-quality-methodology skill + SECOND adoption (after §J) to maintain its eval-list pre-adoption Tier estimate** — adopted via the AGENTS.md §4.4.3 7-step process; meta-applies to the project's own `/codex-review` feedback-loop methodology — the skill's six diagnostic checks (error analysis, evaluator design, judge validation, human-review process, labeled data, pipeline hygiene) map directly onto our `prompt.md` + `codex-finding-schema.json` artifact set, so future quarterly reviews can grade our own loop using its doctrine. Vendored at `.skills/hamelsmu/eval-audit/` (single SKILL.md, no companion files; upstream `evals-skills/LICENSE` is MIT, sha256 `e2e8edff4cd1b2a37818287afc35ac08a2fe8e9ee2fff36fc92a032b87451d56` — vendorable per AGENTS.md §4.4.1 community-licensing clause) from `github.com/hamelsmu/evals-skills` @ SHA `febdb335bd658a01f756b8b5b3364277a4fa6a4a`. **Repo correction at Step 1 source audit:** the eval-list URL pointed at `hamelsmu/prompts/evals-skills/...` which 404s; the actual repo is `hamelsmu/evals-skills/` (no `prompts/` parent). Captured as a source-audit finding and propagated to the §AG Decision Record's URL / Source-canonical fields. Audited 2026-05-19: ✅ no encoded content; ✅ no transitive `load skill X` refs beyond UN-adopted-target citations (six sister skills `error-analysis` / `generate-synthetic-data` / `write-judge-prompt` / `validate-evaluator` / `build-review-interface` + implicitly `evaluate-rag` are all advisory citations to UN-adopted targets, handled by AGENT_SKILLS.md §2E "Conditional load of an UN-adopted target" clause; the agent MUST NOT auto-load any sister); ✅ no `claude -p` / SDK subprocess paths (§2E "Bundled-script same-model self-invocation" clause is non-applicable); ✅ no bundled subagents (§2E same-model bounded-subagent carve-out also non-applicable); ✅ no executable shell/python/node snippets vendored (pure markdown prose; the `Report Format` template at SKILL.md L168-173 is an inline ASCII scaffold the agent emits as transcript content, NOT a file-write directive). **Tier 1** (MAINTAINED from eval-list authoring's Tier 1 estimate — only the SECOND adoption to do so after §J, breaking the §H/§D/§T/§A Tier 1→2 correction streak; per §J R3-A-3 create-vs-edit operation-surface doctrine, file CREATE = repo-write = Tier 2, and §AG's primary deliverable is an inline transcript report not a file artifact, so the Tier-2 escalation is NON-APPLICABLE). Measured runtime context cost via tiktoken cl100k_base: SKILL.md **body 1970 tokens / 9505 body bytes** post-frontmatter-strip per AGENT_SKILLS.md §2D canonical body-extraction algorithm (full-file 2071 tokens / 9949 bytes including upstream YAML frontmatter); smallest-archive-shape Tier 1 to date — well under 5K-preferred per AGENT_SKILLS.md §3B, no archive justification required. Score sum 29 (passes §4.4.4 ≥28 threshold; corrected from sum 30 per Codex §AG Round-1 Section B Finding B-R1-1 — maint 4→3 because upstream is 77 days stale at adoption, single-maintainer signal, aligning with the moderately-stale community-repo calibration). Author Hamel Husain, community per AGENTS.md §4.4.1 known-community list (provenance 3, raised from typical solo-author baseline of 2 per the round-1 Finding 6 community calibration). Fit: eval methodology audits (the primary skill use case) + meta-use for grading our own Codex feedback-loop doctrine. **No framework lifts this round** — unlike §V which introduced Tier 3 handling rule + triple-compound loader enum, or §A which surfaced the inline-generic same-model bounded-subagent pattern, §AG fits cleanly inside the existing §2D + §2E + §J R3-A-3 doctrine and required only the standard §4.5.1 + §4.5.3 + §4.5.6 + evaluation_list 5-item atomic-commit set. **AGENTS.md §4.5.1 trigger row** added: positive-triggers cover "audit my evals", "is this eval system trustworthy", "review the judge prompts", "are our LLM-as-judge metrics any good" patterns plus meta-applicability for `research/feedback_loops/` self-audit; negative-trigger list covers EIGHT adjacent classes (new-evaluator construction, eval-code review, general ML training-quality questions, eval-infra setup, data-quality audits not tied to evaluator/judge methodology, model-performance regression incident triage, eval/observability vendor comparison, production-monitoring reliability audits) — expanded per Codex §AG R1 A-R1-1 from the initial 4-item list because §J R2-C-1 negative-trigger doctrine requires explicit guarding of plausible adjacent task classes; precedence rule (per Codex §AG R2 A-R2-1) sets §AG ahead of §H when the artifact under review is evaluator/judge methodology content (judge prompts, rubric design, pass/fail criteria, scorer calibration) — §H reviews implementation, §AG reviews methodology. **AGENTS.md §4.5.3 §AG Policy Gates Index** entry added: NO policy gates beyond standard Tier-1 advisory rules (§3.2) — the skill is read-only + transform with NO file CREATE, NO file EDIT, NO shell-execute, NO skill-declared network egress. Sister-skill forward references handled per AGENT_SKILLS.md §2E UN-adopted-target clause; optional observability-MCP reads (Phoenix / Braintrust / LangSmith / Truesight at SKILL.md L28) use the USER's existing host MCP grants — same pattern as §A's optional MCP integrations clause; §AG itself does NOT declare network scope. **Vendor concentration AFTER this adoption: 4 ToB + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit + 1 hamelsmu (new) = 4/10 (40%)** — ToB drops from 44% (4/9) to 40% (4/10) via the §AG hamelsmu-vendor-diversification effect; **§J R3-B-2 ToB cluster gate remains INACTIVE** under strict `>50%` rule. Adoption budget post-landing: **10 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the §2D canonical algorithm (body sha256 `366a16f8e777e3326f52f4065e4036703590897b0b9bfe3076ef72c74a6486d1`; body bytes 9505; full upstream file 9949 bytes / 183 lines; vendored file 12745 bytes with 3240-byte forge-skills frontmatter+source-comment wrapper). The §AG adoption was hardened through a **2-round sliced Codex feedback loop** producing **4 findings (0H/0M/4L)**, 100% applied; loader 10/10 pass throughout. R1 surfaced 3 findings (A-R1-1 negative-trigger list expansion to 8 adjacent classes; B-R1-1 maint 4→3 with sum 30→29; B-R1-2 "pattern emerging" softened to provisional observation — propagated to AGENTS.md ledger + evaluation_list Counts + .skills/.../SKILL.md wrapper for cross-document consistency per [[feedback_loop_quality_bar]]). R2 reached **2/3 SOLID** (Sections B + C SOLID; Section A `needs_work` with 1 LOW finding A-R2-1 — judge-prompt precedence + eval governance/process exclusion, applied). **Shipped at R2 per `[[feedback_loop_stop_signal]]`** — fastest convergence to date: §AG (Tier 1 single-file) at R2 with 2/3 SOLID exceeds §J (Tier 1 multi-file) at R3 with 1/3 SOLID and matches §A (Tier 2 single-file) at R3 with 2/3 SOLID. Finding-count trend 3→1 (decreasing — convergence signal). Memory anchor [[feedback_loop_stop_signal]] updated with the new Tier-keyed data point: **Tier 1 single-file ≈ R2** (faster than Tier 1 multi-file which converges at R3, consistent with the "moving parts to audit" convergence-driver hypothesis from §A's adoption). **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §AG full record at line 957; budget ledger updated to 10/30 at line 224 with vendor concentration drop 44%→40%; §AG Notes section with R1-R2 doctrinal blocks + Ordering basis attestation (solo 2026-05-19 cohort). Round-1+2 audit trail at `research/feedback_loops/agents-evaluation_list/20260519T060000Z-AG/` (local-only, gitignored). Eval-list `Ordering basis:` field added per AGENTS.md §4.5.5 same-date canonical tie-break rule: §AG is solo on its 2026-05-19 user-decision date (no same-date siblings; sequence-anchor is the §AG adoption commit on `main`).
- **Ninth vendored skill: `anthropics/doc-coauthoring` — second Anthropic-first-party adoption + smallest-archive-shape Tier 2 + 4th Tier 1→2 correction at adoption review** — adopted via the AGENTS.md §4.4.3 7-step process; daily-driver value for documentation drafting workflows (the exact skill we kept reaching for while hand-drafting §V's CHANGELOG narrative + adoption commit body during the §V loop). Vendored at `.skills/anthropics/doc-coauthoring/` (single SKILL.md, no companion files; no LICENSE — upstream `skills/doc-coauthoring/` subtree contains none at the recorded SHA, and the repo root has only `.gitignore`/`README.md`/`THIRD_PARTY_NOTICES.md`; fabricating a LICENSE would be false provenance) from `github.com/anthropics/skills` @ SHA `6a5bb06904ab164a345e41c381fc9097954b83da`. Path correction: actual upstream path is `skills/doc-coauthoring/` (Anthropic restructured the repo since §D's adoption SHA `f458cee31a7577a47ba0c9a101976fa599385174` when skills lived under `document-skills/<slug>/`). Audited 2026-05-18: ✅ no encoded content; ✅ no transitive `load skill X` refs; ✅ no `claude -p` / SDK subprocess paths (the §2E "Bundled-script same-model self-invocation" clause is non-applicable — no scripts at all in the vendored set); ✅ no executable shell/python/node snippets vendored (pure markdown prose; `create_file`/`str_replace` mentions are tool-name references to Claude.ai artifact API + Claude Code Edit tool, NOT embedded scripts); ⚠️ **Generic same-model bounded sub-agent (Stage 3 Reader Testing, SKILL.md L251-273):** workflow invokes a generic UNNAMED sub-agent via the host's Task tool — a THIRD pattern beyond §G/§H's registry-named (`function-analyzer`, `adversarial-modeler` in `~/.claude/agents/`) and §D's vendored-named (`agents/{analyzer,comparator,grader}.md`). The inline-generic pattern is now first-class in the framework (see "Framework lift" below); ✅ no §3.3 commit-message authority conflict — the speculative eval-list flag ("Verify it doesn't claim authority over commit-message format") was checked against the SKILL.md at adoption and resolved cleanly (skill scope is PRDs / design-docs / decision-docs / RFCs / specs / proposals; never touches commit messages, Conventional Commits format, or CHANGELOG narrative). **Tier 2** (corrected from eval-list authoring's Tier 1 estimate per §4.4.3 step 2 — eval-list misclassified §A as `read-only+transform` based on SKILL.md's READ-only content; observed runtime scope: skill CREATES a new markdown file at Stage 2 [`decision-doc.md`, `technical-spec.md`, user-specified name] as workflow's primary deliverable, then iteratively edits via `str_replace`; per §J R3-A-3 create-vs-edit operation-surface doctrine, file CREATE = repo-write artifact = Tier 2). Same correction pattern as §H/§D/§T; 4th Tier 1→2 correction. Measured runtime context cost via tiktoken cl100k_base: SKILL.md body **3289 tokens** (no companion files; worst-case archive = 3289 tokens — **under 5K-preferred per AGENT_SKILLS.md §3B, no archive justification required**; same class as §T's 1022t/4076t archive and §V's 1068t single-file shape). Author Anthropic, first-party. Fit: documentation drafting (PRDs, design docs, ADRs, RFCs, specs, proposals, runbooks); pairs naturally with §F when write-a-doc request is itself underspecified. **Framework lift this round (codified across AGENTS.md + research/agents/AGENT_SKILLS.md):** (1) **AGENT_SKILLS.md §2E condition 4 amendment** — enumerated to THREE permissible spec sources for the same-model bounded-subagent carve-out: **(a) registry-named** (SHA-pinned at adoption via `~/.claude/agents/`), **(b) vendored-named** (SHA-pinned at adoption via skill's bundled spec file), **(c) inline-generic** (no spec file; spawn-site itself proves boundedness via FIVE on-site checks: same-model family + no tool-envelope expansion + strict input-subset of parent's session context + explicit fail-closed fallback + **explicit allowed-input contract** declaring exact inputs the spawn prompt may include — for §A: `{document content, reader question}` only). The three sources are a CLOSED SET; novel patterns fail-closed to Tier 3 until §2E is amended (added per Codex §A R1+R2 findings A-R1-2/A-R2-2/A-R2-3 — closes a doctrinal soft edge where adopters could claim "condition 4 N/A" without a standardized boundedness test). (2) **AGENT_SKILLS.md §2D canonical body-extraction algorithm** — lifted from §A Decision Record Notes to shared §2D doctrine for all future vendored adoptions; defines deterministic byte-normalization for auditor recomputation of body sha256: preconditions (UTF-8 no-BOM + YAML-only frontmatter — non-conforming files fail-closed as "uncomputable per §2D canonical algorithm"); 5-step algorithm (read raw bytes / frontmatter-classification / strip-through-closing-`---`-inclusive / retain-subsequent-bytes-verbatim including immediate-leading-newline / sha256); closed-set extensibility (added per Codex §A R1+R2 findings B-R1-3/B-R2-3/B-R2-4 — propagates to sister docs immediately per `[[feedback_loop_quality_bar]]` rather than deferring to quarterly). (3) **AGENTS.md §4.5.1 trigger row** added for §A with negative-trigger list covering README updates, inline-code comments/docstrings, commit messages (§3.3), routine CHANGELOG edits, one-paragraph notes, AND short-form communication artifacts (Slack/Teams posts, email drafts, meeting notes, status updates, retro writeups); carve-back wording is "artifact-class is the primary trigger; named audience and intended impact are strong disambiguators, not mandatory co-requirements" (relaxed per Codex §A R2 A-R2-1 from a stricter AND-form that over-constrained legitimate requests). (4) **AGENTS.md §4.5.3 §A Policy Gates Index** added with 5 gates: Tier-2 Phase-A/Phase-B user-confirmation; single-artifact write surface with **6-state path-type preflight matrix** (`REJECT_DIR_TARGET` / `OVERWRITE_REGULAR` / `OVERWRITE_SYMLINK` / `OVERWRITE_UNTRACKED_WARN` / `CREATE_NEW` / `CREATE_REQUIRES_PARENT` — added per Codex §A R2 B-R2-1 because §A accepts user-specified filenames/paths unlike §T's deterministic pattern, so an unintended OVERWRITE risk exists that binary CREATE/OVERWRITE classification didn't cover); generic same-model bounded sub-agent gate citing §2E inline-generic carve-out with §A-specific allowlist `{document content, reader question}`; no-shell-execute / no-`claude -p` / no-self-invocation gate; user-mediated optional MCP integrations (Slack/Drive/etc. at SKILL.md L70-80, L304 are user/host-mediated, NOT skill-declared network egress — same pattern as §F using host's `gh`). (5) **AGENTS.md §4.5.5 enumeration ordering rule + same-date canonical tie-break + adoption atomic-commit set** — three new sub-sections codified per Codex §A R1+R2 findings C-R1-2/C-R2-1/C-R2-2: (i) active-adopted enumerations across §4.5.1 / §4.5.3 / §4.5.6 / evaluation_list ledger+Counts MUST be adoption-chronological; (ii) same-date tie-break uses committer timestamp on `main` as primary canonical key, CHANGELOG `### Added` bullet order at adoption commit time as secondary fallback, and per-adoption Decision Record `Ordering basis:` field as per-adoption attestation; (iii) adoption commit MUST land a 5-item set in ONE commit: (a) §4.5.1 row + (b) §4.5.3 entry + (c) evaluation_list ledger+Counts + (d) vendored `.skills/<vendor>/<slug>/` payload + (e) CHANGELOG `### Added` bullet — splitting across multiple commits is a doctrine breach catchable at PR merge review (mirrors the removal atomic-quad doctrine from §V Round-3+Round-4). (6) **AGENTS.md §4.5.6 anti-pattern arithmetic** updated for 9-skill set: ~20.9K tokens for all SKILL.md bodies combined; ~59.4K tokens worst-case read-into-context archive. (7) **CHANGELOG §V correction note** added per Codex §A R2 B-R2-2 — the "remains armed for any future 5th ToB pick" framing was forward-looking from §V's adoption state but conflicts with strict `> 50%` semantics after §A dropped concentration to 4/9 = 44%; corrected with dated qualifier + post-§A status. **Vendor concentration AFTER this adoption: 4 ToB + 2 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit = 4/9 (44%)** — ToB drops below the 50% boundary via §A's Anthropic-vendor-diversification effect; **§J R3-B-2 ToB cluster gate is now INACTIVE** (was at-the-boundary armed at 4/8 = 50.0% post-§V). Threshold semantics: strict `> 50%` (not inclusive `≥ 50%`) — a hypothetical 5th ToB adoption at 5/10 (50.0%) does NOT trigger the gate; gate activates only at strictly > 50% (e.g., 6/11 = 54.5%). Adoption budget post-landing: **9 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via the new §2D canonical algorithm (body sha256 `c37b110ac86e414d72d7b46a1b0e521c23109698debb35c45c24fa1f82be1bd7`; body bytes 15343; full upstream file 15815 bytes / 375 lines; vendored file 18550 bytes with 3207 forge-skills frontmatter+source-comment wrapper). The §A adoption was hardened through a **3-round sliced Codex feedback loop** producing **18 findings (0H/8M/10L)**, 100% applied; loader 21/21 pass + ruff-clean throughout. R1 surfaced 6 findings (5 distinct after dedup) including the §2E inline-generic codification + target-path overwrite disclosure + body-sha methodology + ordering rule. R2 surfaced 9 findings refining all R1 fixes (allowed-input contract; 6-state path-type matrix; algorithm preconditions; lift-to-§2D; canonical tie-break; explicit single-commit MUST set). **R3 reached 2/3 SOLID (Sections A + B SOLID; Section C `needs_changes` with 3 governance-refinement findings)** — convergence trend 6→9→3 findings, finding count finally decreasing; Section A+B verdict shift from `needs_changes` to `solid` matches §V's R6 SOLID-section-ship pattern. **Shipped at R3 per `[[feedback_loop_stop_signal]]`** ("ship at first SOLID-section verdict unless the skill's risk profile genuinely warrants more depth"; §A is Tier 2 single-artifact, low operational risk; R3 2/3 SOLID exceeds the §V R6 1/3 SOLID ship threshold). **Three R3 Section C findings filed as gitignored backlog** for next §4.4.5 quarterly review: **AB-029** (multi-adoption same-date squash tertiary authority clause — currently degenerate as zero squash cohorts exist); **AB-030** (extend 5-item atomic set with conditional (f) §4.5.6 arithmetic [already covered elsewhere] / (g) loader enum+test co-edits / (h) doctrine amendment items); **AB-031** (adoption commit recovery semantics for pre-commit-failures / iterated-commits / post-merge fix-commits). **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §A full record at line 252; budget ledger updated to 9/30 at line 224; §A Notes section with R1-R3 doctrinal blocks + 3 backlog items. Round-1+2+3 audit trail at `research/feedback_loops/agents-evaluation_list/20260518T181000Z-A/` (local-only, gitignored). Eval-list `Ordering basis:` field added per AGENTS.md §4.5.5 same-date canonical tie-break rule: §A is solo on its 2026-05-18 user-decision date (no same-date siblings; §V's adoption commit `9854b13` landed on 2026-05-18 UTC but §V's user-decision date is 2026-05-17 per the Decision Record).
- **Eighth vendored skill: `coderabbitai/code-review` — first Tier 3 adoption + first CodeRabbit-vendor adoption + first triple-compound tool-scope `shell-execute+network+cross-agent`** — adopted via the AGENTS.md §4.4.3 7-step process; the largest Tier correction to date (eval-list Tier 1 → adoption Tier 3) and the first adoption to require codifying Tier 3 invocation doctrine at §4.5.2 step 4. Vendored at `.skills/coderabbitai/code-review/` (SKILL.md only; LICENSE preserved at vendored root) from `github.com/coderabbitai/skills` @ SHA `a81eb76a1539e4a3f2b5c6fc133849124e72d303`. Upstream `agents/code-reviewer.md`, `commands/coderabbit-review.md`, and `assets/coderabbit-logomark.svg` are **NOT vendored** — they are Claude Code plugin-marketplace artifacts outside the SKILL.md scope per §4.4.3 strict reading (same pattern as §H's unvendored `adversarial-modeler.md`). Audited 2026-05-17: ✅ no encoded content; ✅ no transitive `load skill X` refs; ❌ **cross-agent invocation: YES** (CodeRabbit's hosted AI models analyze diffs → canonical Tier 3 per AGENT_SKILLS.md §2C content rule + §2E cross-agent flag; the dominant criterion is execution-boundary + tool-grant non-inheritance — the spawned SaaS review session does not inherit forge-works's tool/permission envelope, per Codex §V R1-B-2 — CodeRabbit's underlying model family (GPT-4-based) is incidental); ❌ **network: YES** (CLI → `api.coderabbit.ai`); ❌ **shell-execute: YES** (`coderabbit`, `cr`, `git` invocations); ✅ no `claude -p` / SDK subprocess paths; ✅ no executable shell/python/node snippets vendored (the bash blocks in SKILL.md are command examples for runtime invocation under the Tier 3 gate, NOT vendored scripts). **Tier 3** (corrected from eval-list authoring's Tier 1 estimate per §4.4.3 step 2 — eval-list misclassified §V as `read-only` by conflating SKILL.md's content with runtime tool-scope; observed runtime scope dominated by cross-agent SaaS egress). Measured runtime context cost via tiktoken cl100k_base: SKILL.md body **1068 tokens** (recorded in frontmatter; well under 5K-preferred per AGENT_SKILLS.md §3B, no archive justification required — no companion files vendored). Author CodeRabbit (`@coderabbitai`), first-party. Fit: complements the in-house `/codex-review` loop (different cross-perspective AI review on the same diff surface); may follow the §G → §T → §H pipeline as a final external second-opinion review when stakes justify the cross-agent egress; ships byte-identical with the existing `.coderabbit.yaml` repo-root config (Renovate-style advisory comments via the CodeRabbit GitHub App vs. on-demand CLI invocation via §V — distinct surfaces, shared CodeRabbit account). **Framework lift this round (codified in AGENTS.md):** §4.5.2 step 4 grew a **new Tier 3 handling rule** with three flavor-specific prompt templates (cross-agent/network egress; external-write state mutation; triple-compound combining both — only the (i) cross-agent template fires for §V, (ii)+(iii) pre-authored as scaffolds to avoid doctrine-by-PR pressure at first adoption per Codex R2-A-2). The Tier 3 prompt MUST disclose: cross-agent target identity + endpoint; data-egress preview (file paths + diff content + auth-token scope); per-invocation user-approval gate (no Phase A / Phase B split — there is no read-only summary mode that can run gate-free at Tier 3); explicit no-cascade rule (approval for one §V invocation does NOT carry to a subsequent invocation in the same session). The loader `ToolScope` Literal was extended with the new triple compound `shell-execute+network+cross-agent` (`tools/forge-skills-mcp/src/forge_skills_mcp/frontmatter.py`) + matching unit test in `tests/test_frontmatter.py` (8/8 pass; ruff-clean). The canonical compound ordering rule (severity-ascending: shell-execute → network → cross-agent) is codified in `research/agents/AGENT_SKILLS.md` §2E. **AGENTS.md §4.5.1 trigger row** added: triggers on explicit `Use CodeRabbit / Run coderabbit / Review with CodeRabbit` requests, or when the agent judges the diff stakes warrant a third cross-perspective review beyond Claude + Codex (≥ 100 lines touched in security-adjacent paths AND §H+§G has unresolved findings); explicit negative trigger ("Do NOT trigger for general LLM review, simple style/lint, or any diff under 10 lines"); mandatory transcript rationale logging (`[§V pre-gate rationale: ...]`) before the Tier 3 prompt fires. **AGENTS.md §4.5.3 §V Policy Gates Index** entry added with **4+ gates**: (1) per-invocation user-approval gate (Tier 3, no cascade); (2) data-egress disclosure (file paths + auth-token scope + endpoint); (3) auth-token minimum-scope verification (CodeRabbit CLI authenticated with read-only review scope, no admin scopes; attestation captured in adoption commit body + each quarterly §4.4.5 review); (4) CodeRabbit upstream-version-divergence check at quarterly review (skill SHA pin vs. published CLI semver). **Plugin-marketplace vs forge-skills disambiguation:** the forge-skills slug `coderabbitai/code-review` is the **canonical Tier-3-gated path** for invoking CodeRabbit from forge-works sessions; the Claude Code plugin-marketplace skill (`coderabbit:code-review`) does NOT carry §4.5.3 §V gate doctrine — invoking it directly would bypass the Tier-3 prompt. The agent MUST use `mcp__forge-skills__get_skill('coderabbitai/code-review')` for any CodeRabbit-review invocation and emit two transcript lines IMMEDIATELY before the `coderabbit review` subprocess: (a) provenance marker `[§V provenance: forge-skills | plugin-marketplace | direct-cli]`; (b) machine-checkable evidence `[§V evidence: tool=<exact-tool-id> args-sha256=<hex>]` where args-sha256 is RFC 8785 JCS canonicalization + SHA-256 of the **parsed JSON value tree** captured directly from the MCP tool-call argument object (NOT a language `str()`/`repr()` representation — typed JSON values preserved at IEEE 754 precision per Codex R5-B-1). First-3-invocations audit per §4.4.5 verifies marker/evidence consistency AND recomputes the hash from the recorded args; mismatch FAILS the audit. **Vendor concentration AFTER this adoption: 4 ToB + 1 Anthropic + 1 OpenAI + 1 obra + 1 CodeRabbit = 4/8 (50%)** — ToB drops from 57% (4/7) to 50% (4/8) via §V's vendor-diversification effect; CodeRabbit joins as the 5th distinct vendor. The §J R3-B-2 ToB cluster gate (file backlog entry before next ToB adoption when concentration is strictly > 50%) does NOT fire because §V is non-ToB; as-of §V adoption (concentration 4/8 = 50.0%, exactly at the strict threshold boundary), the gate remains armed for any future 5th ToB pick. **Correction note (added 2026-05-18 per Codex §A Round-2 Section B Finding B-R2-2 — the "remains armed" framing was forward-looking from §V's adoption state but conflicts with strict `> 50%` semantics after §A's adoption dropped concentration to 4/9 = 44%):** under strict `> 50%` semantics, a hypothetical 5th ToB adoption at 5/10 (50.0%) does NOT trigger the gate; the gate activates only when concentration is strictly above 50% (e.g., 6/11 = 54.5%). Current post-§A status: gate INACTIVE at 4/9 = 44%. See `research/agents/evaluation_list.md` budget ledger and §A Decision Record Notes for the current state. Adoption budget post-landing: **8 / 30** (per §4.4.4 soft cap). Body byte-identical to upstream verified via Python byte-compare; SHA `a81eb76a1539e4a3f2b5c6fc133849124e72d303` captured in adoption commit. The §V adoption was hardened through a **6-round sliced Codex feedback loop** — the longest convergence path in the agents-evaluation_list loop family to date — producing **50 findings (11H/27M/4L)**, 100% applied; loader 8/8 pass throughout. R1 surfaced 9 framework-lift findings (initial Tier 3 prompt scaffold; plugin-marketplace disambiguation; cross-agent boundary criterion). R2 added 8 findings refining provenance signal contract + auth-token attestation ledger (CHANGELOG path chosen over rejected `research/agents/attestations/` because `/research/` is gitignored). R3 added 9 findings introducing machine-checkable invocation evidence (marker/evidence consistency rule) + attestation-ledger retention rule on removal. R4 added 9 findings codifying RFC 8785 JCS canonicalization + mandatory auditor recomputation step + atomic-quad coupling on removal (closure-entry + step 3 triad same-commit). R5 added 9 findings (3H/6M) with two-layer atomic-quad enforcement (pre-commit hook + CI/branch-protection backstop) + typed JSON value capture + grandfather criterion. **R6 (Section B reached SOLID — first solid-section verdict across 6 rounds)** added 6 findings (3H/3M/0L): regex ReDoS 3-guardrail protection (100ms per-match timeout + load-time pattern rejection of nested unbounded quantifiers + 8KB input-length cap), sidecar tombstone 3-mitigation abuse protection (verification-before-honor + append-only history + SHA-strict scope), tight-coupling rationale schema (inseparability proof + breakage-if-split + rejected alternative + anti-boilerplate substring-match), §4.5.1 §V row drift checks (batched preflight + 1-hour cache + advisory-on-failure with hard-drift > 1000 lines escalating to BLOCKING), §4.5.3 Gate 4 per-invocation `no_prefill` freshness rule (mandatory transcript classification line `[§V mode: chained|ad-hoc; Gate 4 prefill: §H|no_prefill]`), §4.5.3 Gate 3 control #4 override governance (dedicated Notes subsection + fixed-field schema + tightening-mid-quarter / loosening-mid-quarter asymmetry). **Loop-methodology lesson captured:** sliced Codex review loops do NOT naturally converge — Codex always finds SOMETHING; the loop ends when the agent/user decides to ship. Stopping heuristic: **ship at first SOLID-section verdict** unless the skill's risk profile genuinely warrants more depth. §V was initially pushed to R8 (73 findings; R7 + R8 added AB-027 through AB-033 backlog dependencies — ~weeks of engineering work per adoption); retrospective analysis identified R6 as the optimal stopping point (first SOLID section, finding-count trough, severity profile shifted from "missing doctrine" to "deploy tool X / pin version Y / calibrate threshold Z" — backlog territory rather than doctrine territory). R7 + R8 doctrine edits and artifacts were rolled back; only R1-R6 findings shipped. Pattern captured as `[[feedback_loop_stop_signal]]` memory for future adoption rounds: §V (first Tier 3, cross-agent SaaS egress) at R6 vs. §J (Tier 1) at R3 represents the typical Tier-keyed convergence depth. **Decision Record additions (in `research/agents/evaluation_list.md`, gitignored):** §V full record at line 709; budget ledger updated to 8/30 at line 224; §V Notes section with R1-R6 doctrinal blocks (plugin-marketplace disambiguation, provenance signal contract, machine-checkable invocation evidence, RFC 8785 JCS canonicalization, typed JSON value capture, no_prefill freshness, override governance) all preserved. One backlog item filed: **AB-026** — per-operator first-use attestation for contributor handling (deferred until a contributor actually joins; forge-works is currently solo-developer per `git config`). Round-1+2+3+4+5+6 audit trail at `research/feedback_loops/agents-evaluation_list/20260517T180230Z/` (local-only, gitignored; 60 R1-R6 artifacts retained; R7-R8 artifacts removed during rollback). **Adoption-time §3.1 minimum-scope attestation (Codex §V R1-B-3 verification control):** (i) CodeRabbit CLI authenticated with minimum-scope token (read-only review access, no admin scopes); (ii) token not committed anywhere in repo (verified via `gitleaks` + `detect-secrets`); (iii) dashboard URL `https://app.coderabbit.ai/settings/tokens` (captured for stability re-confirmation at each quarterly §4.4.5 review)
- **Authentication Frontend** (Action Plan 5)
  - Login page at `/login` with LoginForm component
  - Register page at `/register` with RegisterForm component
  - Next.js middleware for route protection
  - AuthGuard component for client-side protected routes
  - AuthButton integrated in dashboard header
  - Redirect logic for authenticated/unauthenticated users
- **ADR `docs/decisions/RELEASE_TOOLING.md`** — captures the Changesets → Conventional Commits + release-please decision, evaluated against eight factors (flow methodology, branch strategy, semver, cadence, industry standards, best practices, stack composition, vision)
- **PR status lifecycle labeler** — 4-state convention `pr status:{opened,review,merged,closed}` mirrored into `.github/labels.yml` (so `labels-sync.yml` keeps the repo consistent), plus a new `.github/workflows/pr-status-labeler.yml` that auto-transitions on `pull_request` events (opened/reopened/converted_to_draft → opened, ready_for_review/review_requested → review, closed → merged|closed). "Blocked" stays in the orthogonal `status:blocked` work-state label — one source of truth. Renovate's `addLabels: ["pr status:opened"]` (in `.github/renovate.json5`) seeds the lifecycle on every dependency PR — using `addLabels` instead of `labels` so packageRules' major-bump override doesn't drop it
- **Checkov IaC scan job in `ci.yml`** — new `checkov` job runs the [Checkov](https://github.com/bridgecrewio/checkov) OSS CLI directly (NOT the Bridgecrew SaaS App) against `kubernetes`, `dockerfile`, and `github_actions` frameworks. Findings export to SARIF and upload to the GitHub Security tab via `github/codeql-action/upload-sarif`. First-pass uses `soft_fail: true` to baseline the existing footprint without breaking CI; tightening per-severity policy follows triage. Wired into the `ci-success` aggregate. Adopted following the framework in `research/github_apps/GITHUB_APPS.md` §2A (OSS CLI preferred over Marketplace App when both exist) — complements the existing CodeQL job on the disjoint infra-vs-application-code surface and goes deeper than Snyk IaC on policy breadth (1000+ built-in policies vs Snyk's narrower set)
- **`codecov.yml` repo-root config** — Codecov was already uploading via `codecov/codecov-action@v4` (commit `bd7e221`) but operating on Codecov's defaults: no flag definitions, no ignore list, no PR-comment layout, no threshold policy. New `codecov.yml` codifies: per-flag path mappings (`backend → src/backend/app/`, `frontend → src/frontend/src/`), carryforward semantics (a missing flag upload uses the previous value rather than collapsing to 0%), ignore list for tests/vendored/generated/migrations/infra/docs/research, PR-comment layout, and **advisory-only thresholds for the first rollout** (`informational: true` on both project and patch status — Codecov will post comments and Check Runs but will not fail PRs until baselines stabilize). After ~2 weeks of data we flip the informational flags off and promote the Codecov Check Run to Required in branch protection. Also fixed the backend coverage upload step in `ci.yml` which was missing `flags: backend` (frontend already had its flag) — backend coverage was uploading unflagged so it didn't apply to the `backend` flag rules. Adopted per `research/github_apps/evaluation_list.md` (Decision Record: Codecov)
- **Codecov Test Analytics enabled for backend (Pytest) + frontend (Vitest)** — separate feature from coverage; tracks flaky tests, slow tests, and recent failure patterns across PRs. JUnit XML output added to both runners: `pytest --junitxml=junit.xml` in the backend test step; `vitest.config.mts` `test.reporters` block now emits `['default', ['junit', { outputFile: 'junit.xml' }]]` when `process.env.CI` is set (local dev keeps the plain default reporter). Two new upload steps in `ci.yml` use `codecov/test-results-action@v1` (the dedicated Test Analytics action, distinct from `codecov-action`), each scoped to its flag and gated by `if: ${{ !cancelled() }}` so failure data still uploads when tests fail (the whole point of analytics) but not when a job is intentionally cancelled
- **Sentry SDK wired into FastAPI backend** — `sentry_sdk.init()` called before `FastAPI()` instantiation in `src/backend/app/main.py`; FastAPI integration auto-enables because `fastapi` is already in the dep tree. Init is gated on `SENTRY_DSN`: no-op when the env var is unset (local dev default), so the SDK ships dormant. Reads `SENTRY_DSN`, `SENTRY_ENVIRONMENT` (falls back to `ENVIRONMENT`), and `SENTRY_RELEASE` (falls back to `app_version`) from `app.core.config.settings`. `send_default_pii=False` is set explicitly — auth headers and JWTs would otherwise leak into events. Validated end-to-end against the `forge-works-backend` Sentry project (org `adamatdevops-hs`, region `us.sentry.io`) on 2026-05-14 via a temporary `/sentry-debug` endpoint raising `ZeroDivisionError`; the event surfaced as `FORGE-WORKS-BACKEND-1` with the Sentry middleware visible in the traceback, then the debug endpoint was reverted ([ef375f3], [1aef655]). Production cluster wiring deferred to AB-021 (blocks on AB-002 + backend Deployment manifest)
- **`.coderabbit.yaml` repo-root config** — pre-emptive doctrine codification for the CodeRabbit App (Decision Record in `research/github_apps/evaluation_list.md`; App install pending). Five pre-install gates baked in: (1) `request_changes_workflow: false` prevents formal "Request Changes" reviews from blocking merges; (2) no commit-mutating features used → CodeRabbit can only post advisory comments; (3) no `labels:` block → no race with `pr-status-labeler.yml` / `labels-sync.yml`; (4) default bot identity (`@coderabbitai`) preserves §3C "bot must not impersonate human"; (5) training-data opt-out remains a one-time dashboard verification before install (documented in the file header). Path filters scope review to `src/**` only (excludes lockfiles, generated artifacts, tests, migrations, infra, docs, research). Per-directory `path_instructions` for backend/frontend/flink-jobs/.github/workflows enforce project doctrine (RichFunction lifecycle in Flink, SHA-pin convention in workflows, no `any` in TS, etc.). Per-language linters that already run in CI (ruff/eslint/hadolint/yamllint/markdownlint/gitleaks) are disabled in `tools` block to avoid duplicate noise on PR comments — CodeRabbit's value is semantic/architectural review, not re-running tools we already gate on
- **`forge-skills` MCP loader (AB-022 Phase 1)** — in-house stdio MCP server at `tools/forge-skills-mcp/` that serves vendored skills from `.skills/<vendor>/<slug>/SKILL.md` identically to Claude Code and Codex CLI. Replaces upstream `claude-skills-mcp` v1.0.0 which was broken on 2026-05-15 (silent backend no-start; remote mode unimplemented). Stack: Python 3.11+, FastMCP, Pydantic v2, PyYAML. Exposes 4 tools: `list_skills(tier_max)`, `get_skill(slug)`, `find_skills(query, tier_max)`, `loader_health()`. Server-side doctrine enforcement: 40-char hex SHA validation on `source-sha` (per `AGENT_SKILLS.md` §2D); categorical rejection of `tool-scope: git-write` or `workflow-write` (per §2E); path/frontmatter consistency check on every scan. 19 tests pass (unit + integration); ruff-clean. Documented at `tools/forge-skills-mcp/README.md`. `AGENTS.md` §4.4 swapped `claude-skills-mcp` → `forge-skills` as the primary dual-agent loader. Phase 1 is file-vendored only; runtime fetch lands in Phase 3 per `roadmap/ACTION_PLAN_SKILL_LOADERS.md`
- **AGENTS.md §4.5 "Skill invocation triggers — runtime rules for adopted skills"** — new doctrine section codifying when agents MUST consult adopted skills via `mcp__forge-skills__get_skill`. Before §4.5 existed, adoption only made a skill discoverable via `list_skills`; whether the agent invoked it on a given task depended on per-session judgment, producing inconsistent behavior. §4.5 codifies trigger → skill mapping for every active adopted skill (4 today: §F, §AF, §G, §H), with a compact §4.5.1 trigger table, a §4.5.2 6-step invocation pattern (including Tier-2 user-confirmation gate for shell/write phases, with neutral 3-option prompt + ambiguous-response handling), a §4.5.3 Policy Gates Index that lifts load-bearing policy gates from `evaluation_list.md` Notes into always-loaded doctrine (e.g., §H's `issue-writer` Tier-3 forward-reference gate, the observed-vs-enforced binary distinction, the same-model bounded-subagent carve-outs for `function-analyzer` and `adversarial-modeler`), a §4.5.4 pairing doctrine documenting both chained (§G→§H) and lifecycle-bookend (§F+§AF) patterns, a §4.5.5 update procedure tying §4.5.1 row + §4.5.3 entry maintenance atomically to `evaluation_list.md` updates (with the audit-gap acknowledgment that transcript-citation enforcement is observance-based, AB-025 backlog filed), and a §4.5.6 anti-pattern list (8 items). Two callsite edits land atomically: §4.4.3 step 3 (Decision Record completion) gains "+ add §4.5.1 row + §4.5.3 entry" substep; §4.4.6 removal procedure gains "+ remove §4.5.1 row + §4.5.3 entry" as step 3. The §4.5 doctrine was hardened through a 3-round Codex feedback loop (R1: 9 findings 3H+4M+2L, R2: 7 findings 2H+3M+2L, R3: 2 findings 1M+1L — convergence visible in the finding-count trajectory). Notable doctrine-level decisions made during the loop: SHOULD→MUST modal hardening (R1 F1) to match §3.6 strictness; Codex review-profile fallback routes to direct vendored-file Read (R1 F6 + R2 F2) rather than to AGENT_SKILLS.md framework doctrine; Tier-2 Phase A/Phase B split with explicit boundary rule distinguishing skill-content reads (admin) from workload-input reads (data) (R1 F2 + R2 F1 + R3 F1); 7-condition exception eliminated in favor of a single Codex review-profile case (R2 F5). Round-1+2+3 audit trail at `research/feedback_loops/agents-skill-invocation-triggers/20260516T193023Z/` (local-only, gitignored). AGENTS.md grew from 290 to 389 lines (+99 lines)
- **Seventh vendored skill: `trailofbits/modern-python` — fourth Trail of Bits adoption (crosses 50% vendor-concentration threshold for the first time)** — adopted via the AGENTS.md §4.4.3 7-step process; first adoption today **without a Tier 1→2 correction at adoption review** (§H, §D, §T all required Tier corrections; §J's eval-list Tier 1 estimate matched observed scope). Vendored at `.skills/trailofbits/modern-python/` (SKILL.md + 11 byte-verbatim companion files: 9 reference docs in `references/` — `dependabot.md`, `migration-checklist.md`, `pep723-scripts.md`, `prek.md`, `pyproject.md`, `ruff-config.md`, `security-setup.md`, `testing.md`, `uv-commands.md`; 2 templates in `templates/` — `dependabot.yml` (Dependabot v2 config with 7-day cooldown for supply-chain hardening) + `pre-commit-config.yaml` (pre-commit skeleton with `<latest>` version placeholders for user to fill); inert YAML config skeletons for user copy-paste, NOT skill-executed) from `github.com/trailofbits/skills` @ SHA `a56045e9ae00b3506cacefea0f672aab0a1a6e3c` (same canonical SHA as §F + §G + §H — single supply-chain root for all 4 ToB adoptions; SHA-pinning is the per-skill protection). Audited 2026-05-17: ✅ no encoded content; ✅ no transitive `load skill X` refs; ✅ no cross-agent invocations; ✅ no `claude -p` / SDK subprocess paths (the §2E "Bundled-script same-model self-invocation" clause is non-applicable); ✅ embedded bash snippets across SKILL.md + 9 references/\*.md (`uv init`, `uv add`, `uv sync`, `prek install`, `uvx cookiecutter gh:...`, etc.) are documentation/user-recommendations, NOT skill-executed at runtime. **Tier 1 `read-only + transform`** (advisory only — skill reads existing config like pyproject.toml/requirements.txt/setup.py + proposes new configuration via standard Edit tool; user reviews per §3.2 advisory-only). NO shell-execute, NO repo-write by the skill, NO network within §J's contract — the upstream `uvx cookiecutter gh:trailofbits/cookiecutter-python` example IS a network operation but is explicitly fenced OUT of §J's tool-scope contract per Codex R2-B-2 (governed by AGENTS.md §3.2 + §6 per-invocation rules with explicit approval-trigger surface per Codex R3-B-1: host Bash prompt when available; conversational approval + recorded transcript otherwise). Measured runtime context cost via tiktoken cl100k_base: SKILL.md body 2614 tokens; worst-case read-into-context archive = **14789 tokens** (2614 + 12175 across 11 companions). Above 10K hard-justify threshold per §3B — justified by skill being Python-tooling-phase-only (not always-loaded; only fetched when user is configuring a new Python project, doing pyproject.toml work, migrating from pip/poetry/mypy/black, or asking about Python tooling). Same justification class as §G/§H/§D. Author Trail of Bits, first-party. Fit: daily-driver value across the backend (FastAPI src/backend/) + 3 normalizer deployments (src/normalizer/{k8s,terraform,github-actions}/) + forge-skills-mcp loader (tools/forge-skills-mcp/) + webhook-gateway + job-dispatcher — high invocation frequency. Compatibility: forge-works targets Python 3.11+ in src/backend/pyproject.toml `requires-python = ">=3.11,<3.13"`, matches the skill's "modern Python" baseline. **AGENTS.md §4.5.1 trigger row** added: triggers on Python project tooling / configuration / pyproject.toml work / migration from legacy tooling / PEP 723 scripts; explicit negative trigger ("Do NOT trigger for Python BUG-fixing, code-review, or runtime debugging"). **AGENTS.md §4.5.3 §J Policy Gates Index** entry added with **5 gates** (advisory-only at Tier 1; documentation-vs-executed bash snippet distinction; templates as inert skeletons; stack-divergence guard broadened to cover ALL tooling switches §J recommends — mypy→ty, pre-commit→prek, pip→uv, flake8/black/isort→ruff, etc.; §2E non-applicability). Adoption budget post-landing: **7 / 30** (per §4.4.4 soft cap). **Vendor concentration: 4 Trail of Bits + 1 Anthropic + 1 OpenAI + 1 obra (community) — ToB crosses 50% threshold.** Operationalized cluster controls codified per Codex R2-B-1 + R3-B-2 + R3-B-3: quarterly cluster re-audit with two-stage triage protocol (Stage 1 = changed-file summary on adopted-skill subtrees + shared executables/templates; Stage 2 = full changed-paths review only on high-risk file changes); next-ToB-adoption gate applies regardless of candidate score; commit message records prior explicit user approval (does NOT constitute approval). Body byte-identical to upstream verified via Python byte-compare (body sha256 `f587b51a98cf5759...`); 11 companion files byte-identical via shasum at adoption. The §J adoption was hardened through a **3-round sliced Codex feedback loop** with multiple methodology firsts: (1) **First sliced-review use** — R1's mono-prompt argv-stall (10K-char prompt as single argv caused 42-min hang at 0.10s CPU, zero network) led the user to direct sliced-review pattern (3 sections, embedded content, stdin pipe) for R2 onward; (2) **R2 produced 6 findings (0H/4M/2L)** — 5 of 6 were doctrine improvements (atomic co-edit pairs expanded to triple, §4.5.1 negative-trigger doctrine, §4.5.2 third gate-semantics tier, §4.5.6 worst-case counting rule, evaluation_list.md operationalized cluster controls); 6th was the §J-specific network out-of-scope fence; (3) **R3 produced 8 findings (1H/6M/1L)** including the loop's first HIGH (§4.4.6 removal procedure restructured to mirror §4.4.3 adoption atomic co-edit triad — closes drift loophole on de-adoption) + 2 prior-art catches (§H row grew negative trigger per its own new R2-C-1 doctrine; §T context-cost corrected 4127→4076 per the new R2-C-3 counting rule which correctly excludes 51-token inert `agents/openai.yaml` metadata). All 16 findings applied (1 with partial counter — R3-A-2 specific claim about §AF was based on prompt mis-characterization, but the underlying doctrinal proposal landed as the §4.5.2 "Frontmatter authority rule"). **Framework additions this round (codified in AGENTS.md):** §4.4.3 step 3 atomic co-edit triad (a/b/c) extended with Counts/ledger sync; §4.4.6 step 3 restructured to match; §4.5.1 "Negative-trigger doctrine" paragraph; §4.5.2 step 4 Tier-1 split into `read-only` vs `read-only+transform` sub-rules; create-vs-edit operation-surface test for tier classification; "Frontmatter authority rule" for tier-handling ambiguity; §4.5.3 §J Gates Index entry (5 gates); §4.5.6 anti-pattern 1 "Worst-case counting rule" sentence + updated 7-skill arithmetic. **Decision Record additions (in evaluation_list.md, gitignored):** §J full record; budget ledger + "Counts" block both at 7/30; §J vendor-concentration paragraph with operationalized 3-rule cluster controls (re-audit + AB-NNN gate + per-skill SHA-pinning); §T context-cost line corrected with full provenance. Round-1+2+3 audit trail at `research/feedback_loops/agents-evaluation_list/20260517T095336Z/` (local-only, gitignored) with full per-section prompt+response+reconciliation triples plus aggregated R2 + R3 summaries
- **Sixth vendored skill: `openai/security-threat-model` — first OpenAI-first-party adoption + first three-step security-pipeline pairing (§G → §T → §H)** — adopted via the AGENTS.md §4.4.3 7-step process; first OpenAI-published skill landed under the forge-skills loader, and the first adoption since §D's Round-6 §2E framework lift that does NOT extend the framework (no new carve-outs, no doctrine amendments). Vendored at `.skills/openai/security-threat-model/` (SKILL.md + 4 byte-verbatim companion files: `agents/openai.yaml` (OpenAI runtime UI metadata `interface: { display_name, short_description, default_prompt }` — NOT a Claude Code subagent definition like §D's `agents/*.md`; inert for Claude Code/Codex CLI at runtime), `references/prompt-template.md` (LLM system prompt + output contract), `references/security-controls-and-assets.md` (asset categories + control-types checklist), `LICENSE.txt`) from `github.com/openai/skills` @ SHA `c25113bf4c64c8dba6bfe61acf06051d79aa43f6`. **Vendor structure discovery:** OpenAI uses `.curated/<slug>/` path (vs Anthropic's `skills/<slug>/` for §D and Trail of Bits' `plugins/<name>/skills/<name>/` for §F+§G+§H) — likely signals "production-curated" subset within their repo. Audited 2026-05-17: ✅ no encoded content; ✅ no transitive `load skill X` refs (the 2 `references/*.md` are companion-file links, not cross-skill refs); ✅ no cross-agent invocations; ✅ no script execution / `claude -p` / SDK calls (the single grep hit on "subprocess" in references/prompt-template.md L133 is a thematic threat-category mention — "sandboxing/isolation, privilege boundaries, subprocess execution" — NOT an actual subprocess call); ✅ no executable shell/python/node snippets; ⚠️ **NO framework carve-outs required** — first adoption since the Round-6 §2E "Bundled-script same-model self-invocation" amendment that doesn't extend the framework further; `agents/openai.yaml` is inert metadata (NOT a subagent), no shell-execute surface, no self-invocation paths. **Tier 2** (corrected from eval-list authoring's Tier 1 estimate per §4.4.3 step 2; upstream SKILL.md workflow step 8 writes single artifact `<repo-or-dir-name>-threat-model.md` → repo-write scope per AGENT_SKILLS.md §2E → Tier 2). **Effective tool grant: parent-session inherited** — upstream SKILL.md declares NO `allowed-tools` frontmatter, same "observed vs. enforced" doctrine as §H/§D (host-doctrine bounds, not loader-enforced allowlist). Tool scope is the narrowest among Tier 2 adoptions so far: ONE markdown file per invocation, no edits to existing files, no iterative writes, no shell ops. **Built-in upstream user gate:** SKILL.md workflow step 6 explicitly states "Pause and wait for user feedback before producing the final report" — pre-loads the Phase A → Phase B transition and aligns natively with our §4.5.2 step 4 Tier-2 confirmation doctrine. Measured context cost via tiktoken cl100k_base: SKILL.md body 1022 tokens; worst-case archive (SKILL.md + 3 read-by-Claude companions: agents/openai.yaml 51 + references/prompt-template.md 2676 + references/security-controls-and-assets.md 378) = **4127 tokens** (recorded in frontmatter). **Under 5K-preferred threshold per AGENT_SKILLS.md §3B — no justification required (unlike §G 7549, §H 8087, §D 18659 which all needed worst-case-archive justifications).** Author OpenAI, first-party. Fit: Engine Phase 6 architectural threat-model authoring; Sentry release-webhook surface security review; multi-tenancy design exercises. **AGENTS.md §4.5.1 trigger row** added: triggers ONLY when the user explicitly asks to threat model a codebase or path, enumerate trust boundaries / abuse paths / mitigations, or perform AppSec threat modeling; explicit negative trigger ("Do NOT trigger for general architecture summaries, code review, or non-security design work") — first row to include negative-trigger language. **AGENTS.md §4.5.3 §T Policy Gates Index** entry added with **4 gates**: Tier-2 user-confirmation gate (Phase A reading + reasoning vs Phase B single-file write, with the built-in upstream step-6 pause as the natural transition), single-artifact write-surface gate (filename pattern must be cited in the §4.5.2 step 4 prompt), `agents/openai.yaml` inert-metadata documentation, no-shell-execute / no-self-invocation gate (the §2E "Bundled-script same-model self-invocation" clause is non-applicable here). **AGENTS.md §4.5.4 Pairing doctrine** grew from "Two pairing patterns" to "Three pairing patterns": new **Three-step security-pipeline pairing — §G → §T → §H** between the existing §G → §H chained pairing and §F + §AF lifecycle bookend. The new pairing is explicitly framed as **agent-level runtime orchestration**, NOT skill-level composition — unlike §G → §H (where §H's vendored `methodology.md` includes a `if audit-context-building skill is available` conditional that invokes the §2E "Cross-skill composition carve-out"), §T's vendored SKILL.md does not reference §G or §H at all. The agent recognizes the three independent triggers across the security pipeline and orchestrates them as a chain when the workload calls for it; no carve-out applicability claim needed (clarification added Round-1 of the Codex loop per Finding R1-2). **AGENTS.md §4.5.6 anti-pattern arithmetic** updated for 6-skill set: ~14.0K tokens for all SKILL.md bodies combined; ~40.3K tokens worst-case read-into-context archive. Adoption budget post-landing: **6 / 30** (per §4.4.4 soft cap; vendor concentration now 3 Trail of Bits, 1 Anthropic, 1 OpenAI, 1 obra community). Body byte-identical to upstream verified via Python byte-compare (body sha256 `f54ff21c5cd11816...`); 4 byte-verbatim companion files verified via shasum at adoption. The §T adoption was hardened through a **3-round Codex feedback loop** (R1: 2 findings 1M+1L → R2: 1 finding 1L → R3: 0 findings `solid` — **fastest convergence to date** in the agents-evaluation_list loop family, 3 findings total vs §D's 7, §H's 14+). Notable doctrine-level decisions during the loop: byte-verbatim wording rewritten to separate "body byte-identical to upstream" (SKILL.md after our forge-skills frontmatter wrapper is prepended) from "byte-verbatim companion copies" (true whole-file integrity for agents/openai.yaml + references/\*.md + LICENSE.txt) (R1-F1); §G→§T→§H pairing reframed from misapplied §2E "Cross-skill composition carve-out" citation to explicit "agent-level runtime orchestration, NOT skill-level composition" framing (R1-F2); body byte-count figure removed in favor of hash-as-authoritative-identity-proof after frontmatter-stripping methods produced inconsistent counts (R2-F1). Round-1+2+3 audit trail at `research/feedback_loops/agents-evaluation_list/20260517T085410Z/` (local-only, gitignored)
- **Fifth vendored skill: `anthropics/skill-creator` — first Anthropic-first-party + first meta-skill + first Tier 2 with intra-skill Tier-3 escalation** — adopted via the AGENTS.md §4.4.3 7-step process; first Anthropic-published skill landed under the forge-skills loader, and the framework's most complex adoption to date. Vendored at `.skills/anthropics/skill-creator/` (SKILL.md + 17 companion files: 3 bundled same-model subagent defs in `agents/` — `analyzer.md`/`comparator.md`/`grader.md`; `assets/eval_review.html` HTML template; `eval-viewer/{generate_review.py, viewer.html}` local loopback HTTP server for eval result review; `references/schemas.md` JSON schema docs; 8 executable Python scripts in `scripts/` — `__init__.py`, `aggregate_benchmark.py`, `generate_report.py`, `improve_description.py`, `package_skill.py`, `quick_validate.py`, `run_eval.py`, `run_loop.py`, `utils.py`; plus `LICENSE.txt`) from `github.com/anthropics/skills` @ SHA `f458cee31a7577a47ba0c9a101976fa599385174`. Audited 2026-05-17: ✅ no encoded content; ✅ no transitive `load skill X` refs; ⚠️ THREE distinct cross-reference patterns flagged + handled this round, two requiring NEW framework carve-outs: (1) **bundled same-model subagents** — `agents/{grader,comparator,analyzer}.md` shipped WITHIN the skill (not `~/.claude/agents/` registry like §G's `function-analyzer` or §H's `adversarial-modeler`); same Claude model family, child grants ⊆ parent via Task tool, no external API; satisfies the §2E same-model bounded-subagent carve-out via the **broadened condition 4** added this round (allows both registry-installed AND vendored-skill-local agent specs pinned by source SHA; pre-existing condition 4 only covered registry). (2) **`claude -p` subprocess invocations in 3 scripts** — `scripts/run_eval.py` L71-85, `scripts/improve_description.py` L26-45, `scripts/run_loop.py` (orchestrator) invoke `subprocess.Popen(["claude", "-p", ...])` to spawn a fresh Claude CLI session whose tool grants load from `~/.claude/settings.json` (NOT bounded by parent's scope); **does NOT** satisfy §2E bounded-subagent condition 3 (`child grants ⊆ parent`) because the subprocess opens a fresh tool envelope. The §2E **"Bundled-script same-model self-invocation"** clause added this round (Round-6) codifies the new pattern: such scripts default to Tier 3 — per-invocation user approval — even when the parent skill is Tier 2; the clause is **mechanism-agnostic** (covers CLI subprocesses, SDK calls in fresh processes, container-launched model spawns, env-variable-indirected command construction) with a **fail-closed default for inconclusive cases** (added Round-2 of the Codex loop) and a parallel `AGENTS.md §4.4.3 step 1` audit-checklist addition for detecting subprocess + model-client tokens at adoption review. (3) **Local loopback HTTP server** — `eval-viewer/generate_review.py` launches an HTTP server on `localhost:<port>` for eval result review; loopback-only, NOT external network for Tier classification; documented for transparency. **Tier 2 with intra-skill Tier-3 escalation** (corrected from eval-list authoring's Tier 1 estimate per §4.4.3 step 1 + step 2; overall skill operates as Tier 2 `shell-execute+repo-write`, but the three `claude -p` scripts are gated at Tier 3 per the new §2E clause via `AGENTS.md §4.5.3` Gate 2 with explicit gate-sequencing doctrine: Tier-2 Phase-B confirmation FIRST, then per-script Tier-3 approval IMMEDIATELY BEFORE each escalated invocation; approval for one Tier-3 script does NOT carry to another in the same session). Measured runtime context cost via tiktoken cl100k_base: SKILL.md body 7252 tokens; worst-case read-into-context archive (SKILL.md + 5 read-by-Claude companions: 3 `agents/*.md` + `references/schemas.md` + `assets/eval_review.html`) = **18659 tokens** (recorded in frontmatter); full archive including the 8 executable Python scripts + viewer.html + generate_review.py = 50312 tokens, but those execute via shell rather than load into Claude's context window (methodology note per AGENT_SKILLS.md §3B). Above 10K hard-justify threshold per §3B — justified by skill being skill-authoring-phase-only (not always-loaded; only fetched when user engages in skill creation/optimization workflow), same logic as §G (audit-phase) and §H (PR-review-phase). Author Anthropic, first-party (authors of the SKILL.md format itself). Fit: meta-skill for authoring local skills under `.skills/forge-works/<slug>/` — direct hit on §1 Goal #1 (codify project-specific operations); strategic-pair with the in-house forge-skills MCP loader. **AGENTS.md §4.5.1 trigger row** added: triggers on "Create / modify / improve a skill, run skill evals, optimize skill description for triggering accuracy, benchmark skill performance, or package a skill"; may pair with §F (ask-questions-if-underspecified) when the create-a-skill request is itself underspecified. **AGENTS.md §4.5.3 §D Policy Gates Index** entry added with **5 gates**: Tier-2 user-confirmation gate, intra-skill Tier-3 escalation gate (with explicit gate-sequencing for combined Phase-B + Tier-3 flows), bundled same-model subagents carve-out citation, observed-vs-enforced shell-execute caveat, local HTTP server documentation. **AGENTS.md §4.5.6 anti-pattern list** grew from 8 to 9 items (new entry for "Executing a §2E bundled-script same-model self-invocation path without per-invocation Tier-3 user approval" — explicitly calls out Phase-B approval does NOT cascade to Gate 2). Adoption budget post-landing: **5 / 30** (per §4.4.4 soft cap; first Tier 2 + Tier-3 hybrid). Body byte-identical to upstream verified via Python byte-compare (32625 bytes; SHA `0b58e93f8aeb0a23...` for body content); 16 companion files (excluding the synthesized forge-skills frontmatter on SKILL.md + LICENSE.txt + scripts/**init**.py) byte-identical via shasum at adoption. The §D adoption was hardened through a **3-round Codex feedback loop** (R1: 4 findings 2M+2L → R2: 3 findings 1M+2L → R3: 0 findings `solid` — clean convergence in 3 rounds vs §H's 5+ rounds). Notable doctrine-level decisions during the loop: §2E trigger framing rewritten to be mechanism-agnostic with explicit "provable subset" test + fail-closed default (R1-F1 + R2-F1); §2E condition 4 broadened to allow vendored bundled subagents (R1-F2) and calibrated to acknowledge registry entries can be equivalently bounded under matching governance (R2-F3); §4.4.3 step 1 audit-checklist gained a subprocess + model-client scan bullet for §2E enforceability at adoption review (R2-F2); §4.5.3 gate-sequencing paragraph codified for combined Phase-B + Tier-3 flows (R1-F3); §4.5.6 9th anti-pattern explicitly distinguishes Phase-B approval from Gate 2 cascade (R1-F4). Round-1+2+3 audit trail at `research/feedback_loops/agents-evaluation_list/20260517T070135Z/` (local-only, gitignored)
- **Fourth vendored skill: `trailofbits/differential-review` — first Tier 2 adoption** — adopted via the AGENTS.md §4.4.3 7-step process; first Tier 2 skill landed under the forge-skills loader. Vendored at `.skills/trailofbits/differential-review/` (SKILL.md + 4 sibling companion files in the skill dir — `adversarial.md`, `methodology.md`, `patterns.md`, `reporting.md`; sibling layout, NOT a `resources/` subdir as in §G) from `github.com/trailofbits/skills` @ SHA `a56045e9ae00b3506cacefea0f672aab0a1a6e3c` (same canonical repo as §F + §G). Audited 2026-05-16: ✅ no encoded content, ✅ embedded bash blocks are inspectable + scoped to declared diff-review purpose (allowed binaries explicitly listed in the Decision Record: `git` for read-only diff/log/blame/checkout, `gh` for read-only PR view, `find` + `grep` for enumeration/search — no destructive shell ops), ✅ no executable python/node snippets, ⚠️ TWO cross-reference patterns flagged and handled by **two new framework carve-outs added this round** (Round-5 amendment to AGENT_SKILLS.md §2C + §2E): (1) the `adversarial-modeler` Claude Code subagent referenced in SKILL.md is a same-model bounded subagent with fewer tool grants than the parent → satisfies the Round-4 same-model carve-out → in-set delegation, NOT cross-agent; if user's `~/.claude/agents/` lacks `adversarial-modeler` the spawn fails-closed and the agent reads `adversarial.md` manually instead; (2) conditional cross-skill composition with §G `audit-context-building` (methodology.md L9/L17/L178/L181 + SKILL.md L125/L162/L163) — all gated by explicit `if available / if NOT available` graceful-degradation conditionals → satisfies the new **Cross-skill composition carve-out** (Round-5, §2E): composing with another already-adopted skill in our own audited loader is the OPPOSITE of supply-chain expansion. A forward reference to a not-yet-adopted `issue-writer` skill (SKILL.md L137-139 + reporting.md L344/L349-354) includes prescriptive command syntax under an "Integration" heading; Codex Round-5 Finding 3 flagged this as too strong for "documentation-only" classification and surfaced a third doctrinal state — **Conditional load of an UN-adopted target** — that the original §2E carve-out didn't cover. AGENT_SKILLS.md §2E was amended this round with a new clause requiring an explicit per-invocation-user-approval gate (Tier 3 default) in the Decision Record for any conditional invocation of a non-adopted target; §H Notes now explicitly states agents MUST NOT invoke `issue-writer` without user approval until/unless it is adopted via its own §4.4.3 process. **Tier 2** (corrected from eval-list authoring's Tier 1 estimate per §4.4.3 step 1; upstream frontmatter declares `allowed-tools: Read Write Grep Glob Bash` → repo-write + shell-execute scope per AGENT_SKILLS.md §2E). Loader enum extended this round to accept `shell-execute+repo-write` (extends Round-3's `read-only+transform` precedent); `tools/forge-skills-mcp/src/forge_skills_mcp/frontmatter.py` ToolScope literal updated + new test in `tests/test_frontmatter.py` (7/7 pass; no other ruff/test regressions). Measured runtime context cost 1668 tokens (SKILL.md only — what the loader's `get_skill` serves); total vendored archive 8087 tokens worst-case including 4 companion files (`adversarial.md` 1186t, `methodology.md` 1688t, `patterns.md` 1742t, `reporting.md` 1803t) loaded on-demand when SKILL.md's Decision Tree directives are followed. Above 5K-preferred but under 10K hard-justify per AGENT_SKILLS.md §3B — justified by skill being PR-review-phase-only, not always-loaded. Author Trail of Bits, first-party. Fit: pre-commit Decision Record patches, PR review against base SHA, Flink 1.20 → 2.0 version-bump review (Task #24). Pairs with §G `audit-context-building` as the security-review pipeline: §G builds baseline context, §H reviews diffs against it. Adoption budget post-landing: 4 / 30 (per §4.4.4 soft cap). Body byte-identical to upstream verified via Python byte-compare (6596 bytes); 4 companion files byte-identical via shasum. Upstream plugin also ships `agents/adversarial-modeler.md` + `commands/diff-review.md` (slash command) at plugin root — these are NOT vendored as part of §H per §4.4.3 strict reading; user may install separately if desired. Two further AGENT_SKILLS.md §2E doctrine amendments landed in the same PR per Codex Round-5 Findings 1 + 2: (a) cross-skill composition condition 2 ("graceful degradation") tightened — the fallback must produce the same artifact class and preserve core acceptance criteria, not just satisfy "if available / if NOT available" wording; reviewer-check clause added to reject perfunctory fallbacks; (b) new "observed vs. enforced" doctrine paragraph clarifies that `shell-execute` Tier 2 binary lists are auditor-facing observed usage, NOT runtime allowlists, unless the loader provides per-binary enforcement (Phase 1 forge-skills loader does not); Decision Records must explicitly distinguish "observed in upstream content" from "enforced at runtime". §H Tool scope was rewritten accordingly to lead with "Effective shell grant per upstream `allowed-tools: ... Bash`: full Bash tool surface is in scope at runtime — NOT a binary-level allowlist", with git/gh/find/grep labeled as observed (not enforced). Codex Round-5 Findings 4 + 5 (both LOW — same-model carve-out condition 3 wording nit + multi-file layout policy gap) were deferred consistent with the prior-round nit-deferral pattern. Round-5 audit trail at `research/feedback_loops/agents-evaluation_list/20260516T190219Z/` (local-only, gitignored). Loader-runtime note: the live MCP server caches the Python `Literal` enum and will reject `shell-execute+repo-write` until the next Claude Code session restart spawns a fresh server process; disk state is correct + unit tests confirm the enum landing
- **Third vendored skill: `trailofbits/audit-context-building`** — adopted via the AGENTS.md §4.4.3 7-step process; first multi-file vendored skill under the forge-skills loader. Vendored at `.skills/trailofbits/audit-context-building/` (SKILL.md + 3 companion files in `resources/`) from `github.com/trailofbits/skills` @ SHA `a56045e9ae00b3506cacefea0f672aab0a1a6e3c` (same canonical repo as §F). Audited 2026-05-16: ✅ no encoded content, ✅ no transitive `load skill X` refs (the 3 relative refs to `resources/*.md` are companion-file links, not cross-skill), ✅ no cross-agent invocations (§8 references `function-analyzer` subagent which is within Claude's own toolset — bounded subagent spawn per AGENTS.md §4.4.3 spirit, not cross-LLM), ✅ no executable shell/python/node snippets. Tier 1 (read-only / advisory; "pure context building only" per skill's §10 Non-Goals which explicitly excludes vulnerability findings, fix proposals, PoCs, exploit modeling, severity rating). Measured runtime context cost 2133 tokens (SKILL.md only — what the Phase 1 loader serves via `get_skill`); total vendored archive 7549 tokens including 3 companion files (`COMPLETENESS_CHECKLIST.md` 425t, `FUNCTION_MICRO_ANALYSIS_EXAMPLE.md` 4419t, `OUTPUT_REQUIREMENTS.md` 572t) loaded on-demand when the SKILL.md's relative refs are followed. Author Trail of Bits, first-party. Gateway skill for sensitive-code review (normalizer IAM, auth boundaries, Sentry redaction, ML advisory output paths); pairs with the upcoming Engine Phase 6 work. Adoption budget post-landing: 3 / 30 (per §4.4.4 soft cap). All 4 files byte-identical to upstream verified via cmp + shasum
- **Second vendored skill: `obra/verification-before-completion`** — adopted via the AGENTS.md §4.4.3 7-step process; second skill landed under the forge-skills loader. Vendored at `.skills/obra/verification-before-completion/SKILL.md` from `github.com/obra/superpowers` @ SHA `f2cbfbefebbfef77321e4c9abc9e949826bea9d7`. Audited 2026-05-16: ✅ no encoded content, ✅ no transitive `load skill X` refs, ✅ no cross-agent invocations, ✅ no executable shell/python/node snippets (code blocks contain illustrative patterns like `[Run test command]`, not actual commands). Tier 1 (read-only / advisory; upstream's `read-only + transform` simplified to `read-only` at vendoring per loader's §2E enum). Measured context cost 987 tokens (cl100k_base via tiktoken) — within the eval-list 1-2K range. Author Jesse Vincent (obra) is known-community per AGENTS.md §4.4.1 → content review applied per solo-author rule and passed. Pairs with §F (ToB ask-questions-if-underspecified) as bookend skills for agent honesty: ask before starting → verify before claiming done. Adoption budget post-landing: 2 / 30 (per §4.4.4 soft cap). Byte-identical body verified via shasum against upstream
- **First vendored skill: `trailofbits/ask-questions-if-underspecified`** — adopted via the AGENTS.md §4.4.3 7-step process; first skill landed under the forge-skills loader. Vendored at `.skills/trailofbits/ask-questions-if-underspecified/SKILL.md` from `github.com/trailofbits/skills` @ SHA `a56045e9ae00b3506cacefea0f672aab0a1a6e3c` (the marketplace plugin path is canonical; an identical `.codex/skills/` mirror exists upstream). Audited 2026-05-16: ✅ no encoded content, ✅ no transitive `load skill X` refs, ✅ no cross-agent invocations, ✅ no executable shell/python/node snippets (only formatting examples). Tier 1 (read-only / advisory). Measured context cost 884 tokens (cl100k_base via tiktoken) — within 12% of the eval-list ~1K estimate. Decision Record at `research/agents/evaluation_list.md` §F updated with audited SHA + measured cost + canonical-home path. Smoke-tested via `mcp__forge-skills__list_skills` and `get_skill` — both Claude Code and interactive Codex pick the skill up on next `list_skills` call (the forge-skills scanner rescans per-request, no restart needed). Reinforces AGENTS.md §3.6 "don't infer missing requirements"; highest-leverage skill identified in the agents evaluation pass
- **MCP invocation pattern: pre-built `.venv` + direct python (AB-022 fix)** — original `uv run --directory ...` MCP invocation failed under Codex's sandboxed `codex exec` because `uv` writes to `~/.cache/uv/` (outside the workspace) at runtime, which seatbelt blocks; Codex surfaces the failure as `error: "user cancelled MCP tool call"` (misleading — the server never started). Replaced with `.venv/bin/python -m forge_skills_mcp --skills-root <abs path>` + `env = { PYTHONDONTWRITEBYTECODE = "1" }` so the spawned Python performs zero writes at runtime. `.venv` lives inside `tools/forge-skills-mcp/` (gitignored). `--skills-root` is passed explicitly because Codex inherits its own CWD to spawned MCP servers, not the package's directory, so the default cwd-walker can't locate `.skills/`. Same investigation also confirmed Codex 0.130.0 auto-cancels MCP tool calls in `codex exec --profile review` (sandbox=read-only, approval=never) **by design** — no public config key (`always_allow`, `auto_approve`, `tools.default_tools_approval_mode`, per-server allowlist) overrides this; documented as a known constraint in `AGENTS.md` §4.4 preamble and `tools/forge-skills-mcp/README.md` § "Codex sandbox constraint". The `/codex-review` loop is unaffected (it reviews diffs, doesn't need skills); dual-agent skill access holds for interactive development sessions

### Skill Quarterly Attestations

**2026-Q2 (2026-05-20)** — FIRST quarterly attestation pass since framework adoption began 2026-05-16 with §F. The 14 active-adopted skills + 2 deferred-rescore candidates (AB-034 build-review-interface, AB-035 spec-to-code-compliance) attested per AGENTS.md §4.4.5 four-component review (first-3-invocations audit / source-SHA tracking / AGENTS.md conflict check / telemetry-light observation). **Shared-upstream-SHA optimization applied** per §4.4.5 cluster-co-adoption note (codified at §AH adoption): hamelsmu cluster (5 skills @ `febdb335bd65`) — single shared cluster check; ToB cluster (4 skills @ `a56045e9ae00`) — single shared cluster check; 5 standalone-vendor skills checked individually (§A/§AF/§D/§T/§V). **All 14 SHA-current** (verified via gh API: per-cluster path-touch comparison against recorded SHA; byte-identity spot-checked on §AG eval-audit — SKILL.md IDENTICAL at recorded SHA vs upstream `main`; cluster co-adoption SHA semantics held — `febdb335` is 5 commits AHEAD / 0 BEHIND vs last at-path commit `d3a9f0f1`). **No AGENTS.md conflicts** — this same review elevates the inline-output-stays-Tier-1 observation to normative + codifies the Tier-staleness expectation rule (additive doctrine; no carve-out invalidation for existing skills). **First-3-invocations audit deferred for all skills** — framework is days-old (oldest adoption §F 2026-05-16 = 4 days; newest §AL 2026-05-19 = 1 day); the 4-day window is below the minimum sample for invocation-based audit; first-3-audit findings will surface at 2026-Q3 with the first 30d of observed usage. **Telemetry-light observation: all 14 skills in grace period** (no 30-day-no-fires removal triggers possible at 4-day-max adoption age). **Cluster-co-adoption note elevation:** the §4.4.5 provisional cluster-co-adoption optimization (codified at §AH at single-cluster-sample) is empirically VALIDATED this quarter — applied to both hamelsmu (5-skill) AND ToB (4-skill) clusters with zero divergence between per-cluster shared-check semantics and per-skill verification; recommend elevation from provisional to normative at 2026-Q3 if no operational divergence surfaces by then.

**Per-skill attestations** (adoption-chronological per §4.5.5; cluster siblings grouped):

- 2026-Q2 — §F `trailofbits/ask-questions-if-underspecified`: ATTESTED — SHA `a56045e9ae00` byte-current (cluster shared check; latest at-path commit `9f7f8ada` 2026-02-18 predates recorded SHA 2026-05-05); Tier 1 read-only; secrets-exposure none; Notes carve-outs hold (no §3.2/§3.6 conflicts); first-3-invocations audit: PENDING (no observed invocations in 4-day window); 30d-telemetry: grace period (adopted 2026-05-16).
- 2026-Q2 — §G `trailofbits/audit-context-building`: ATTESTED — SHA `a56045e9ae00` byte-current (cluster shared check; latest at-path commit `debfb29c` 2026-04-28 predates recorded SHA 2026-05-05); Tier 1 read-only; secrets-exposure none; Notes carve-outs hold; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-16).
- 2026-Q2 — §H `trailofbits/differential-review`: ATTESTED — SHA `a56045e9ae00` byte-current (cluster shared check; latest at-path commit `540111a5` 2026-04-29 predates recorded SHA 2026-05-05); Tier 2 shell-execute+repo-write; secrets-exposure none (read-only egress via `gh`/`git`); Notes carve-outs hold (§4.5.1 R3-C-1 security-only trigger refinement still applies); Phase-A/Phase-B gate doctrine unchanged; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-16).
- 2026-Q2 — §J `trailofbits/modern-python`: ATTESTED — SHA `a56045e9ae00` byte-current (cluster shared check; latest at-path commit `dc1bc277` 2026-02-13 predates recorded SHA 2026-05-05); Tier 1 read-only+transform; secrets-exposure none; Notes carve-outs hold (the §J R3-A-3 create-vs-edit doctrine extends this quarter with the inline-transcript trichotomy — additive only, no §J Notes amendments needed); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-17).
- 2026-Q2 — §AF `obra/verification-before-completion`: ATTESTED — SHA `f2cbfbefebbf` byte-current (latest at-path commit `48410c7f` 2025-10-17 predates recorded SHA 2026-05-04); Tier 1 read-only+transform; secrets-exposure none; Notes carve-outs hold (lifecycle-bookend with §F); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-16).
- 2026-Q2 — §D `anthropics/skill-creator`: ATTESTED — SHA `f458cee31a75` byte-current (latest at-path commit `b9e19e6f` 2026-04-20 predates recorded SHA 2026-05-09; note upstream restructured from `document-skills/skill-creator/` to `skills/skill-creator/` — `source-canonical` URL frontmatter points to the old path at the recorded SHA, still resolvable); Tier 2 shell-execute+repo-write (intra-skill Tier-3 escalation on 3 scripts); secrets-exposure none; Notes carve-outs hold; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-17).
- 2026-Q2 — §T `openai/security-threat-model`: ATTESTED — SHA `c25113bf4c64` byte-current (latest at-path commit `5c8f1e26` 2026-02-02 predates recorded SHA 2026-05-12; repo is `openai/skills` with path `skills/.curated/security-threat-model`); Tier 2 repo-write; secrets-exposure none; Notes carve-outs hold (Phase-A/Phase-B prompt unchanged); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-17).
- 2026-Q2 — §V `coderabbitai/code-review`: ATTESTED — SHA `a81eb76a1539` EXACTLY-CURRENT (latest at-path commit IS the recorded SHA, dated 2026-05-04; first vendored skill in the framework to be exactly-current at its at-path latest); Tier 3 shell-execute+network+cross-agent; secrets-exposure CodeRabbit CLI auth token (per-invocation user-approval gate enforced); Notes carve-outs hold (R1-R6 calibrated doctrine all preserved: pre-gate eligibility (α/β/γ), regex engine + parser spec, ReDoS guardrails (ε-1/ε-2/ε-3), sidecar regression rules (γ-1/γ-2/γ-3), tombstone abuse mitigations (γ-3-a/γ-3-b/γ-3-c), tight-coupling rationale schema); calibrated security-critical-path list still cites forge-works concrete paths; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-17).
- 2026-Q2 — §A `anthropics/doc-coauthoring`: ATTESTED — SHA `6a5bb06904ab` byte-current (latest at-path commit `00756142` 2025-12-04 predates recorded SHA 2026-05-17); Tier 2 repo-write; secrets-exposure none (Stage 3 Reader Testing uses host's Task tool — inline-generic same-model bounded-subagent per §2E carve-out condition 4c); allowed-input contract `{document content, reader question}` still enumerated; Notes carve-outs hold; AB-029 (multi-adoption same-date squash tertiary authority), AB-030 (atomic-set extension), AB-031 (commit recovery semantics) — all 3 backlog items remain OPEN, queued for 2026-Q3 review (no §A-specific operational pressure surfaced this quarter; defer-stand); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-18).
- 2026-Q2 — **hamelsmu cluster (5 skills @ `febdb335bd65`)**: SHA byte-current verified at cluster level (compared `febdb335` against current upstream `main`: ahead 5 / behind 0; `d3a9f0f1` is the last commit touching `skills/`, dated 2026-03-03 — all 5 subsequent main commits touched other paths only). Byte-identity spot-checked on `skills/eval-audit/SKILL.md` — IDENTICAL at recorded SHA vs current main. Cluster siblings attested in §-letter order:
  - §AG `hamelsmu/evals-skills/eval-audit`: ATTESTED — SHA `febdb335bd65` byte-current (cluster shared check); Tier 1 read-only; secrets-exposure none; sister-reference status (4 adopted: §AJ/§AH/§AK/§AL; 3 still UN-adopted: `build-review-interface`/`evaluate-rag`/`meta-skill`) still accurate; AB-034 build-review-interface remains DEFERRED per this review (see deferred-rescore-confirmed entry below); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-19).
  - §AH `hamelsmu/evals-skills/write-judge-prompt`: ATTESTED — SHA `febdb335bd65` byte-current (cluster shared check); Tier 1 read-only; secrets-exposure none; escape-hatch transcript log doctrine + Likert non-applicability rule + cluster boundary with §AG/§AJ/§AK all preserved; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-19).
  - §AJ `hamelsmu/evals-skills/error-analysis`: ATTESTED — SHA `febdb335bd65` byte-current (cluster shared check); Tier 1 read-only; secrets-exposure none; trace-count 5-mode taxonomy + §AJ → §AH gate-stack inheritance + §AJ → §AL precursor chain all preserved; AB-034 build-review-interface sister-reference status: UN-adopted (unchanged); first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-19).
  - §AK `hamelsmu/evals-skills/validate-evaluator`: ATTESTED — SHA `febdb335bd65` byte-current (cluster shared check); Tier 1 read-only; secrets-exposure none; §AH → §AK SUCCESSOR chain + sample-size 5-mode taxonomy with imbalance guard + 4 metric-discipline rules all preserved; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-19).
  - §AL `hamelsmu/evals-skills/generate-synthetic-data`: ATTESTED — SHA `febdb335bd65` byte-current (cluster shared check); Tier 1 read-only; secrets-exposure none; §AL → §AJ PRECURSOR chain + §AJ re-entry semantics + 4 dimension-discipline rules + 3 anti-pattern enforcement rules all preserved; first-3-invocations audit: PENDING; 30d-telemetry: grace period (adopted 2026-05-19).

**Deferred-rescore-confirmed entries** (per AB-NNN backlog convention; full rescore narrative + trigger evaluation lives at `research/agents/evaluation_list.md`, gitignored):

- 2026-Q2 — AB-034 `hamelsmu/evals-skills/build-review-interface`: DEFERRED-CONFIRMED — trigger (i) NOT fired (no human-review tooling initiative); trigger (ii) THIS REVIEW (re-evaluated Tier-2-new-surface cost vs cluster-completion gate-closure value — verdict UNCHANGED); trigger (iii) PARTIALLY-FIRED via the inline-output-stays-Tier-1 normative elevation this commit + explicit file-CREATE carve-out anticipating build-review-interface as canonical test case; next mandatory review 2026-Q3.
- 2026-Q2 — AB-035 `trailofbits/skills/spec-to-code-compliance`: DEFERRED-CONFIRMED — trigger (i) NOT fired (no blockchain code; no CUE-schema spec-compliance initiative); trigger (ii) THIS REVIEW (re-evaluated Tier-2-new-surface cost — verdict UNCHANGED: domain mismatch + 3 framework-surface lifts + 3 UN-adopted-sister fan-out remain prohibitive); trigger (iii) NOT fired (no smaller bundled multi-artifact precedent); key datapoint feeding this review's Tier-staleness pattern codification (N=6: 4 corrections + 2 deferrals); next mandatory review 2026-Q3.

**This-review framework lifts (codified in AGENTS.md):** (1) **Inline-transcript output is Tier 1** — elevated from provisional observation (§AG R1 B-R1-2) to normative at AGENTS.md §4.5.2 step 4 Tier-1 `read-only + transform` paragraph; R3-A-3 trichotomy now: inline-transcript / file-EDIT / file-CREATE; explicit carve-out for build-review-interface + spec-to-code-compliance type cases. (2) **Tier-staleness expectation rule** — codified at AGENTS.md §4.4.3 step 2 with 5-item audit checklist (file-write directives / bundled sub-agent / bundled slash command / network egress / cross-agent invocation directives); eval-list Tier estimates are pre-adoption working estimates, NOT authoritative; adopters MUST run the upstream surface audit and treat the eval-list Tier as a hypothesis. (3) **Cluster-co-adoption normative elevation candidate** — flagged for 2026-Q3 elevation if no operational divergence surfaces.

### Changed

- **Release process: Changesets → Conventional Commits + manual `git tag` (interim) → release-please (target)** — `RELEASE.md` rewritten to describe the interim manual process; release-please wires in task #23
- **CI Success aggregate now gates on Security job** — `ci.yml` was silently passing CI Success when Snyk failed; added `needs.security.result` to the conditional
- **Language version constraints tightened to match CI** — was producing scan-time drift on the Snyk App (installed 2026-05-12) where Snyk's runners could pick a different interpreter/runtime than CI uses, generating false positives/negatives. `src/backend/pyproject.toml`: `requires-python = ">=3.11"` → `">=3.11,<3.13"` (locks to 3.11.x/3.12.x — Snyk and CI now agree). Root `package.json` and (newly added) `src/frontend/package.json` `engines` blocks: `node` `>=18.0.0` → `>=20.0.0 <22.0.0`, `pnpm` `>=8.0.0` → `>=9.0.0 <10.0.0` (matches `packageManager: pnpm@9.15.0` pin and CI's Node 20). Frontend package previously had no `engines` block at all — Snyk would have defaulted to its own Node version when scanning the workspace
- **`codecov/codecov-action@v4` → `@v5`** across both backend + frontend coverage uploads; passes `token: ${{ secrets.CODECOV_TOKEN }}` explicitly per Codecov dashboard onboarding (v5 supports OIDC tokenless upload on public repos, but explicit token is more reliable across branch-protection scenarios and works identically for both public and private repos). Same `token` argument also added to the `codecov/test-results-action@v1` steps for Test Analytics so they authenticate consistently
- **`pytest --cov-branch` added** to the backend test step (branch coverage in addition to line coverage — distinguishes "both true/false legs of an `if` were tested" from "only one was"; more granular signal for the `codecov/patch` Check Run). Kept `--cov=app` (explicit scope) instead of Codecov wizard's `--cov` (auto-detect) because explicit is more deterministic
- **`fetch-depth: 2`** added to checkout step in `test-backend` and `test-frontend` jobs — Codecov needs HEAD + previous commit to compute diff coverage and resolve the PR base ref; default `fetch-depth: 1` was leaving Codecov to guess

### Removed

- **Changesets infrastructure** — `.changeset/` directory, `.github/workflows/changeset-check.yml`, `.github/workflows/release.yml`, `pnpm changeset:*` / `pnpm release` scripts, `@changesets/cli` + `@changesets/changelog-github` devDependencies, `needs-changeset` + `skip-changeset` labels. Rationale in `docs/decisions/RELEASE_TOOLING.md`
- **Label cleanup** — pruned 12 labels from the repo to make `.github/labels.yml` the single source of truth: `breaking-change` (redundant with `semver:major`), `needs-changeset` + `skip-changeset` (Changesets removed in `ebc141f`), 3 GitHub defaults overlapping with `type:*` (`bug` → `type:fix`, `documentation` → `type:docs`, `enhancement` → `type:feature`), and 6 unused GitHub triage defaults (`duplicate`, `good first issue`, `help wanted`, `invalid`, `question`, `wontfix`). Kept `security` (referenced by `renovate.json5` `vulnerabilityAlerts`) and added it to the manifest

### Security

- **Bumped `jackson-core` 2.15.3 → 2.18.7** across all 3 Flink modules — fixes 2 HIGH-severity Snyk findings (Allocation of Resources Without Limits / DoS, SNYK-JAVA-COMFASTERXMLJACKSONCORE-15907551 and -15365924)
- **Added dated `.snyk` ignores** for 8 HIGH transitive CVEs in `kafka-clients@3.4.0`, `commons-lang3@3.12.0`, and `lz4-java@1.8.0` — all require Flink 1.20 → 2.0 to remediate at the source. Each ignore carries a per-CVE rationale, in-context mitigation note, and an `expires: 2026-08-10` re-evaluation date. Tracked under task #24 (Flink 2.0 upgrade evaluation)
- **Snyk `.snyk` policy path** — added `--policy-path=.snyk` to both Snyk steps in `ci.yml`. With `--all-projects`, Snyk only auto-applies a `.snyk` file to manifests at the same path as the policy file; our Flink poms live under `src/flink-jobs/<module>/`, so without `--policy-path` the root ignores were silently skipped
- **Maven Snyk `--exclude`** — Maven step now uses basename-only exclude `tests,frontend,node_modules,.venv,venv` (mirrors the Python step). Stops the Maven scan from picking up npm/Python manifests and surfacing irrelevant cross-ecosystem CVEs
- **Bumped `next` 16.1.1 → 16.2.3** in the frontend — fixes Medium-severity [CVE-2025-59471](https://nvd.nist.gov/vuln/detail/CVE-2025-59471) (DoS via Image Optimizer `/_next/image` when `remotePatterns` is configured for external image domains; impact preemptive — we don't currently configure `remotePatterns`). Renovate-driven, PR #9 ([f2748eb])
- **Bumped `python-dotenv` → 1.2.2** in the backend — fixes Medium-severity [CVE-2026-28684](https://nvd.nist.gov/vuln/detail/CVE-2026-28684) (CVSS 6.6, local attack vector with required user interaction). Renovate-driven, PR #5 ([eaeb021])
- **Bumped `pyjwt` 2.10.1 → 2.12.0** in the backend — fixes [CVE-2026-32597](https://nvd.nist.gov/vuln/detail/CVE-2026-32597) (same vulnerability class as CVE-2025-59420 which rated CVSS 7.5 HIGH on Authlib). Renovate-driven, PR #7 ([8d312b6])
- **Bumped `pytest` 9.0.2 → 9.0.3** in the backend dev-deps — fixes Medium-severity [CVE-2025-71176](https://nvd.nist.gov/vuln/detail/CVE-2025-71176) (CVSS 6.8, predictable `/tmp/pytest-of-{user}` path allows local DoS or potential privilege escalation on UNIX; dev-tool only, not present in any production wheel). Renovate-driven, PR #4 ([137fd92])
- **Bumped `black` 25.12.0 → 26.3.1** in the backend dev-deps — fixes **HIGH-severity** [CVE-2026-32274](https://nvd.nist.gov/vuln/detail/CVE-2026-32274) (CVSS 8.7, most severe in this batch). Dry-run before merge confirmed identical reformatting behavior between v25 and v26 on this codebase (same 45-of-82 files flagged, same patches) so the bump is binary-only and reformats no source files. Black is not wired into pre-commit or CI — the project uses `ruff format` (line-length 100); `[tool.black]` config in `src/backend/pyproject.toml` (line-length 88) is orphan and a candidate for removal in a future cleanup. Renovate-driven, PR #10 ([7c84026])

### Fixed

- **`.coderabbit.yaml` `tone_instructions` parse error** — first CodeRabbit run on PR #10 (triggered `2026-05-13`) reported `Validation error: String must contain at most 250 character(s) at "tone_instructions"` and fell back to Organization-UI defaults instead of our YAML. The literal-block form (350+ chars) was over CodeRabbit's per-field limit. Replaced with a single-quoted flow scalar trimmed to 233 chars while preserving the substantive doctrine guidance (cite paths/lines, Conventional Commits, no premature abstractions, no WHAT-comments, advisory only). All 5 doctrine pre-checks remain in effect; once parsed, the YAML becomes the source of truth instead of dashboard defaults
- **Frontend test step ran in watch mode + masked failures** — `ci.yml` invoked `pnpm test --coverage --passWithNoTests`, which (a) launched vitest in watch mode (would never terminate), (b) passed flags pnpm didn't recognize, (c) was wrapped in `continue-on-error: true` so the broken step appeared green. Replaced with `pnpm test:coverage` (uses the existing `vitest run --coverage` script) and removed `continue-on-error`. The frontend's ~10 unit-test files now actually gate CI
- **Frontend coverage tooling** — added `@vitest/coverage-v8@4.0.16` as a frontend devDependency (vitest config required `provider: 'v8'` but the peer dep was never installed); exact-pinned `vitest` + `@vitest/coverage-v8` to `4.0.16` (no caret) because `@vitest/coverage-v8` declares vitest as an _exact_ peer — caret ranges let the two drift apart and produced a peer-dep mismatch on `pnpm install` (Renovate can group-bump them when newer 4.x lands); added `'lcov'` to `vitest.config.mts` `coverage.reporter` so the Codecov upload step in `ci.yml` actually receives coverage data instead of silently no-op'ing on a missing `lcov.info`
- **Flink SpotBugs cleanup across all 3 modules** — addressed 66 SpotBugs findings (13 event-router + 19 insight-generator + 34 pattern-matcher) and flipped `failOnError` from `false` (rollout) to `true` (gate) across all 3 module poms. Real fixes: `serialVersionUID = 1L` added to 19 Serializable classes; defensive `Collections.unmodifiableList/Map` getters and `new ArrayList/HashMap<>()` setter copies on DTO `Map`/`List` fields (`EventEnvelope`, `Insight`, both `PatternAlert`s) to close `EI_EXPOSE_REP`/`EI_EXPOSE_REP2`; narrowed `catch (Exception)` to `catch (NoSuchAlgorithmException)` in `sha256` helpers (3 places); replaced 6 double-brace-init anonymous `HashMap` blocks with `Map.of()` to close `SIC_INNER_SHOULD_BE_STATIC_ANON`; refactored `InsightAggregator.processElement` and `PatternWindowFunction.enrichWithScore` to use setters since getters now return unmodifiable views; narrowed `catch (Exception)` to `catch (RuntimeException)` in `ModelLoader.reloadFromMLflow` (best-effort skip on runtime failures; the inner `mlflowAdapter.checkForUpdate` already absorbs its own checked exceptions). False positives suppressed via shared `src/flink-jobs/spotbugs-exclude.xml` with rationale: `UWF_FIELD_NOT_INITIALIZED_IN_CONSTRUCTOR` on Flink RichFunction subclasses (state init is `open()` lifecycle, not constructor); `SE_TRANSIENT_FIELD_NOT_RESTORED` on Flink Schema impls (transient `ObjectMapper` re-initialized in `open()` plus defensive null-check guard); and `SE_TRANSIENT_FIELD_NOT_RESTORED` on `PatternWindowFunction.modelLoader` + `ModelLoader.{reloadScheduler,mlflowAdapter}` (re-initialized via the same `open()` / `initialize()` lifecycle — `ModelLoader` is held through a transient field, so the parent is never actually deserialized in our pipeline)

### Pending

- Database migration for auth tables (requires Docker)
- End-to-end auth flow testing
- ML training pipeline
- Real-time anomaly detection
- Cost optimization engine

---

## [0.8.0] - 2026-05-10

Sprint E5.1c platform release: multi-source normalizers, DLQ pipeline,
codified IAM, plus a CI hardening pass that brings Java tooling, Python
security rules, and a full pre-commit hooks suite online.

### Added

- **Multi-source normalizers** — `TerraformNormalizer` and `GitHubActionsNormalizer` join the existing K8s normalizer; each runs as its own Deployment with its own Kafka input topic and IRSA role ([26a8c59])
- **Per-source isolation guard** — `FW_EXPECTED_SOURCE` env var causes a normalizer pod to reject events whose `source` field doesn't match its declared scope; mismatches go to DLQ ([26a8c59])
- **DLQ pipeline on exception** — normalizer routes any unexpected processing error to `forge.dlq.events` instead of crashing; S3 errors propagate explicitly so they can be retried ([26a8c59])
- **CUE ↔ Pydantic schema fidelity gate** — CI job that diffs the CUE schemas in `src/normalizer/cue/` against the Pydantic models and fails on drift ([26a8c59])
- **IAM codified in `infra/iam/`** — the IRSA roles for the three normalizer service accounts now live in version-controlled Terraform/manifests instead of being applied ad-hoc ([26a8c59])
- **Pre-commit hooks suite** — 18 hooks across 7 upstream repos: trailing-whitespace, end-of-file-fixer, check-yaml/json/toml, check-merge-conflict, mixed-line-ending, ruff (lint + format), prettier, yamllint, markdownlint-cli2, shellcheck, hadolint, detect-secrets ([8ec09fc])
- **ruff `S` security rules** (flake8-bandit) added to root and backend ruff configs ([b5796ef])
- **Maven Spotless + SpotBugs** wired into all 3 Flink modules; Spotless enforces google-java-format 1.33.0 AOSP, SpotBugs runs at `effort=Max threshold=Low` with `failOnError=false` for the rollout phase ([b5796ef])
- **CI `java-build` matrix job** — runs `mvn verify` per Flink module on every push, gating Spotless + SpotBugs + tests ([b5796ef])
- **`docs/PRE_COMMIT_EVALUATION.md`** — decision framework and hook-by-hook verdict explaining the 18-hook selection ([8ec09fc], [b5796ef])
- **Engine Phase 6 forward reference** in `roadmap/ACTION_PLAN_ENGINE_PHASE-5.md` — points at the planned Agentic Reasoning Layer that consumes Phase 5's normalized context ([0eea6cd])

### Changed

- **CI Java toolchain bumped to JDK 21** (was 11) — required by google-java-format 1.33.0 which references `com/sun/tools/javac/tree/JCTree$JCAnyPattern` (Java 21+). Bytecode targets unchanged: each Flink pom keeps `<maven.compiler.source/target>11</>` ([9abab8b], [a101019])
- **Spotless plugin upgraded** 2.46.1 → 3.4.0 across all Flink poms; `<importOrder/>` and `<removeUnusedImports/>` removed so google-java-format owns ordering end-to-end ([178f400])
- **Java formatting authority consolidated to Spotless** — the `pretty-format-java` pre-commit hook was diverging from Spotless's import handling on every commit (likely a JVM-version effect between pre-commit's bundled JRE and the Maven JVM); the hook was removed and `mvn spotless:apply` is now sole authority ([178f400])
- **`markdownlint` allowed_elements** — added `p` and `em` to MD033 allow-list for the README's centered architecture-image pattern ([a101019])
- **Normalizer package discovery** — `src/normalizer/pyproject.toml` switched from explicit `packages = ["app"]` to `[tool.setuptools.packages.find]` with `include=["app*"]` so newly added subpackages auto-discover ([6471248])

### Fixed

- **Normalizer wheel was missing `app.normalizers` and `app.routes` subpackages** — `pyproject.toml` declared `packages = ["app"]` which ships only top-level `app/` files; pods crashed at startup with `ModuleNotFoundError: No module named 'app.normalizers'` and were CrashLoopBackOff on dev cluster for ~22h before discovery during v0.8.0 cluster verify ([6471248])
- **Backend `ruff` UP042 violations on CI Lint** — root `ruff.toml` added UP042 to ignore, but `src/backend/pyproject.toml` has its own `[tool.ruff]` block which wins by setuptools' nearest-config precedence; UP042 added to the backend block too ([178f400])
- **Markdownlint debt cleared** across 6 docs files: heading-increment in `AWS_INFRA_ACTION_PLAN.md` (4×) and `NAMING_CONVENTION.md`; table-pipe-style + table-column-count in `DOMAIN_VOCABULARY.md` (3 tables); trailing whitespace in `DOMAIN_VOCABULARY.md` + `BRAINSTORM.md` + `Brainstorm-Discussion.md` + `FEEDBACK_LOOP.md`; missing trailing newline in `STACK.md`; broken link fragment in `EKS_OPERATIONS.md`; duplicate `## References` heading in `roadmap/TASKS.md` ([a101019])
- **Yamllint long-line errors** in `.github/workflows/normalizer-image.yml` (lines 41 / 108 / 109 exceeded 120 chars) — refactored URL into shell vars and grouped step-summary echoes into a single redirect block ([9abab8b])
- **GHCR provenance-attestation rejection** on normalizer image push — added `provenance: false` to `docker/build-push-action@v5` step ([bed15fa])
- **Editable-install package discovery** in CI for the normalizer test job ([88a1940])
- **Hadolint DL3008 false-positive** on normalizer Dockerfile — suppressed to match the existing backend Dockerfile pattern ([d139725])

### Internal

- **`.gitignore`** now excludes `.claude/`, `.cursor/`, and `.codex/` from remote tracking; previously-tracked AI-tooling files removed from the repo ([ab8dfde], [1823fe0], [02656f3])
- Editor-config tweaks consolidated under the gitignore work ([73c1c65], [a90e5d9])

---

## [0.4.0] - 2025-01-14

### Phase 4: Real-time - Complete

#### WebSocket Infrastructure (Sprint 4.1)

- **Connection Manager**
  - Channel-based subscriptions (services, anomalies, pipelines, kubernetes)
  - Automatic reconnection with exponential backoff
  - Heartbeat/ping-pong for connection health
  - Broadcast events to subscribed clients

- **Frontend Integration**
  - `useWebSocket` hook for real-time connections
  - `useRealtimeServices` hook for service updates
  - `useRealtimeAnomalies` hook for anomaly alerts
  - `useRealtimePipelines` hook for pipeline status
  - `useRealtimeKubernetes` hook for K8s updates
  - TanStack Query cache invalidation on events
  - Connection status indicator component

#### Kubernetes Adapter (Sprint 4.2)

- **Backend Adapter** (`src/backend/app/adapters/kubernetes.py`)
  - Mock/Live mode switching via environment
  - Cluster info and health status
  - Namespace listing and management
  - Node status with resource metrics (CPU/Memory)
  - Deployment status with replica counts
  - Pod health with container states
  - Pod log retrieval

- **API Routes** (`/api/v1/kubernetes`)
  - `GET /cluster` - Cluster information
  - `GET /stats` - Aggregate statistics
  - `GET /namespaces` - List namespaces
  - `GET /nodes` - List nodes with metrics
  - `GET /deployments` - List deployments
  - `GET /deployments/{ns}/{name}` - Deployment details
  - `GET /pods` - List pods
  - `GET /pods/{ns}/{name}/logs` - Pod logs

- **KubernetesLayer Component**
  - View modes: Overview, Deployments, Pods, Nodes
  - Stats cards with health indicators
  - Resource utilization progress bars
  - Real-time status updates
  - Collapsible deployment details

#### CI/CD Workflows (Sprint 4.3)

- **Unified CI Workflow** (`.github/workflows/ci.yml`)
  - Lint job: Ruff (Python) + ESLint (TypeScript) + TypeCheck
  - Security job: Gitleaks + Snyk
  - Test Backend: pytest with coverage → Codecov
  - Test Frontend: vitest with coverage → Codecov
  - Build: Docker (backend) + Next.js (frontend)
  - CI Success gate for all jobs

- **Supporting Workflows**
  - Release workflow with Changesets
  - Changeset validation on PRs
  - Auto-labeler for packages
  - Labels sync from configuration

#### Bug Fixes

- Fixed hydration mismatch in LayerPanel (DndContext client-only rendering)
- Fixed `layer.glueKeys` undefined error with optional chaining
- Added placeholder UI for server-side rendering

#### Repository Maintenance

- Added TypeScript, ESLint, Ruff, Snyk badges to README
- Updated .gitignore with AI/Codex directory exclusions
- Removed tracked .codex directories from repository

---

## [0.3.0] - 2025-01-12

### Phase 3: Experience - Complete

#### Frontend Dashboard (Next.js 14+)

- **Layer Architecture Implementation**
  - LayerPanel with visibility toggles and drag reordering
  - LayerRenderer with lazy loading and Suspense boundaries
  - GlueBus pub/sub system for cross-layer communication

- **Layer Components (5 layers)**
  - ServicesLayer: Service catalog with health status, filtering, actions
  - TemplatesLayer: Golden path templates with recommendations
  - AnomaliesLayer: Anomaly detection with acknowledge/resolve workflows
  - PipelineLayer: GitHub workflow runs and deployment status
  - MetricsLayer: DORA metrics, health scores, deployment stats

- **UI Components (shadcn/ui)**
  - Card, Button, Badge, Skeleton, Progress, Accordion
  - Custom StatusBadge, ServiceCard, MetricCard components
  - Responsive design with Tailwind CSS

- **State Management**
  - Zustand store for layer state persistence
  - TanStack Query for data fetching with caching
  - Real-time updates with configurable refresh intervals

#### Backend Enhancements

- **Anomalies API** (`/api/v1/anomalies`)
  - Full CRUD operations
  - Acknowledge/Resolve workflows
  - Filtering by severity, type, status
  - Statistics endpoint

- **Metrics API** (`/api/v1/metrics`)
  - Comprehensive dashboard metrics
  - DORA metrics calculation
  - Service health aggregation
  - Deployment statistics

- **Live Adapters**
  - GitHubLiveAdapter: Repository info, workflow runs, commits
  - ArgoCDLiveAdapter: Application sync status, health checks
  - Mock/Live mode switching via environment variables

#### Testing & Quality

- 31 frontend tests (unit + integration)
- Accessibility audit (WCAG 2.1 AA compliance)
- Performance optimization (lazy loading, memoization)
- TypeScript strict mode compliance

#### Documentation

- Comprehensive API documentation (`docs/API.md`)
- 10 Mermaid architecture diagrams (`docs/diagrams/SYSTEM_DIAGRAMS.md`)
  - System Context (C4 L1)
  - Container Diagram (C4 L2)
  - Component Diagrams (C4 L3) - Backend & Frontend
  - Data Flow, Deployment, Sequence Diagrams
  - ERD, Layer Architecture, Adapter Pattern diagrams

---

## [0.2.0] - 2025-01-08

### Phase 2: Intelligence - Complete

#### Added

- ML Template Recommender endpoint (`POST /api/v1/templates/recommend`)
- Rule-based recommendation model
- Anti-pattern warning detection
- Override logging and audit trail
- Training data generation (750+ synthetic records)
- Workload-to-template scoring logic

#### Technical Details

- Recommendation input: workload_type, language, requirements
- Recommendation output: ranked templates with scores, warnings
- Response time target: <500ms achieved

---

## [0.1.0] - 2025-01-08

### Phase 1: Foundation - Complete

#### Added

- TurboRepo + PNPM monorepo architecture
- FastAPI backend structure
- Service Catalog API with full CRUD operations
  - `GET /api/v1/services` - List all services
  - `GET /api/v1/services/{id}` - Service detail
  - `POST /api/v1/services` - Create service
  - `GET /api/v1/services/stats` - Service statistics
- Template API with ML-powered recommendations (rule-based Phase 1)
  - `GET /api/v1/templates` - List templates
  - `GET /api/v1/templates/{id}` - Template detail
- Database schema with Alembic migrations
- Database models: Team, Service, Template, Anomaly, Recommendation, Action
- Async SQLAlchemy with PostgreSQL support
- Pydantic schemas for request/response validation
- Seed data for demo purposes
- Health check endpoints with adapter status
- Mock adapters for GitHub and ArgoCD integrations
- Docker Compose for local development
- PNPM workspaces for frontend/backend/shared packages
- Comprehensive documentation (MONOREPO_SETUP.md, LOCAL_DEV.md)
- Project configuration files (LICENSE, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md)
- Integration tests with pytest-asyncio

#### Infrastructure

- PostgreSQL 15 database
- GitHub adapter with mock mode for repositories, branches, commits, PRs, and workflows
- ArgoCD adapter with mock mode for applications, sync operations, and deployment status

---

## [0.0.1] - 2025-01-06

### Phase 0: Planning - Complete

#### Added

- Initial project scaffold
- Project vision and identity (`planning/VISION.md`)
- Feature scope definition (`planning/SCOPE.md`)
- System architecture design (`decisions/ARCHITECTURE.md`)
- Technology stack selection (`decisions/TECH_STACK.md`)
- Architectural Decision Records (ADR 001-006)
- MVP definition (`docs/MVP.md`)
- Success criteria (`docs/SUCCESS_CRITERIA.md`)
- Golden Path requirements (`docs/GOLDEN_PATH_REQUIREMENTS.md`)
- Phase-based roadmap (`roadmap/PHASE.md`)
- Task management framework (`roadmap/TASKS.md`)
- Prioritization framework (`roadmap/PRIORITIZATION.md`)
- Action plan templates (`roadmap/ACTION_PLAN.md`)

---

## Version History Summary

| Version | Phase                 | Status       | Date       |
| ------- | --------------------- | ------------ | ---------- |
| 1.0.0   | Phase 5: Intelligence | Planned      | TBD        |
| 0.4.0   | Phase 4: Real-time    | **Complete** | 2025-01-14 |
| 0.3.0   | Phase 3: Experience   | Complete     | 2025-01-12 |
| 0.2.0   | Phase 2: Intelligence | Complete     | 2025-01-08 |
| 0.1.0   | Phase 1: Foundation   | Complete     | 2025-01-08 |
| 0.0.1   | Phase 0: Planning     | Complete     | 2025-01-06 |

---

## Links

- [Phase Definitions](roadmap/PHASE.md)
- [Task Management](roadmap/TASKS.md)
- [Phase 3 Action Plan](roadmap/ACTION_PLAN_PHASE3.md)
- [Layers Architecture](docs/features/LAYERS_ARCHITECTURE.md)
- [Prioritization Framework](roadmap/PRIORITIZATION.md)
