# ML Models Directory

This directory contains trained ML models.

- `template_recommender.pkl` - Template recommendation model

## Training

To train a new model:

```bash
cd src/backend
python -m app.ml.train
```
