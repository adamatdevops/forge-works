---
name: gh-address-comments
vendor: openai
slug: openai/gh-address-comments
source-url: https://github.com/openai/skills/tree/main/skills/.curated/gh-address-comments
source-canonical: https://github.com/openai/skills/tree/c25113bf4c64c8dba6bfe61acf06051d79aa43f6/skills/.curated/gh-address-comments
source-sha: c25113bf4c64c8dba6bfe61acf06051d79aa43f6
audited: 2026-05-20
goal: 4
tier: 2
tool-scope: shell-execute+repo-write
target-agents: [claude-code, codex]
context-cost-tokens: 224
owner: adamatdevops
---

<!-- Source: https://github.com/openai/skills/tree/c25113bf4c64c8dba6bfe61acf06051d79aa43f6/skills/.curated/gh-address-comments/SKILL.md · SHA: c25113bf4c64c8dba6bfe61acf06051d79aa43f6 · Audited: 2026-05-20 · **22nd vendored skill + 2nd OpenAI-vendor adoption (after §T security-threat-model) + FIRST OpenAI cluster (§T + §AT at shared SHA `c25113bf...`) + SIXTH cluster overall in framework (after hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2 / Anthropic 2) + ELEVENTH consecutive Tier 1→2 estimate-confirm at adoption review (§AT's eval-list pre-adoption Tier 2 estimate HELD per new §4.4.3 step 2 audit — second consecutive estimate-correct adoption after §AS; N=14 staleness datapoints with 11 corrections + 3 deferrals).** **Tier-staleness audit (per new §4.4.3 step 2 checklist):** (i) **YES — file-write directives in SKILL.md step 3 "Apply fixes for the selected comments"**: agent edits source files in the PR branch to address review feedback (per the SKILL.md L24 directive); file-EDIT (not file-CREATE — agent modifies EXISTING source files) → Tier 1 transform OR Tier 2 repo-write per §J R3-A-3 — clarified at adoption: SKILL.md is generic about WHAT files to edit (depends on PR comments which depend on PR content), so the operation surface is "edit existing files" → Tier 1 transform; however the broader gh-CLI surface (shell-execute) drives the overall Tier 2 classification. (ii) NO `allowed-tools` frontmatter (host's default tool envelope, same pattern as §AM/§AQ/§AS); (iii) NO `commands/` directory (only `agents/` + `assets/` + `scripts/`); (iv) **`gh` CLI shell-execute** — SKILL.md L8 "Run all `gh` commands with elevated network access" + L12 "Prereq: ensure `gh` is authenticated (for example, run `gh auth login` once)" + L20 "Run scripts/fetch_comments.py" which itself uses subprocess to invoke `gh api graphql`. Same pattern as §H differential-review (gh-CLI shell-execute subsumes network into shell-execute scope); NO standalone network egress declared; (v) NO `Task` tool / cross-agent surface declared. **Final Tier 2 verdict for §AT gh-address-comments**: `shell-execute+repo-write` — runs `gh` CLI + `python scripts/fetch_comments.py` via Bash + edits source files in the PR branch via Edit tool. Matches §H differential-review + §AQ mcp-builder pattern. **`agents/openai.yaml` doctrinal observation (NEW framework pattern surfaced — DIFFERENT from Claude's `agents/` convention):** the `agents/` directory in OpenAI's skill format contains **OpenAI plugin-marketplace UI metadata** (display_name, short_description, icon_small/large, default_prompt) — NOT Claude sub-agent definitions per AGENT_SKILLS.md §2E condition (b). The 303-byte `agents/openai.yaml` file is HARMLESS branding-only metadata. **DO NOT CONFUSE** this with Claude's `agents/*.md` sub-agent definitions (e.g., §D anthropics/skill-creator's `agents/{analyzer,comparator,grader}.md`, §AP semgrep's `agents/semgrep-scanner.md`). Future audits MUST inspect file content to distinguish OpenAI-format YAML branding vs Claude-format markdown sub-agent specs. Vendored verbatim per AGENT_SKILLS.md §C provenance rule (the YAML is irrelevant to forge-skills loader which doesn't read it, but preserved for cross-platform provenance). Flagged for §4.4.5 quarterly codification of the "agents/ directory disambiguation" rule. **`scripts/fetch_comments.py` R2-2 audit:** ✅ `subprocess.run(['gh', 'api', 'graphql', ...])` (process-spawn YES — invokes gh CLI to query GitHub GraphQL API); ✅ NO `claude`/`anthropic`/`openai`/`messages.create`/SDK tokens — pure GitHub API wrapper that prints PR comment JSON to stdout. Per §2E "Bundled-script same-model self-invocation" clause: NON-APPLICABLE (no model-client invocation). SKILL.md L20 DIRECTS agent to run the script ("Run scripts/fetch_comments.py which will print out all the comments and review threads on the PR") — this is in-scope agent-execution, NOT cross-agent (same execution boundary as agent's Bash tool). **Read-only on GitHub (NOT external-write — distinguishes §AT from §AR):** §AT READS PR comments via `gh api graphql` (network read) + EDITS local files in the PR branch + the user handles commit/push per AGENTS.md §3.3 commit-message authority + memory `[[feedback_commit_push_authority]]`. §AT does NOT post reply comments to GitHub (contra §AR which uses `gh pr comment`) and does NOT push commits to github.com (contra §AR which uses `git push`). Therefore §AT is Tier 2 (gh-CLI shell-execute + repo-write) NOT Tier 3 (no external-write). **Distinct from §AR coderabbitai/autofix** — both handle PR comments but: (a) §AT is GENERIC (any commenter; CodeRabbit, human reviewer, dependabot, etc.); (b) §AR is CodeRabbit-specific (verifies author is `coderabbitai`/`coderabbit[bot]`/`coderabbitai[bot]` per §AR gate #4). (c) §AT is Tier 2 (read + edit); §AR is Tier 3 external-write (read + edit + post-comment + push). (d) §AT requires user clarification on which comments to address (SKILL.md L17-18 "Ask the user which numbered comments should be addressed"); §AR requires per-change approval gate. **Canonical pairing patterns:** (i) generic PR comment review: invoke §AT to fetch + summarize comments + edit selectively; (ii) CodeRabbit-specific autofix: invoke §AR (which adds external-write surface); (iii) hybrid: §AT for human-reviewer comments + §AR for CodeRabbit-bot comments in the same PR (both at same SHA c25113bf for §T cluster but DIFFERENT vendor cluster — §AT in OpenAI cluster, §AR in CodeRabbit cluster). **`assets/` directory** — 2 image files (github-small.svg 853b + github.png 1838b) — OpenAI plugin branding only; NOT used by forge-skills loader; vendored verbatim per provenance. **OpenAI cluster (FIRST):** §T + §AT share repo `openai/skills` + SHA `c25113bf...` — FIRST OpenAI cluster in framework. Per §4.4.5 cluster-co-adoption note: cluster requires SHARED SHA — verified at both `§T security-threat-model` and `§AT gh-address-comments` paths in the same repo at `c25113bf...`. Quarterly source-SHA review for the 2-skill OpenAI cluster MAY use ONE shared upstream check covering both skills (each Decision Record still independently attested). **Cluster-co-adoption pattern empirical validation EXTENDED to 6 vendors:** with 6 clusters now (hamelsmu 5 / ToB 7 / obra 2 / CodeRabbit 2 / Anthropic 2 / OpenAI 2), the §4.4.5 cluster-co-adoption pattern is validated across 6 vendor profiles + 6 cluster sizes (5, 7, 2, 2, 2, 2) + 5 Tier-mix shapes (hamelsmu all-Tier-1 / ToB mixed Tier-1+Tier-2 / obra Tier-1+Tier-1 / CodeRabbit all-Tier-3 / Anthropic Tier-2+Tier-2 / OpenAI Tier-2+Tier-2). VERY STRONG case for 2026-Q3 normative elevation of the cluster-co-adoption rule from provisional to normative — 6 of 6 framework vendors with multi-skill adoptions now form clusters where SHAs align, and the optimization (one shared SHA check per cluster) holds in 100% of cases. **§T gate entry BACKPATCHED in this commit** per §4.5.3 sister-reference status maintenance rule (codified at §AK): §T was previously solo on the OpenAI vendor; now has §AT as cluster sibling at shared SHA. **§T Ordering basis backpatch (per §4.5.5 later-sibling rule):** OpenAI cluster sequence §T (2026-05-17 audit) → §AT (2026-05-20 audit). **Bundle:** SKILL.md 282t full-file (224t body) — TINIEST SKILL.md in framework + 4 companion files (agents/openai.yaml 62t OpenAI marketplace metadata + 2 assets/*.{svg,png} 937t branding only + scripts/fetch_comments.py 1549t PR-comment-fetching wrapper) + LICENSE.txt 2270t Apache 2.0. **Worst-case archive 5100 tokens** — slightly over §3B 5K-preferred cap (driven primarily by LICENSE 2270t + fetch_comments.py 1549t); the 4 companion files including OpenAI metadata are vendored for provenance (not all load-bearing for agent operation — only `scripts/fetch_comments.py` is directed by SKILL.md). **Body sha256:** `760c50276d5ad1ec...`. **Phase-A/Phase-B handling per §4.5.2 step 4 Tier 2:** §AT is Phase B from invocation start in most cases — Step 1 begins with `gh auth status` + `python scripts/fetch_comments.py` (both Phase B Bash). Phase A applies only if PR comments are already in session context (e.g., user pasted the PR review JSON) AND agent only edits local files based on those comments (rare). Most invocations fire neutral 3-option confirmation prompt from step 1. **Direct project fit for forge-works:** pairs naturally with §V/§AR CodeRabbit cluster + §H differential-review for the PR review workflow. The user already runs CodeRabbit reviews on forge-works PRs; §AT extends to handling NON-CodeRabbit reviewer comments (human reviewers, dependabot suggestions, GitHub Copilot suggestions, etc.). **Sister-skill cross-references:** ZERO direct cross-references to other adopted skills (the only adjacency is the §T cluster co-adoption at shared SHA — distinct skills with distinct workflows; §T is threat-modeling, §AT is PR-comment-handling). NO transitive `load skill X` refs, NO Claude-format bundled subagents (the `agents/openai.yaml` is OpenAI marketplace metadata, not a Claude sub-agent spec), NO `claude -p` CLI subprocess paths, NO model-client invocations across the 4 companion files. -->


# PR Comment Handler

Guide to find the open PR for the current branch and address its comments with gh CLI. Run all `gh` commands with elevated network access.

Prereq: ensure `gh` is authenticated (for example, run `gh auth login` once), then run `gh auth status` with escalated permissions (include workflow/repo scopes) so `gh` commands succeed. If sandboxing blocks `gh auth status`, rerun it with `sandbox_permissions=require_escalated`.

## 1) Inspect comments needing attention
- Run scripts/fetch_comments.py which will print out all the comments and review threads on the PR

## 2) Ask the user for clarification
- Number all the review threads and comments and provide a short summary of what would be required to apply a fix for it
- Ask the user which numbered comments should be addressed

## 3) If user chooses comments
- Apply fixes for the selected comments

Notes:
- If gh hits auth/rate issues mid-run, prompt the user to re-authenticate with `gh auth login`, then retry.
